"""Page builders for batch fundamentals 2."""

from __future__ import annotations

from _batch_fundamentals_2_data import cond_index

LANGS = ["de", "es", "fr", "it", "ja", "ko", "nl", "pl", "pt-br", "ru", "sv", "th", "zh-cn", "zh-tw"]

def _j(*p):
    return "\n".join(p)


def _page_content_hub_scripting_fundamentals_Conditionals_conditionals_cond_md(lang: str) -> tuple[str, str]:
    bodies = {'de': {'title': 'cond', 'body': 'In Scheme wählt das bedingte `cond` anhand mehrerer Tests einen von mehreren Codeblöcken — wie ein mehrgliedriges `if`, geprüft in Reihenfolge bis zum ersten Treffer.\n\n### Syntax\n\n```scheme\n(cond\n  (test-1 consequent-1)\n  (test-2 consequent-2)\n  ...\n  (else fallback-consequent))\n```\n\n- Tests in Schreibreihenfolge.\n- Erster `#t`-Test: **consequent** läuft, `cond` stoppt.\n- `else` optional als Fallback.\n\n### So funktioniert es\n\n1. **Jede Bedingung testen** in Reihenfolge.\n2. **Passenden consequent ausführen**; sonst `else` falls vorhanden.\n\n### Beispiele\n\n#### Beispiel 1: Einzeilige consequents\n\n```scheme\n(cond\n  ((< 3 2) "This won\'t run")\n  ((= 3 3) "This will run")\n  (else "Fallback"))\n```\n\nErgebnis: **"This will run"**\n\n#### Beispiel 2: Mehrere Aktionen mit `begin`\n\n```scheme\n(cond\n  ((< 5 3)\n    (begin\n      (lumi-message "This won\'t run")\n      (* 2 3)))\n  ((> 5 3)\n    (begin\n      (lumi-message "Condition met")\n      (* 5 5)))\n  (else\n    (begin\n      (lumi-message "Fallback")\n      0)))\n```\n\nErgebnis: **Gibt "Condition met" aus und liefert 25.**\n\n#### Beispiel 3: `let` im consequent\n\n```scheme\n(cond\n  ;; Fall 1: Wenn 0 kleiner als -1 ist\n  ((< 0 -1)\n    (let ((x 10))\n      (* x x)))\n\n  ;; Fall 2: Wenn 0 größer als -1 ist\n  ((> 0 -1)\n    (let ((y 20))\n      (lumi-message "Positive condition met")\n      (+ y y)))\n\n  ;; Standardfall: Wenn keine der obigen Bedingungen zutrifft\n  (else\n    (let ((z 0))\n      z)))\n```\n\nErgebnis: **Gibt "Positive condition met" aus und liefert 40.**\n\n#### Beispiel 4: Fallback mit `else`\n\n```scheme\n(cond\n  ((< 5 3) "This won\'t run")\n  ((= 5 3) "This won\'t run either")\n  (else "Fallback value"))\n```\n\nErgebnis: **"Fallback value"**\n\n### Zusammenfassung\n\n- `cond` für mehrere Bedingungen klar und kompakt.\n- consequents einzeln oder mit `begin` gruppiert.\n- `let` für lokale Variablen; `else` als Fallback empfohlen.'}, 'es': {'title': 'cond', 'body': 'En Scheme, el condicional `cond` elige uno de varios bloques según múltiples pruebas — como un `if` multirrama evaluado en orden hasta el primer acierto.\n\n### Sintaxis\n\n```scheme\n(cond\n  (test-1 consequent-1)\n  (test-2 consequent-2)\n  ...\n  (else fallback-consequent))\n```\n\n- Pruebas en orden.\n- Primer `#t`: **consequent** ejecutado, `cond` para.\n- `else` opcional como respaldo.\n\n### Cómo funciona\n\n1. **Probar cada condición** en orden.\n2. **Ejecutar el consequent** correspondiente; si no, `else` si existe.\n\n### Ejemplos\n\n#### Ejemplo 1: consecuentes de una expresión\n\n```scheme\n(cond\n  ((< 3 2) "This won\'t run")\n  ((= 3 3) "This will run")\n  (else "Fallback"))\n```\n\nResultado: **"This will run"**\n\n#### Ejemplo 2: varias acciones con `begin`\n\n```scheme\n(cond\n  ((< 5 3)\n    (begin\n      (lumi-message "This won\'t run")\n      (* 2 3)))\n  ((> 5 3)\n    (begin\n      (lumi-message "Condition met")\n      (* 5 5)))\n  (else\n    (begin\n      (lumi-message "Fallback")\n      0)))\n```\n\nResultado: **Imprime "Condition met" y devuelve 25.**\n\n#### Ejemplo 3: bloque `let` en el consecuente\n\n```scheme\n(cond\n  ;; Caso 1: si 0 es menor que -1\n  ((< 0 -1)\n    (let ((x 10))\n      (* x x)))\n\n  ;; Caso 2: si 0 es mayor que -1\n  ((> 0 -1)\n    (let ((y 20))\n      (lumi-message "Positive condition met")\n      (+ y y)))\n\n  ;; Caso por defecto: si ninguna condición anterior se cumple\n  (else\n    (let ((z 0))\n      z)))\n```\n\nResultado: **Imprime "Positive condition met" y devuelve 40.**\n\n#### Ejemplo 4: respaldo con `else`\n\n```scheme\n(cond\n  ((< 5 3) "This won\'t run")\n  ((= 5 3) "This won\'t run either")\n  (else "Fallback value"))\n```\n\nResultado: **"Fallback value"**\n\n### Resumen\n\n- `cond` para varias condiciones con claridad.\n- Consecuentes simples o con `begin`.\n- `let` para variables locales; `else` recomendado.'}, 'fr': {'title': 'cond', 'body': 'En Scheme, le conditionnel `cond` sélectionne l\'un de plusieurs blocs à exécuter selon plusieurs tests — comme un `if` à branches multiples, évalué dans l\'ordre jusqu\'au premier succès.\n\n### Syntaxe\n\n```scheme\n(cond\n  (test-1 consequent-1)\n  (test-2 consequent-2)\n  ...\n  (else fallback-consequent))\n```\n\n- Tests dans l\'ordre d\'écriture.\n- Premier `#t` : **consequent** exécuté, `cond` s\'arrête.\n- `else` optionnel en repli.\n\n### Comment ça marche\n\n1. **Tester chaque condition** dans l\'ordre.\n2. **Exécuter le consequent** correspondant ; sinon `else` si présent.\n\n### Exemples\n\n#### Exemple 1 : conséquents sur une expression\n\n```scheme\n(cond\n  ((< 3 2) "This won\'t run")\n  ((= 3 3) "This will run")\n  (else "Fallback"))\n```\n\nRésultat : **"This will run"**\n\n#### Exemple 2 : actions multiples avec `begin`\n\n```scheme\n(cond\n  ((< 5 3)\n    (begin\n      (lumi-message "This won\'t run")\n      (* 2 3)))\n  ((> 5 3)\n    (begin\n      (lumi-message "Condition met")\n      (* 5 5)))\n  (else\n    (begin\n      (lumi-message "Fallback")\n      0)))\n```\n\nRésultat : **Affiche « Condition met » et renvoie 25.**\n\n#### Exemple 3 : bloc `let` dans un conséquent\n\n```scheme\n(cond\n  ;; Cas 1 : si 0 est inférieur à -1\n  ((< 0 -1)\n    (let ((x 10))\n      (* x x)))\n\n  ;; Cas 2 : si 0 est supérieur à -1\n  ((> 0 -1)\n    (let ((y 20))\n      (lumi-message "Positive condition met")\n      (+ y y)))\n\n  ;; Cas par défaut : si aucune condition ci-dessus n\'est remplie\n  (else\n    (let ((z 0))\n      z)))\n```\n\nRésultat : **Affiche « Positive condition met » et renvoie 40.**\n\n#### Exemple 4 : repli avec `else`\n\n```scheme\n(cond\n  ((< 5 3) "This won\'t run")\n  ((= 5 3) "This won\'t run either")\n  (else "Fallback value"))\n```\n\nRésultat : **"Fallback value"**\n\n### Résumé\n\n- `cond` pour plusieurs conditions clairement.\n- Conséquents simples ou groupés avec `begin`.\n- `let` pour variables locales ; `else` recommandé en repli.'}, 'it': {'title': 'cond', 'body': 'En Scheme, le conditionnel `cond` sélectionne l\'un de plusieurs blocs à exécuter selon plusieurs tests — comme un `if` à branches multiples, évalué dans l\'ordre jusqu\'au premier succès.\n\n### Syntaxe\n\n```scheme\n(cond\n  (test-1 consequent-1)\n  (test-2 consequent-2)\n  ...\n  (else fallback-consequent))\n```\n\n- Tests dans l\'ordre d\'écriture.\n- Premier `#t` : **consequent** exécuté, `cond` s\'arrête.\n- `else` optionnel en repli.\n\n### Comment ça marche\n\n1. **Tester chaque condition** dans l\'ordre.\n2. **Exécuter le consequent** correspondant ; sinon `else` si présent.\n\n### Exemples\n\n#### Exemple 1 : conséquents sur une expression\n\n```scheme\n(cond\n  ((< 3 2) "This won\'t run")\n  ((= 3 3) "This will run")\n  (else "Fallback"))\n```\n\nRésultat : **"This will run"**\n\n#### Exemple 2 : actions multiples avec `begin`\n\n```scheme\n(cond\n  ((< 5 3)\n    (begin\n      (lumi-message "This won\'t run")\n      (* 2 3)))\n  ((> 5 3)\n    (begin\n      (lumi-message "Condition met")\n      (* 5 5)))\n  (else\n    (begin\n      (lumi-message "Fallback")\n      0)))\n```\n\nRésultat : **Affiche « Condition met » et renvoie 25.**\n\n#### Exemple 3 : bloc `let` dans un conséquent\n\n```scheme\n(cond\n  ;; Caso 1: se 0 è minore di -1\n  ((< 0 -1)\n    (let ((x 10))\n      (* x x)))\n\n  ;; Caso 2: se 0 è maggiore di -1\n  ((> 0 -1)\n    (let ((y 20))\n      (lumi-message "Positive condition met")\n      (+ y y)))\n\n  ;; Caso predefinito: se nessuna condizione sopra è soddisfatta\n  (else\n    (let ((z 0))\n      z)))\n```\n\nRésultat : **Affiche « Positive condition met » et renvoie 40.**\n\n#### Exemple 4 : repli avec `else`\n\n```scheme\n(cond\n  ((< 5 3) "This won\'t run")\n  ((= 5 3) "This won\'t run either")\n  (else "Fallback value"))\n```\n\nRésultat : **"Fallback value"**\n\n### Résumé\n\n- `cond` pour plusieurs conditions clairement.\n- Conséquents simples ou groupés avec `begin`.\n- `let` pour variables locales ; `else` recommandé en repli.'}, 'ja': {'title': 'cond', 'body': 'En Scheme, le conditionnel `cond` sélectionne l\'un de plusieurs blocs à exécuter selon plusieurs tests — comme un `if` à branches multiples, évalué dans l\'ordre jusqu\'au premier succès.\n\n### Syntaxe\n\n```scheme\n(cond\n  (test-1 consequent-1)\n  (test-2 consequent-2)\n  ...\n  (else fallback-consequent))\n```\n\n- Tests dans l\'ordre d\'écriture.\n- Premier `#t` : **consequent** exécuté, `cond` s\'arrête.\n- `else` optionnel en repli.\n\n### Comment ça marche\n\n1. **Tester chaque condition** dans l\'ordre.\n2. **Exécuter le consequent** correspondant ; sinon `else` si présent.\n\n### Exemples\n\n#### Exemple 1 : conséquents sur une expression\n\n```scheme\n(cond\n  ((< 3 2) "This won\'t run")\n  ((= 3 3) "This will run")\n  (else "Fallback"))\n```\n\nRésultat : **"This will run"**\n\n#### Exemple 2 : actions multiples avec `begin`\n\n```scheme\n(cond\n  ((< 5 3)\n    (begin\n      (lumi-message "This won\'t run")\n      (* 2 3)))\n  ((> 5 3)\n    (begin\n      (lumi-message "Condition met")\n      (* 5 5)))\n  (else\n    (begin\n      (lumi-message "Fallback")\n      0)))\n```\n\nRésultat : **Affiche « Condition met » et renvoie 25.**\n\n#### Exemple 3 : bloc `let` dans un conséquent\n\n```scheme\n(cond\n  ;; ケース 1: 0 が -1 より小さい場合\n  ((< 0 -1)\n    (let ((x 10))\n      (* x x)))\n\n  ;; ケース 2: 0 が -1 より大きい場合\n  ((> 0 -1)\n    (let ((y 20))\n      (lumi-message "Positive condition met")\n      (+ y y)))\n\n  ;; デフォルト: 上記の条件がいずれも満たされない場合\n  (else\n    (let ((z 0))\n      z)))\n```\n\nRésultat : **Affiche « Positive condition met » et renvoie 40.**\n\n#### Exemple 4 : repli avec `else`\n\n```scheme\n(cond\n  ((< 5 3) "This won\'t run")\n  ((= 5 3) "This won\'t run either")\n  (else "Fallback value"))\n```\n\nRésultat : **"Fallback value"**\n\n### Résumé\n\n- `cond` pour plusieurs conditions clairement.\n- Conséquents simples ou groupés avec `begin`.\n- `let` pour variables locales ; `else` recommandé en repli.'}, 'ko': {'title': 'cond', 'body': 'En Scheme, le conditionnel `cond` sélectionne l\'un de plusieurs blocs à exécuter selon plusieurs tests — comme un `if` à branches multiples, évalué dans l\'ordre jusqu\'au premier succès.\n\n### Syntaxe\n\n```scheme\n(cond\n  (test-1 consequent-1)\n  (test-2 consequent-2)\n  ...\n  (else fallback-consequent))\n```\n\n- Tests dans l\'ordre d\'écriture.\n- Premier `#t` : **consequent** exécuté, `cond` s\'arrête.\n- `else` optionnel en repli.\n\n### Comment ça marche\n\n1. **Tester chaque condition** dans l\'ordre.\n2. **Exécuter le consequent** correspondant ; sinon `else` si présent.\n\n### Exemples\n\n#### Exemple 1 : conséquents sur une expression\n\n```scheme\n(cond\n  ((< 3 2) "This won\'t run")\n  ((= 3 3) "This will run")\n  (else "Fallback"))\n```\n\nRésultat : **"This will run"**\n\n#### Exemple 2 : actions multiples avec `begin`\n\n```scheme\n(cond\n  ((< 5 3)\n    (begin\n      (lumi-message "This won\'t run")\n      (* 2 3)))\n  ((> 5 3)\n    (begin\n      (lumi-message "Condition met")\n      (* 5 5)))\n  (else\n    (begin\n      (lumi-message "Fallback")\n      0)))\n```\n\nRésultat : **Affiche « Condition met » et renvoie 25.**\n\n#### Exemple 3 : bloc `let` dans un conséquent\n\n```scheme\n(cond\n  ;; 경우 1: 0이 -1보다 작을 때\n  ((< 0 -1)\n    (let ((x 10))\n      (* x x)))\n\n  ;; 경우 2: 0이 -1보다 클 때\n  ((> 0 -1)\n    (let ((y 20))\n      (lumi-message "Positive condition met")\n      (+ y y)))\n\n  ;; 기본: 위 조건이 모두 거짓일 때\n  (else\n    (let ((z 0))\n      z)))\n```\n\nRésultat : **Affiche « Positive condition met » et renvoie 40.**\n\n#### Exemple 4 : repli avec `else`\n\n```scheme\n(cond\n  ((< 5 3) "This won\'t run")\n  ((= 5 3) "This won\'t run either")\n  (else "Fallback value"))\n```\n\nRésultat : **"Fallback value"**\n\n### Résumé\n\n- `cond` pour plusieurs conditions clairement.\n- Conséquents simples ou groupés avec `begin`.\n- `let` pour variables locales ; `else` recommandé en repli.'}, 'nl': {'title': 'cond', 'body': 'En Scheme, le conditionnel `cond` sélectionne l\'un de plusieurs blocs à exécuter selon plusieurs tests — comme un `if` à branches multiples, évalué dans l\'ordre jusqu\'au premier succès.\n\n### Syntaxe\n\n```scheme\n(cond\n  (test-1 consequent-1)\n  (test-2 consequent-2)\n  ...\n  (else fallback-consequent))\n```\n\n- Tests dans l\'ordre d\'écriture.\n- Premier `#t` : **consequent** exécuté, `cond` s\'arrête.\n- `else` optionnel en repli.\n\n### Comment ça marche\n\n1. **Tester chaque condition** dans l\'ordre.\n2. **Exécuter le consequent** correspondant ; sinon `else` si présent.\n\n### Exemples\n\n#### Exemple 1 : conséquents sur une expression\n\n```scheme\n(cond\n  ((< 3 2) "This won\'t run")\n  ((= 3 3) "This will run")\n  (else "Fallback"))\n```\n\nRésultat : **"This will run"**\n\n#### Exemple 2 : actions multiples avec `begin`\n\n```scheme\n(cond\n  ((< 5 3)\n    (begin\n      (lumi-message "This won\'t run")\n      (* 2 3)))\n  ((> 5 3)\n    (begin\n      (lumi-message "Condition met")\n      (* 5 5)))\n  (else\n    (begin\n      (lumi-message "Fallback")\n      0)))\n```\n\nRésultat : **Affiche « Condition met » et renvoie 25.**\n\n#### Exemple 3 : bloc `let` dans un conséquent\n\n```scheme\n(cond\n  ;; Geval 1: als 0 kleiner is dan -1\n  ((< 0 -1)\n    (let ((x 10))\n      (* x x)))\n\n  ;; Geval 2: als 0 groter is dan -1\n  ((> 0 -1)\n    (let ((y 20))\n      (lumi-message "Positive condition met")\n      (+ y y)))\n\n  ;; Standaard: als geen van bovenstaande voorwaarden geldt\n  (else\n    (let ((z 0))\n      z)))\n```\n\nRésultat : **Affiche « Positive condition met » et renvoie 40.**\n\n#### Exemple 4 : repli avec `else`\n\n```scheme\n(cond\n  ((< 5 3) "This won\'t run")\n  ((= 5 3) "This won\'t run either")\n  (else "Fallback value"))\n```\n\nRésultat : **"Fallback value"**\n\n### Résumé\n\n- `cond` pour plusieurs conditions clairement.\n- Conséquents simples ou groupés avec `begin`.\n- `let` pour variables locales ; `else` recommandé en repli.'}, 'pl': {'title': 'cond', 'body': 'En Scheme, le conditionnel `cond` sélectionne l\'un de plusieurs blocs à exécuter selon plusieurs tests — comme un `if` à branches multiples, évalué dans l\'ordre jusqu\'au premier succès.\n\n### Syntaxe\n\n```scheme\n(cond\n  (test-1 consequent-1)\n  (test-2 consequent-2)\n  ...\n  (else fallback-consequent))\n```\n\n- Tests dans l\'ordre d\'écriture.\n- Premier `#t` : **consequent** exécuté, `cond` s\'arrête.\n- `else` optionnel en repli.\n\n### Comment ça marche\n\n1. **Tester chaque condition** dans l\'ordre.\n2. **Exécuter le consequent** correspondant ; sinon `else` si présent.\n\n### Exemples\n\n#### Exemple 1 : conséquents sur une expression\n\n```scheme\n(cond\n  ((< 3 2) "This won\'t run")\n  ((= 3 3) "This will run")\n  (else "Fallback"))\n```\n\nRésultat : **"This will run"**\n\n#### Exemple 2 : actions multiples avec `begin`\n\n```scheme\n(cond\n  ((< 5 3)\n    (begin\n      (lumi-message "This won\'t run")\n      (* 2 3)))\n  ((> 5 3)\n    (begin\n      (lumi-message "Condition met")\n      (* 5 5)))\n  (else\n    (begin\n      (lumi-message "Fallback")\n      0)))\n```\n\nRésultat : **Affiche « Condition met » et renvoie 25.**\n\n#### Exemple 3 : bloc `let` dans un conséquent\n\n```scheme\n(cond\n  ;; Przypadek 1: gdy 0 jest mniejsze niż -1\n  ((< 0 -1)\n    (let ((x 10))\n      (* x x)))\n\n  ;; Przypadek 2: gdy 0 jest większe niż -1\n  ((> 0 -1)\n    (let ((y 20))\n      (lumi-message "Positive condition met")\n      (+ y y)))\n\n  ;; Domyślnie: gdy żaden z powyższych warunków nie jest spełniony\n  (else\n    (let ((z 0))\n      z)))\n```\n\nRésultat : **Affiche « Positive condition met » et renvoie 40.**\n\n#### Exemple 4 : repli avec `else`\n\n```scheme\n(cond\n  ((< 5 3) "This won\'t run")\n  ((= 5 3) "This won\'t run either")\n  (else "Fallback value"))\n```\n\nRésultat : **"Fallback value"**\n\n### Résumé\n\n- `cond` pour plusieurs conditions clairement.\n- Conséquents simples ou groupés avec `begin`.\n- `let` pour variables locales ; `else` recommandé en repli.'}, 'pt-br': {'title': 'cond', 'body': 'En Scheme, le conditionnel `cond` sélectionne l\'un de plusieurs blocs à exécuter selon plusieurs tests — comme un `if` à branches multiples, évalué dans l\'ordre jusqu\'au premier succès.\n\n### Syntaxe\n\n```scheme\n(cond\n  (test-1 consequent-1)\n  (test-2 consequent-2)\n  ...\n  (else fallback-consequent))\n```\n\n- Tests dans l\'ordre d\'écriture.\n- Premier `#t` : **consequent** exécuté, `cond` s\'arrête.\n- `else` optionnel en repli.\n\n### Comment ça marche\n\n1. **Tester chaque condition** dans l\'ordre.\n2. **Exécuter le consequent** correspondant ; sinon `else` si présent.\n\n### Exemples\n\n#### Exemple 1 : conséquents sur une expression\n\n```scheme\n(cond\n  ((< 3 2) "This won\'t run")\n  ((= 3 3) "This will run")\n  (else "Fallback"))\n```\n\nRésultat : **"This will run"**\n\n#### Exemple 2 : actions multiples avec `begin`\n\n```scheme\n(cond\n  ((< 5 3)\n    (begin\n      (lumi-message "This won\'t run")\n      (* 2 3)))\n  ((> 5 3)\n    (begin\n      (lumi-message "Condition met")\n      (* 5 5)))\n  (else\n    (begin\n      (lumi-message "Fallback")\n      0)))\n```\n\nRésultat : **Affiche « Condition met » et renvoie 25.**\n\n#### Exemple 3 : bloc `let` dans un conséquent\n\n```scheme\n(cond\n  ;; Caso 1: se 0 for menor que -1\n  ((< 0 -1)\n    (let ((x 10))\n      (* x x)))\n\n  ;; Caso 2: se 0 for maior que -1\n  ((> 0 -1)\n    (let ((y 20))\n      (lumi-message "Positive condition met")\n      (+ y y)))\n\n  ;; Padrão: se nenhuma condição acima for atendida\n  (else\n    (let ((z 0))\n      z)))\n```\n\nRésultat : **Affiche « Positive condition met » et renvoie 40.**\n\n#### Exemple 4 : repli avec `else`\n\n```scheme\n(cond\n  ((< 5 3) "This won\'t run")\n  ((= 5 3) "This won\'t run either")\n  (else "Fallback value"))\n```\n\nRésultat : **"Fallback value"**\n\n### Résumé\n\n- `cond` pour plusieurs conditions clairement.\n- Conséquents simples ou groupés avec `begin`.\n- `let` pour variables locales ; `else` recommandé en repli.'}, 'ru': {'title': 'cond', 'body': 'En Scheme, le conditionnel `cond` sélectionne l\'un de plusieurs blocs à exécuter selon plusieurs tests — comme un `if` à branches multiples, évalué dans l\'ordre jusqu\'au premier succès.\n\n### Syntaxe\n\n```scheme\n(cond\n  (test-1 consequent-1)\n  (test-2 consequent-2)\n  ...\n  (else fallback-consequent))\n```\n\n- Tests dans l\'ordre d\'écriture.\n- Premier `#t` : **consequent** exécuté, `cond` s\'arrête.\n- `else` optionnel en repli.\n\n### Comment ça marche\n\n1. **Tester chaque condition** dans l\'ordre.\n2. **Exécuter le consequent** correspondant ; sinon `else` si présent.\n\n### Exemples\n\n#### Exemple 1 : conséquents sur une expression\n\n```scheme\n(cond\n  ((< 3 2) "This won\'t run")\n  ((= 3 3) "This will run")\n  (else "Fallback"))\n```\n\nRésultat : **"This will run"**\n\n#### Exemple 2 : actions multiples avec `begin`\n\n```scheme\n(cond\n  ((< 5 3)\n    (begin\n      (lumi-message "This won\'t run")\n      (* 2 3)))\n  ((> 5 3)\n    (begin\n      (lumi-message "Condition met")\n      (* 5 5)))\n  (else\n    (begin\n      (lumi-message "Fallback")\n      0)))\n```\n\nRésultat : **Affiche « Condition met » et renvoie 25.**\n\n#### Exemple 3 : bloc `let` dans un conséquent\n\n```scheme\n(cond\n  ;; Случай 1: если 0 меньше -1\n  ((< 0 -1)\n    (let ((x 10))\n      (* x x)))\n\n  ;; Случай 2: если 0 больше -1\n  ((> 0 -1)\n    (let ((y 20))\n      (lumi-message "Positive condition met")\n      (+ y y)))\n\n  ;; По умолчанию: если ни одно из условий выше не выполнено\n  (else\n    (let ((z 0))\n      z)))\n```\n\nRésultat : **Affiche « Positive condition met » et renvoie 40.**\n\n#### Exemple 4 : repli avec `else`\n\n```scheme\n(cond\n  ((< 5 3) "This won\'t run")\n  ((= 5 3) "This won\'t run either")\n  (else "Fallback value"))\n```\n\nRésultat : **"Fallback value"**\n\n### Résumé\n\n- `cond` pour plusieurs conditions clairement.\n- Conséquents simples ou groupés avec `begin`.\n- `let` pour variables locales ; `else` recommandé en repli.'}, 'sv': {'title': 'cond', 'body': 'En Scheme, le conditionnel `cond` sélectionne l\'un de plusieurs blocs à exécuter selon plusieurs tests — comme un `if` à branches multiples, évalué dans l\'ordre jusqu\'au premier succès.\n\n### Syntaxe\n\n```scheme\n(cond\n  (test-1 consequent-1)\n  (test-2 consequent-2)\n  ...\n  (else fallback-consequent))\n```\n\n- Tests dans l\'ordre d\'écriture.\n- Premier `#t` : **consequent** exécuté, `cond` s\'arrête.\n- `else` optionnel en repli.\n\n### Comment ça marche\n\n1. **Tester chaque condition** dans l\'ordre.\n2. **Exécuter le consequent** correspondant ; sinon `else` si présent.\n\n### Exemples\n\n#### Exemple 1 : conséquents sur une expression\n\n```scheme\n(cond\n  ((< 3 2) "This won\'t run")\n  ((= 3 3) "This will run")\n  (else "Fallback"))\n```\n\nRésultat : **"This will run"**\n\n#### Exemple 2 : actions multiples avec `begin`\n\n```scheme\n(cond\n  ((< 5 3)\n    (begin\n      (lumi-message "This won\'t run")\n      (* 2 3)))\n  ((> 5 3)\n    (begin\n      (lumi-message "Condition met")\n      (* 5 5)))\n  (else\n    (begin\n      (lumi-message "Fallback")\n      0)))\n```\n\nRésultat : **Affiche « Condition met » et renvoie 25.**\n\n#### Exemple 3 : bloc `let` dans un conséquent\n\n```scheme\n(cond\n  ;; Fall 1: om 0 är mindre än -1\n  ((< 0 -1)\n    (let ((x 10))\n      (* x x)))\n\n  ;; Fall 2: om 0 är större än -1\n  ((> 0 -1)\n    (let ((y 20))\n      (lumi-message "Positive condition met")\n      (+ y y)))\n\n  ;; Standard: om inget av villkoren ovan uppfylls\n  (else\n    (let ((z 0))\n      z)))\n```\n\nRésultat : **Affiche « Positive condition met » et renvoie 40.**\n\n#### Exemple 4 : repli avec `else`\n\n```scheme\n(cond\n  ((< 5 3) "This won\'t run")\n  ((= 5 3) "This won\'t run either")\n  (else "Fallback value"))\n```\n\nRésultat : **"Fallback value"**\n\n### Résumé\n\n- `cond` pour plusieurs conditions clairement.\n- Conséquents simples ou groupés avec `begin`.\n- `let` pour variables locales ; `else` recommandé en repli.'}, 'th': {'title': 'cond', 'body': 'En Scheme, le conditionnel `cond` sélectionne l\'un de plusieurs blocs à exécuter selon plusieurs tests — comme un `if` à branches multiples, évalué dans l\'ordre jusqu\'au premier succès.\n\n### Syntaxe\n\n```scheme\n(cond\n  (test-1 consequent-1)\n  (test-2 consequent-2)\n  ...\n  (else fallback-consequent))\n```\n\n- Tests dans l\'ordre d\'écriture.\n- Premier `#t` : **consequent** exécuté, `cond` s\'arrête.\n- `else` optionnel en repli.\n\n### Comment ça marche\n\n1. **Tester chaque condition** dans l\'ordre.\n2. **Exécuter le consequent** correspondant ; sinon `else` si présent.\n\n### Exemples\n\n#### Exemple 1 : conséquents sur une expression\n\n```scheme\n(cond\n  ((< 3 2) "This won\'t run")\n  ((= 3 3) "This will run")\n  (else "Fallback"))\n```\n\nRésultat : **"This will run"**\n\n#### Exemple 2 : actions multiples avec `begin`\n\n```scheme\n(cond\n  ((< 5 3)\n    (begin\n      (lumi-message "This won\'t run")\n      (* 2 3)))\n  ((> 5 3)\n    (begin\n      (lumi-message "Condition met")\n      (* 5 5)))\n  (else\n    (begin\n      (lumi-message "Fallback")\n      0)))\n```\n\nRésultat : **Affiche « Condition met » et renvoie 25.**\n\n#### Exemple 3 : bloc `let` dans un conséquent\n\n```scheme\n(cond\n  ;; กรณี 1: ถ้า 0 น้อยกว่า -1\n  ((< 0 -1)\n    (let ((x 10))\n      (* x x)))\n\n  ;; กรณี 2: ถ้า 0 มากกว่า -1\n  ((> 0 -1)\n    (let ((y 20))\n      (lumi-message "Positive condition met")\n      (+ y y)))\n\n  ;; ค่าเริ่มต้น: ถ้าไม่มีเงื่อนไขข้างต้นเป็นจริง\n  (else\n    (let ((z 0))\n      z)))\n```\n\nRésultat : **Affiche « Positive condition met » et renvoie 40.**\n\n#### Exemple 4 : repli avec `else`\n\n```scheme\n(cond\n  ((< 5 3) "This won\'t run")\n  ((= 5 3) "This won\'t run either")\n  (else "Fallback value"))\n```\n\nRésultat : **"Fallback value"**\n\n### Résumé\n\n- `cond` pour plusieurs conditions clairement.\n- Conséquents simples ou groupés avec `begin`.\n- `let` pour variables locales ; `else` recommandé en repli.'}, 'zh-cn': {'title': 'cond', 'body': 'En Scheme, le conditionnel `cond` sélectionne l\'un de plusieurs blocs à exécuter selon plusieurs tests — comme un `if` à branches multiples, évalué dans l\'ordre jusqu\'au premier succès.\n\n### Syntaxe\n\n```scheme\n(cond\n  (test-1 consequent-1)\n  (test-2 consequent-2)\n  ...\n  (else fallback-consequent))\n```\n\n- Tests dans l\'ordre d\'écriture.\n- Premier `#t` : **consequent** exécuté, `cond` s\'arrête.\n- `else` optionnel en repli.\n\n### Comment ça marche\n\n1. **Tester chaque condition** dans l\'ordre.\n2. **Exécuter le consequent** correspondant ; sinon `else` si présent.\n\n### Exemples\n\n#### Exemple 1 : conséquents sur une expression\n\n```scheme\n(cond\n  ((< 3 2) "This won\'t run")\n  ((= 3 3) "This will run")\n  (else "Fallback"))\n```\n\nRésultat : **"This will run"**\n\n#### Exemple 2 : actions multiples avec `begin`\n\n```scheme\n(cond\n  ((< 5 3)\n    (begin\n      (lumi-message "This won\'t run")\n      (* 2 3)))\n  ((> 5 3)\n    (begin\n      (lumi-message "Condition met")\n      (* 5 5)))\n  (else\n    (begin\n      (lumi-message "Fallback")\n      0)))\n```\n\nRésultat : **Affiche « Condition met » et renvoie 25.**\n\n#### Exemple 3 : bloc `let` dans un conséquent\n\n```scheme\n(cond\n  ;; 情况 1：若 0 小于 -1\n  ((< 0 -1)\n    (let ((x 10))\n      (* x x)))\n\n  ;; 情况 2：若 0 大于 -1\n  ((> 0 -1)\n    (let ((y 20))\n      (lumi-message "Positive condition met")\n      (+ y y)))\n\n  ;; 默认：若以上条件均不满足\n  (else\n    (let ((z 0))\n      z)))\n```\n\nRésultat : **Affiche « Positive condition met » et renvoie 40.**\n\n#### Exemple 4 : repli avec `else`\n\n```scheme\n(cond\n  ((< 5 3) "This won\'t run")\n  ((= 5 3) "This won\'t run either")\n  (else "Fallback value"))\n```\n\nRésultat : **"Fallback value"**\n\n### Résumé\n\n- `cond` pour plusieurs conditions clairement.\n- Conséquents simples ou groupés avec `begin`.\n- `let` pour variables locales ; `else` recommandé en repli.'}, 'zh-tw': {'title': 'cond', 'body': 'En Scheme, le conditionnel `cond` sélectionne l\'un de plusieurs blocs à exécuter selon plusieurs tests — comme un `if` à branches multiples, évalué dans l\'ordre jusqu\'au premier succès.\n\n### Syntaxe\n\n```scheme\n(cond\n  (test-1 consequent-1)\n  (test-2 consequent-2)\n  ...\n  (else fallback-consequent))\n```\n\n- Tests dans l\'ordre d\'écriture.\n- Premier `#t` : **consequent** exécuté, `cond` s\'arrête.\n- `else` optionnel en repli.\n\n### Comment ça marche\n\n1. **Tester chaque condition** dans l\'ordre.\n2. **Exécuter le consequent** correspondant ; sinon `else` si présent.\n\n### Exemples\n\n#### Exemple 1 : conséquents sur une expression\n\n```scheme\n(cond\n  ((< 3 2) "This won\'t run")\n  ((= 3 3) "This will run")\n  (else "Fallback"))\n```\n\nRésultat : **"This will run"**\n\n#### Exemple 2 : actions multiples avec `begin`\n\n```scheme\n(cond\n  ((< 5 3)\n    (begin\n      (lumi-message "This won\'t run")\n      (* 2 3)))\n  ((> 5 3)\n    (begin\n      (lumi-message "Condition met")\n      (* 5 5)))\n  (else\n    (begin\n      (lumi-message "Fallback")\n      0)))\n```\n\nRésultat : **Affiche « Condition met » et renvoie 25.**\n\n#### Exemple 3 : bloc `let` dans un conséquent\n\n```scheme\n(cond\n  ;; 情況 1：若 0 小於 -1\n  ((< 0 -1)\n    (let ((x 10))\n      (* x x)))\n\n  ;; 情況 2：若 0 大於 -1\n  ((> 0 -1)\n    (let ((y 20))\n      (lumi-message "Positive condition met")\n      (+ y y)))\n\n  ;; 預設：若以上條件均不滿足\n  (else\n    (let ((z 0))\n      z)))\n```\n\nRésultat : **Affiche « Positive condition met » et renvoie 40.**\n\n#### Exemple 4 : repli avec `else`\n\n```scheme\n(cond\n  ((< 5 3) "This won\'t run")\n  ((= 5 3) "This won\'t run either")\n  (else "Fallback value"))\n```\n\nRésultat : **"Fallback value"**\n\n### Résumé\n\n- `cond` pour plusieurs conditions clairement.\n- Conséquents simples ou groupés avec `begin`.\n- `let` pour variables locales ; `else` recommandé en repli.'}}
    meta = bodies[lang]
    return meta["title"], meta["body"]

def _page_content_hub_scripting_fundamentals_Conditionals_conditionals_if_md(lang: str) -> tuple[str, str]:
    bodies = {'de': {'title': 'if', 'body': 'In seiner einfachsten Form wertet `if` in Scheme einen Test aus und führt je nach Ergebnis einen von zwei Codeblöcken aus:\n\n```scheme\n(if test-is-true\n  do-this)\n```\n\n- Bei `#t` wird der **consequent** ausgeführt (Wert zurückgeben oder Nebeneffekte).\n\n### Beispiel\n\n```scheme\n(if (< 0 1)\n  (lumi-message "True!"))\n```\n\n- Test: `(< 0 1)` ist wahr.\n- `(lumi-message "True!")` wird ausgeführt.\n\n### Else-Zweig: `if-else`\n\n```scheme\n(if test\n  do-this\n  else-do-this)\n```\n\n```scheme\n(if test\n  consequent\n  alternative)\n```\n\n### So funktioniert es\n\n1. **Test** zuerst auswerten.\n2. Bei `#t` **consequent**, bei `#f` **alternative**.\n\nBeide Blöcke können jeden gültigen Scheme-Ausdruck enthalten.\n\n#### Beispiel 1: Wert zurückgeben\n\n```scheme\n(if (< 0 1)\n  1\n  0)\n```\n\nErgebnis: **1**\n\n#### Beispiel 2: `begin`-Block\n\n```scheme\n(if (= 0 1)\n  (begin\n    (lumi-message "This won\'t run")\n    1)\n  (begin\n    (lumi-message "False condition met, calculating...")\n    (* 3 4)))\n```\n\nErgebnis: **Gibt "False condition met, calculating..." aus und liefert 12.**\n\n#### Beispiel 3: `let`-Ausdruck\n\n```scheme\n(if (= 1 1)\n  (let (x -1)\n    (lumi-message "True condition met, calculating...")\n    (* x 10))\n  (let (y 4)\n    (lumi-message "This won\'t run")\n    (* 3 y)))\n```\n\nErgebnis: **Gibt "True condition met, calculating..." aus und liefert -10.**\n\n### Zusammenfassung\n\n- `if` wertet Tests aus und führt passende Blöcke aus.\n- Einfache Ausdrücke oder `begin`/`let`-Gruppen möglich.\n- Ohne explizites `else` nur **consequent** bei wahrem Test.'}, 'es': {'title': 'if', 'body': 'En su forma más simple, `if` en Scheme evalúa una prueba y, según el resultado, ejecuta uno de dos bloques de código:\n\n```scheme\n(if test-is-true\n  do-this)\n```\n\n- Si `#t`, se ejecuta el **consequent** (valor o efectos secundarios).\n\n### Ejemplo\n\n```scheme\n(if (< 0 1)\n  (lumi-message "True!"))\n```\n\n- Prueba: `(< 0 1)` es verdadera.\n- Se ejecuta `(lumi-message "True!")`.\n\n### Rama else: `if-else`\n\n```scheme\n(if test\n  do-this\n  else-do-this)\n```\n\n```scheme\n(if test\n  consequent\n  alternative)\n```\n\n### Cómo funciona\n\n1. **Evaluar** la prueba primero.\n2. Si `#t` **consequent**, si `#f` **alternative**.\n\nAmbos bloques pueden ser cualquier expresión Scheme válida.\n\n#### Ejemplo 1: devolver un valor\n\n```scheme\n(if (< 0 1)\n  1\n  0)\n```\n\nResultado: **1**\n\n#### Ejemplo 2: bloque `begin`\n\n```scheme\n(if (= 0 1)\n  (begin\n    (lumi-message "This won\'t run")\n    1)\n  (begin\n    (lumi-message "False condition met, calculating...")\n    (* 3 4)))\n```\n\nResultado: **Imprime "False condition met, calculating..." y devuelve 12.**\n\n#### Ejemplo 3: expresión `let`\n\n```scheme\n(if (= 1 1)\n  (let (x -1)\n    (lumi-message "True condition met, calculating...")\n    (* x 10))\n  (let (y 4)\n    (lumi-message "This won\'t run")\n    (* 3 y)))\n```\n\nResultado: **Imprime "True condition met, calculating..." y devuelve -10.**\n\n### Resumen\n\n- `if` evalúa pruebas y ejecuta el bloque adecuado.\n- Expresiones simples o grupos `begin`/`let`.\n- Sin `else` explícito, solo **consequent** si es verdadero.'}, 'fr': {'title': 'if', 'body': 'Dans sa forme la plus simple, `if` en Scheme évalue un test et, selon le résultat, exécute l\'un de deux blocs de code :\n\n```scheme\n(if test-is-true\n  do-this)\n```\n\n- Si `#t`, le **consequent** s\'exécute (valeur ou effets de bord).\n\n### Exemple\n\n```scheme\n(if (< 0 1)\n  (lumi-message "True!"))\n```\n\n- Test : `(< 0 1)` est vrai.\n- `(lumi-message "True!")` s\'exécute.\n\n### Branche else : `if-else`\n\n```scheme\n(if test\n  do-this\n  else-do-this)\n```\n\n```scheme\n(if test\n  consequent\n  alternative)\n```\n\n### Comment ça marche\n\n1. **Tester** d\'abord.\n2. Si `#t` **consequent**, si `#f` **alternative**.\n\nLes deux blocs peuvent contenir toute expression Scheme valide.\n\n#### Exemple 1 : renvoyer une valeur\n\n```scheme\n(if (< 0 1)\n  1\n  0)\n```\n\nRésultat : **1**\n\n#### Exemple 2 : bloc `begin`\n\n```scheme\n(if (= 0 1)\n  (begin\n    (lumi-message "This won\'t run")\n    1)\n  (begin\n    (lumi-message "False condition met, calculating...")\n    (* 3 4)))\n```\n\nRésultat : **Affiche « False condition met, calculating... » et renvoie 12.**\n\n#### Exemple 3 : expression `let`\n\n```scheme\n(if (= 1 1)\n  (let (x -1)\n    (lumi-message "True condition met, calculating...")\n    (* x 10))\n  (let (y 4)\n    (lumi-message "This won\'t run")\n    (* 3 y)))\n```\n\nRésultat : **Affiche « True condition met, calculating... » et renvoie -10.**\n\n### Résumé\n\n- `if` évalue un test et exécute le bloc adapté.\n- Expressions simples ou groupes `begin`/`let`.\n- Sans `else` explicite, seul le **consequent** si vrai.'}, 'it': {'title': 'if', 'body': 'Dans sa forme la plus simple, `if` en Scheme évalue un test et, selon le résultat, exécute l\'un de deux blocs de code :\n\n```scheme\n(if test-is-true\n  do-this)\n```\n\n- Si `#t`, le **consequent** s\'exécute (valeur ou effets de bord).\n\n### Exemple\n\n```scheme\n(if (< 0 1)\n  (lumi-message "True!"))\n```\n\n- Test : `(< 0 1)` est vrai.\n- `(lumi-message "True!")` s\'exécute.\n\n### Branche else : `if-else`\n\n```scheme\n(if test\n  do-this\n  else-do-this)\n```\n\n```scheme\n(if test\n  consequent\n  alternative)\n```\n\n### Comment ça marche\n\n1. **Tester** d\'abord.\n2. Si `#t` **consequent**, si `#f` **alternative**.\n\nLes deux blocs peuvent contenir toute expression Scheme valide.\n\n#### Exemple 1 : renvoyer une valeur\n\n```scheme\n(if (< 0 1)\n  1\n  0)\n```\n\nRésultat : **1**\n\n#### Exemple 2 : bloc `begin`\n\n```scheme\n(if (= 0 1)\n  (begin\n    (lumi-message "This won\'t run")\n    1)\n  (begin\n    (lumi-message "False condition met, calculating...")\n    (* 3 4)))\n```\n\nRésultat : **Affiche « False condition met, calculating... » et renvoie 12.**\n\n#### Exemple 3 : expression `let`\n\n```scheme\n(if (= 1 1)\n  (let (x -1)\n    (lumi-message "True condition met, calculating...")\n    (* x 10))\n  (let (y 4)\n    (lumi-message "This won\'t run")\n    (* 3 y)))\n```\n\nRésultat : **Affiche « True condition met, calculating... » et renvoie -10.**\n\n### Résumé\n\n- `if` évalue un test et exécute le bloc adapté.\n- Expressions simples ou groupes `begin`/`let`.\n- Sans `else` explicite, seul le **consequent** si vrai.'}, 'ja': {'title': 'if', 'body': 'Dans sa forme la plus simple, `if` en Scheme évalue un test et, selon le résultat, exécute l\'un de deux blocs de code :\n\n```scheme\n(if test-is-true\n  do-this)\n```\n\n- Si `#t`, le **consequent** s\'exécute (valeur ou effets de bord).\n\n### Exemple\n\n```scheme\n(if (< 0 1)\n  (lumi-message "True!"))\n```\n\n- Test : `(< 0 1)` est vrai.\n- `(lumi-message "True!")` s\'exécute.\n\n### Branche else : `if-else`\n\n```scheme\n(if test\n  do-this\n  else-do-this)\n```\n\n```scheme\n(if test\n  consequent\n  alternative)\n```\n\n### Comment ça marche\n\n1. **Tester** d\'abord.\n2. Si `#t` **consequent**, si `#f` **alternative**.\n\nLes deux blocs peuvent contenir toute expression Scheme valide.\n\n#### Exemple 1 : renvoyer une valeur\n\n```scheme\n(if (< 0 1)\n  1\n  0)\n```\n\nRésultat : **1**\n\n#### Exemple 2 : bloc `begin`\n\n```scheme\n(if (= 0 1)\n  (begin\n    (lumi-message "This won\'t run")\n    1)\n  (begin\n    (lumi-message "False condition met, calculating...")\n    (* 3 4)))\n```\n\nRésultat : **Affiche « False condition met, calculating... » et renvoie 12.**\n\n#### Exemple 3 : expression `let`\n\n```scheme\n(if (= 1 1)\n  (let (x -1)\n    (lumi-message "True condition met, calculating...")\n    (* x 10))\n  (let (y 4)\n    (lumi-message "This won\'t run")\n    (* 3 y)))\n```\n\nRésultat : **Affiche « True condition met, calculating... » et renvoie -10.**\n\n### Résumé\n\n- `if` évalue un test et exécute le bloc adapté.\n- Expressions simples ou groupes `begin`/`let`.\n- Sans `else` explicite, seul le **consequent** si vrai.'}, 'ko': {'title': 'if', 'body': 'Dans sa forme la plus simple, `if` en Scheme évalue un test et, selon le résultat, exécute l\'un de deux blocs de code :\n\n```scheme\n(if test-is-true\n  do-this)\n```\n\n- Si `#t`, le **consequent** s\'exécute (valeur ou effets de bord).\n\n### Exemple\n\n```scheme\n(if (< 0 1)\n  (lumi-message "True!"))\n```\n\n- Test : `(< 0 1)` est vrai.\n- `(lumi-message "True!")` s\'exécute.\n\n### Branche else : `if-else`\n\n```scheme\n(if test\n  do-this\n  else-do-this)\n```\n\n```scheme\n(if test\n  consequent\n  alternative)\n```\n\n### Comment ça marche\n\n1. **Tester** d\'abord.\n2. Si `#t` **consequent**, si `#f` **alternative**.\n\nLes deux blocs peuvent contenir toute expression Scheme valide.\n\n#### Exemple 1 : renvoyer une valeur\n\n```scheme\n(if (< 0 1)\n  1\n  0)\n```\n\nRésultat : **1**\n\n#### Exemple 2 : bloc `begin`\n\n```scheme\n(if (= 0 1)\n  (begin\n    (lumi-message "This won\'t run")\n    1)\n  (begin\n    (lumi-message "False condition met, calculating...")\n    (* 3 4)))\n```\n\nRésultat : **Affiche « False condition met, calculating... » et renvoie 12.**\n\n#### Exemple 3 : expression `let`\n\n```scheme\n(if (= 1 1)\n  (let (x -1)\n    (lumi-message "True condition met, calculating...")\n    (* x 10))\n  (let (y 4)\n    (lumi-message "This won\'t run")\n    (* 3 y)))\n```\n\nRésultat : **Affiche « True condition met, calculating... » et renvoie -10.**\n\n### Résumé\n\n- `if` évalue un test et exécute le bloc adapté.\n- Expressions simples ou groupes `begin`/`let`.\n- Sans `else` explicite, seul le **consequent** si vrai.'}, 'nl': {'title': 'if', 'body': 'Dans sa forme la plus simple, `if` en Scheme évalue un test et, selon le résultat, exécute l\'un de deux blocs de code :\n\n```scheme\n(if test-is-true\n  do-this)\n```\n\n- Si `#t`, le **consequent** s\'exécute (valeur ou effets de bord).\n\n### Exemple\n\n```scheme\n(if (< 0 1)\n  (lumi-message "True!"))\n```\n\n- Test : `(< 0 1)` est vrai.\n- `(lumi-message "True!")` s\'exécute.\n\n### Branche else : `if-else`\n\n```scheme\n(if test\n  do-this\n  else-do-this)\n```\n\n```scheme\n(if test\n  consequent\n  alternative)\n```\n\n### Comment ça marche\n\n1. **Tester** d\'abord.\n2. Si `#t` **consequent**, si `#f` **alternative**.\n\nLes deux blocs peuvent contenir toute expression Scheme valide.\n\n#### Exemple 1 : renvoyer une valeur\n\n```scheme\n(if (< 0 1)\n  1\n  0)\n```\n\nRésultat : **1**\n\n#### Exemple 2 : bloc `begin`\n\n```scheme\n(if (= 0 1)\n  (begin\n    (lumi-message "This won\'t run")\n    1)\n  (begin\n    (lumi-message "False condition met, calculating...")\n    (* 3 4)))\n```\n\nRésultat : **Affiche « False condition met, calculating... » et renvoie 12.**\n\n#### Exemple 3 : expression `let`\n\n```scheme\n(if (= 1 1)\n  (let (x -1)\n    (lumi-message "True condition met, calculating...")\n    (* x 10))\n  (let (y 4)\n    (lumi-message "This won\'t run")\n    (* 3 y)))\n```\n\nRésultat : **Affiche « True condition met, calculating... » et renvoie -10.**\n\n### Résumé\n\n- `if` évalue un test et exécute le bloc adapté.\n- Expressions simples ou groupes `begin`/`let`.\n- Sans `else` explicite, seul le **consequent** si vrai.'}, 'pl': {'title': 'if', 'body': 'Dans sa forme la plus simple, `if` en Scheme évalue un test et, selon le résultat, exécute l\'un de deux blocs de code :\n\n```scheme\n(if test-is-true\n  do-this)\n```\n\n- Si `#t`, le **consequent** s\'exécute (valeur ou effets de bord).\n\n### Exemple\n\n```scheme\n(if (< 0 1)\n  (lumi-message "True!"))\n```\n\n- Test : `(< 0 1)` est vrai.\n- `(lumi-message "True!")` s\'exécute.\n\n### Branche else : `if-else`\n\n```scheme\n(if test\n  do-this\n  else-do-this)\n```\n\n```scheme\n(if test\n  consequent\n  alternative)\n```\n\n### Comment ça marche\n\n1. **Tester** d\'abord.\n2. Si `#t` **consequent**, si `#f` **alternative**.\n\nLes deux blocs peuvent contenir toute expression Scheme valide.\n\n#### Exemple 1 : renvoyer une valeur\n\n```scheme\n(if (< 0 1)\n  1\n  0)\n```\n\nRésultat : **1**\n\n#### Exemple 2 : bloc `begin`\n\n```scheme\n(if (= 0 1)\n  (begin\n    (lumi-message "This won\'t run")\n    1)\n  (begin\n    (lumi-message "False condition met, calculating...")\n    (* 3 4)))\n```\n\nRésultat : **Affiche « False condition met, calculating... » et renvoie 12.**\n\n#### Exemple 3 : expression `let`\n\n```scheme\n(if (= 1 1)\n  (let (x -1)\n    (lumi-message "True condition met, calculating...")\n    (* x 10))\n  (let (y 4)\n    (lumi-message "This won\'t run")\n    (* 3 y)))\n```\n\nRésultat : **Affiche « True condition met, calculating... » et renvoie -10.**\n\n### Résumé\n\n- `if` évalue un test et exécute le bloc adapté.\n- Expressions simples ou groupes `begin`/`let`.\n- Sans `else` explicite, seul le **consequent** si vrai.'}, 'pt-br': {'title': 'if', 'body': 'Dans sa forme la plus simple, `if` en Scheme évalue un test et, selon le résultat, exécute l\'un de deux blocs de code :\n\n```scheme\n(if test-is-true\n  do-this)\n```\n\n- Si `#t`, le **consequent** s\'exécute (valeur ou effets de bord).\n\n### Exemple\n\n```scheme\n(if (< 0 1)\n  (lumi-message "True!"))\n```\n\n- Test : `(< 0 1)` est vrai.\n- `(lumi-message "True!")` s\'exécute.\n\n### Branche else : `if-else`\n\n```scheme\n(if test\n  do-this\n  else-do-this)\n```\n\n```scheme\n(if test\n  consequent\n  alternative)\n```\n\n### Comment ça marche\n\n1. **Tester** d\'abord.\n2. Si `#t` **consequent**, si `#f` **alternative**.\n\nLes deux blocs peuvent contenir toute expression Scheme valide.\n\n#### Exemple 1 : renvoyer une valeur\n\n```scheme\n(if (< 0 1)\n  1\n  0)\n```\n\nRésultat : **1**\n\n#### Exemple 2 : bloc `begin`\n\n```scheme\n(if (= 0 1)\n  (begin\n    (lumi-message "This won\'t run")\n    1)\n  (begin\n    (lumi-message "False condition met, calculating...")\n    (* 3 4)))\n```\n\nRésultat : **Affiche « False condition met, calculating... » et renvoie 12.**\n\n#### Exemple 3 : expression `let`\n\n```scheme\n(if (= 1 1)\n  (let (x -1)\n    (lumi-message "True condition met, calculating...")\n    (* x 10))\n  (let (y 4)\n    (lumi-message "This won\'t run")\n    (* 3 y)))\n```\n\nRésultat : **Affiche « True condition met, calculating... » et renvoie -10.**\n\n### Résumé\n\n- `if` évalue un test et exécute le bloc adapté.\n- Expressions simples ou groupes `begin`/`let`.\n- Sans `else` explicite, seul le **consequent** si vrai.'}, 'ru': {'title': 'if', 'body': 'Dans sa forme la plus simple, `if` en Scheme évalue un test et, selon le résultat, exécute l\'un de deux blocs de code :\n\n```scheme\n(if test-is-true\n  do-this)\n```\n\n- Si `#t`, le **consequent** s\'exécute (valeur ou effets de bord).\n\n### Exemple\n\n```scheme\n(if (< 0 1)\n  (lumi-message "True!"))\n```\n\n- Test : `(< 0 1)` est vrai.\n- `(lumi-message "True!")` s\'exécute.\n\n### Branche else : `if-else`\n\n```scheme\n(if test\n  do-this\n  else-do-this)\n```\n\n```scheme\n(if test\n  consequent\n  alternative)\n```\n\n### Comment ça marche\n\n1. **Tester** d\'abord.\n2. Si `#t` **consequent**, si `#f` **alternative**.\n\nLes deux blocs peuvent contenir toute expression Scheme valide.\n\n#### Exemple 1 : renvoyer une valeur\n\n```scheme\n(if (< 0 1)\n  1\n  0)\n```\n\nRésultat : **1**\n\n#### Exemple 2 : bloc `begin`\n\n```scheme\n(if (= 0 1)\n  (begin\n    (lumi-message "This won\'t run")\n    1)\n  (begin\n    (lumi-message "False condition met, calculating...")\n    (* 3 4)))\n```\n\nRésultat : **Affiche « False condition met, calculating... » et renvoie 12.**\n\n#### Exemple 3 : expression `let`\n\n```scheme\n(if (= 1 1)\n  (let (x -1)\n    (lumi-message "True condition met, calculating...")\n    (* x 10))\n  (let (y 4)\n    (lumi-message "This won\'t run")\n    (* 3 y)))\n```\n\nRésultat : **Affiche « True condition met, calculating... » et renvoie -10.**\n\n### Résumé\n\n- `if` évalue un test et exécute le bloc adapté.\n- Expressions simples ou groupes `begin`/`let`.\n- Sans `else` explicite, seul le **consequent** si vrai.'}, 'sv': {'title': 'if', 'body': 'Dans sa forme la plus simple, `if` en Scheme évalue un test et, selon le résultat, exécute l\'un de deux blocs de code :\n\n```scheme\n(if test-is-true\n  do-this)\n```\n\n- Si `#t`, le **consequent** s\'exécute (valeur ou effets de bord).\n\n### Exemple\n\n```scheme\n(if (< 0 1)\n  (lumi-message "True!"))\n```\n\n- Test : `(< 0 1)` est vrai.\n- `(lumi-message "True!")` s\'exécute.\n\n### Branche else : `if-else`\n\n```scheme\n(if test\n  do-this\n  else-do-this)\n```\n\n```scheme\n(if test\n  consequent\n  alternative)\n```\n\n### Comment ça marche\n\n1. **Tester** d\'abord.\n2. Si `#t` **consequent**, si `#f` **alternative**.\n\nLes deux blocs peuvent contenir toute expression Scheme valide.\n\n#### Exemple 1 : renvoyer une valeur\n\n```scheme\n(if (< 0 1)\n  1\n  0)\n```\n\nRésultat : **1**\n\n#### Exemple 2 : bloc `begin`\n\n```scheme\n(if (= 0 1)\n  (begin\n    (lumi-message "This won\'t run")\n    1)\n  (begin\n    (lumi-message "False condition met, calculating...")\n    (* 3 4)))\n```\n\nRésultat : **Affiche « False condition met, calculating... » et renvoie 12.**\n\n#### Exemple 3 : expression `let`\n\n```scheme\n(if (= 1 1)\n  (let (x -1)\n    (lumi-message "True condition met, calculating...")\n    (* x 10))\n  (let (y 4)\n    (lumi-message "This won\'t run")\n    (* 3 y)))\n```\n\nRésultat : **Affiche « True condition met, calculating... » et renvoie -10.**\n\n### Résumé\n\n- `if` évalue un test et exécute le bloc adapté.\n- Expressions simples ou groupes `begin`/`let`.\n- Sans `else` explicite, seul le **consequent** si vrai.'}, 'th': {'title': 'if', 'body': 'Dans sa forme la plus simple, `if` en Scheme évalue un test et, selon le résultat, exécute l\'un de deux blocs de code :\n\n```scheme\n(if test-is-true\n  do-this)\n```\n\n- Si `#t`, le **consequent** s\'exécute (valeur ou effets de bord).\n\n### Exemple\n\n```scheme\n(if (< 0 1)\n  (lumi-message "True!"))\n```\n\n- Test : `(< 0 1)` est vrai.\n- `(lumi-message "True!")` s\'exécute.\n\n### Branche else : `if-else`\n\n```scheme\n(if test\n  do-this\n  else-do-this)\n```\n\n```scheme\n(if test\n  consequent\n  alternative)\n```\n\n### Comment ça marche\n\n1. **Tester** d\'abord.\n2. Si `#t` **consequent**, si `#f` **alternative**.\n\nLes deux blocs peuvent contenir toute expression Scheme valide.\n\n#### Exemple 1 : renvoyer une valeur\n\n```scheme\n(if (< 0 1)\n  1\n  0)\n```\n\nRésultat : **1**\n\n#### Exemple 2 : bloc `begin`\n\n```scheme\n(if (= 0 1)\n  (begin\n    (lumi-message "This won\'t run")\n    1)\n  (begin\n    (lumi-message "False condition met, calculating...")\n    (* 3 4)))\n```\n\nRésultat : **Affiche « False condition met, calculating... » et renvoie 12.**\n\n#### Exemple 3 : expression `let`\n\n```scheme\n(if (= 1 1)\n  (let (x -1)\n    (lumi-message "True condition met, calculating...")\n    (* x 10))\n  (let (y 4)\n    (lumi-message "This won\'t run")\n    (* 3 y)))\n```\n\nRésultat : **Affiche « True condition met, calculating... » et renvoie -10.**\n\n### Résumé\n\n- `if` évalue un test et exécute le bloc adapté.\n- Expressions simples ou groupes `begin`/`let`.\n- Sans `else` explicite, seul le **consequent** si vrai.'}, 'zh-cn': {'title': 'if', 'body': 'Dans sa forme la plus simple, `if` en Scheme évalue un test et, selon le résultat, exécute l\'un de deux blocs de code :\n\n```scheme\n(if test-is-true\n  do-this)\n```\n\n- Si `#t`, le **consequent** s\'exécute (valeur ou effets de bord).\n\n### Exemple\n\n```scheme\n(if (< 0 1)\n  (lumi-message "True!"))\n```\n\n- Test : `(< 0 1)` est vrai.\n- `(lumi-message "True!")` s\'exécute.\n\n### Branche else : `if-else`\n\n```scheme\n(if test\n  do-this\n  else-do-this)\n```\n\n```scheme\n(if test\n  consequent\n  alternative)\n```\n\n### Comment ça marche\n\n1. **Tester** d\'abord.\n2. Si `#t` **consequent**, si `#f` **alternative**.\n\nLes deux blocs peuvent contenir toute expression Scheme valide.\n\n#### Exemple 1 : renvoyer une valeur\n\n```scheme\n(if (< 0 1)\n  1\n  0)\n```\n\nRésultat : **1**\n\n#### Exemple 2 : bloc `begin`\n\n```scheme\n(if (= 0 1)\n  (begin\n    (lumi-message "This won\'t run")\n    1)\n  (begin\n    (lumi-message "False condition met, calculating...")\n    (* 3 4)))\n```\n\nRésultat : **Affiche « False condition met, calculating... » et renvoie 12.**\n\n#### Exemple 3 : expression `let`\n\n```scheme\n(if (= 1 1)\n  (let (x -1)\n    (lumi-message "True condition met, calculating...")\n    (* x 10))\n  (let (y 4)\n    (lumi-message "This won\'t run")\n    (* 3 y)))\n```\n\nRésultat : **Affiche « True condition met, calculating... » et renvoie -10.**\n\n### Résumé\n\n- `if` évalue un test et exécute le bloc adapté.\n- Expressions simples ou groupes `begin`/`let`.\n- Sans `else` explicite, seul le **consequent** si vrai.'}, 'zh-tw': {'title': 'if', 'body': 'Dans sa forme la plus simple, `if` en Scheme évalue un test et, selon le résultat, exécute l\'un de deux blocs de code :\n\n```scheme\n(if test-is-true\n  do-this)\n```\n\n- Si `#t`, le **consequent** s\'exécute (valeur ou effets de bord).\n\n### Exemple\n\n```scheme\n(if (< 0 1)\n  (lumi-message "True!"))\n```\n\n- Test : `(< 0 1)` est vrai.\n- `(lumi-message "True!")` s\'exécute.\n\n### Branche else : `if-else`\n\n```scheme\n(if test\n  do-this\n  else-do-this)\n```\n\n```scheme\n(if test\n  consequent\n  alternative)\n```\n\n### Comment ça marche\n\n1. **Tester** d\'abord.\n2. Si `#t` **consequent**, si `#f` **alternative**.\n\nLes deux blocs peuvent contenir toute expression Scheme valide.\n\n#### Exemple 1 : renvoyer une valeur\n\n```scheme\n(if (< 0 1)\n  1\n  0)\n```\n\nRésultat : **1**\n\n#### Exemple 2 : bloc `begin`\n\n```scheme\n(if (= 0 1)\n  (begin\n    (lumi-message "This won\'t run")\n    1)\n  (begin\n    (lumi-message "False condition met, calculating...")\n    (* 3 4)))\n```\n\nRésultat : **Affiche « False condition met, calculating... » et renvoie 12.**\n\n#### Exemple 3 : expression `let`\n\n```scheme\n(if (= 1 1)\n  (let (x -1)\n    (lumi-message "True condition met, calculating...")\n    (* x 10))\n  (let (y 4)\n    (lumi-message "This won\'t run")\n    (* 3 y)))\n```\n\nRésultat : **Affiche « True condition met, calculating... » et renvoie -10.**\n\n### Résumé\n\n- `if` évalue un test et exécute le bloc adapté.\n- Expressions simples ou groupes `begin`/`let`.\n- Sans `else` explicite, seul le **consequent** si vrai.'}}
    meta = bodies[lang]
    return meta["title"], meta["body"]

def _page_content_hub_scripting_fundamentals_Conditionals_conditionals_when_md(lang: str) -> tuple[str, str]:
    bodies = {'de': {'title': 'when', 'body': 'In Scheme ist `if` vielseitig, wird aber ohne explizites `else` schnell unübersichtlich — besonders wenn nur der wahre Zweig ausgeführt werden soll. Dann ist `when` klarer und kompakter.\n\nDie Grundform von `when`:\n\n```scheme\n(when test-is-true\n  do-this\n  do-that)\n```\n\n- Bei `#t` werden alle Ausdrücke im Body nacheinander ausgeführt.\n- Bei `#f` passiert nichts; es wird kein Wert zurückgegeben.\n\n### Beispiel\n\n```scheme\n(when (< 0 1)\n  (lumi-message "Condition is true!")\n  (lumi-message "Executing additional actions."))\n```\n\n### `if` und `when` im Vergleich\n\nBeide zusammen im selben Beispiel:\n\n```scheme\n(if (= 0 1)\n  (lumi-message "This will not run")\n  (when (< 0 1)\n    (lumi-message "The \'when\' condition is true!")\n    (lumi-message "Executing multiple actions within \'when\'.")))\n```\n\n#### Erklärung\n\n1. **`if`:** `(= 0 1)` ist falsch, daher der `else`-Zweig.\n2. **`when` im `else`:** `(< 0 1)` ist wahr; beide `lumi-message`-Aufrufe laufen.\n\n#### Warum `when`?\n\n- Kein leerer oder Dummy-`else` nötig.\n- Macht deutlich, dass nur der wahre Zweig zählt.\n\n### Zusammenfassung\n\n- **`if`:** wenn beide Zweige gebraucht werden.\n- **`when`:** nur wahrer Zweig, ggf. mehrere Aktionen.\n- Kombination strukturiert komplexe Bedingungen klar.'}, 'es': {'title': 'when', 'body': 'En Scheme, `if` es versátil, pero sin un `else` explícito puede confundir — sobre todo cuando solo debe ejecutarse la rama verdadera. Entonces `when` es más claro y conciso.\n\nLa forma básica de `when`:\n\n```scheme\n(when test-is-true\n  do-this\n  do-that)\n```\n\n- Si `#t`, todas las expresiones del cuerpo se ejecutan en secuencia.\n- Si `#f`, no ocurre nada; no se devuelve valor.\n\n### Ejemplo\n\n```scheme\n(when (< 0 1)\n  (lumi-message "Condition is true!")\n  (lumi-message "Executing additional actions."))\n```\n\n### Comparar `if` y `when`\n\nAmbos juntos en el mismo ejemplo:\n\n```scheme\n(if (= 0 1)\n  (lumi-message "This will not run")\n  (when (< 0 1)\n    (lumi-message "The \'when\' condition is true!")\n    (lumi-message "Executing multiple actions within \'when\'.")))\n```\n\n#### Explicación\n\n1. **`if`:** `(= 0 1)` es falso, rama `else`.\n2. **`when` en el `else`:** `(< 0 1)` es verdadero; ambos `lumi-message` se ejecutan.\n\n#### ¿Por qué `when`?\n\n- Sin `else` vacío o ficticio.\n- Deja claro que solo importa la rama verdadera.\n\n### Resumen\n\n- **`if`:** cuando importan ambas ramas.\n- **`when`:** solo rama verdadera, varias acciones.\n- Combinarlos estructura condiciones complejas con claridad.'}, 'fr': {'title': 'when', 'body': 'En Scheme, `if` est polyvalent, mais sans `else` explicite il devient vite confus — surtout quand seule la branche vraie doit s\'exécuter. Dans ce cas, `when` est plus clair et concis.\n\nLa forme de base de `when` :\n\n```scheme\n(when test-is-true\n  do-this\n  do-that)\n```\n\n- Si `#t`, toutes les expressions du corps s\'exécutent en séquence.\n- Si `#f`, rien ne se passe ; aucune valeur n\'est renvoyée.\n\n### Exemple\n\n```scheme\n(when (< 0 1)\n  (lumi-message "Condition is true!")\n  (lumi-message "Executing additional actions."))\n```\n\n### Comparer `if` et `when`\n\nLes deux ensemble dans le même exemple :\n\n```scheme\n(if (= 0 1)\n  (lumi-message "This will not run")\n  (when (< 0 1)\n    (lumi-message "The \'when\' condition is true!")\n    (lumi-message "Executing multiple actions within \'when\'.")))\n```\n\n#### Explication\n\n1. **`if` :** `(= 0 1)` est faux, donc branche `else`.\n2. **`when` dans le `else` :** `(< 0 1)` est vrai ; les deux `lumi-message` s\'exécutent.\n\n#### Pourquoi `when` ?\n\n- Pas de `else` vide ou factice.\n- Montre que seule la branche vraie compte.\n\n### Résumé\n\n- **`if` :** quand les deux branches comptent.\n- **`when` :** branche vraie seule, plusieurs actions possibles.\n- Les combiner structure clairement des conditions complexes.'}, 'it': {'title': 'when', 'body': 'En Scheme, `if` est polyvalent, mais sans `else` explicite il devient vite confus — surtout quand seule la branche vraie doit s\'exécuter. Dans ce cas, `when` est plus clair et concis.\n\nLa forme de base de `when` :\n\n```scheme\n(when test-is-true\n  do-this\n  do-that)\n```\n\n- Si `#t`, toutes les expressions du corps s\'exécutent en séquence.\n- Si `#f`, rien ne se passe ; aucune valeur n\'est renvoyée.\n\n### Exemple\n\n```scheme\n(when (< 0 1)\n  (lumi-message "Condition is true!")\n  (lumi-message "Executing additional actions."))\n```\n\n### Comparer `if` et `when`\n\nLes deux ensemble dans le même exemple :\n\n```scheme\n(if (= 0 1)\n  (lumi-message "This will not run")\n  (when (< 0 1)\n    (lumi-message "The \'when\' condition is true!")\n    (lumi-message "Executing multiple actions within \'when\'.")))\n```\n\n#### Explication\n\n1. **`if` :** `(= 0 1)` est faux, donc branche `else`.\n2. **`when` dans le `else` :** `(< 0 1)` est vrai ; les deux `lumi-message` s\'exécutent.\n\n#### Pourquoi `when` ?\n\n- Pas de `else` vide ou factice.\n- Montre que seule la branche vraie compte.\n\n### Résumé\n\n- **`if` :** quand les deux branches comptent.\n- **`when` :** branche vraie seule, plusieurs actions possibles.\n- Les combiner structure clairement des conditions complexes.'}, 'ja': {'title': 'when', 'body': 'En Scheme, `if` est polyvalent, mais sans `else` explicite il devient vite confus — surtout quand seule la branche vraie doit s\'exécuter. Dans ce cas, `when` est plus clair et concis.\n\nLa forme de base de `when` :\n\n```scheme\n(when test-is-true\n  do-this\n  do-that)\n```\n\n- Si `#t`, toutes les expressions du corps s\'exécutent en séquence.\n- Si `#f`, rien ne se passe ; aucune valeur n\'est renvoyée.\n\n### Exemple\n\n```scheme\n(when (< 0 1)\n  (lumi-message "Condition is true!")\n  (lumi-message "Executing additional actions."))\n```\n\n### Comparer `if` et `when`\n\nLes deux ensemble dans le même exemple :\n\n```scheme\n(if (= 0 1)\n  (lumi-message "This will not run")\n  (when (< 0 1)\n    (lumi-message "The \'when\' condition is true!")\n    (lumi-message "Executing multiple actions within \'when\'.")))\n```\n\n#### Explication\n\n1. **`if` :** `(= 0 1)` est faux, donc branche `else`.\n2. **`when` dans le `else` :** `(< 0 1)` est vrai ; les deux `lumi-message` s\'exécutent.\n\n#### Pourquoi `when` ?\n\n- Pas de `else` vide ou factice.\n- Montre que seule la branche vraie compte.\n\n### Résumé\n\n- **`if` :** quand les deux branches comptent.\n- **`when` :** branche vraie seule, plusieurs actions possibles.\n- Les combiner structure clairement des conditions complexes.'}, 'ko': {'title': 'when', 'body': 'En Scheme, `if` est polyvalent, mais sans `else` explicite il devient vite confus — surtout quand seule la branche vraie doit s\'exécuter. Dans ce cas, `when` est plus clair et concis.\n\nLa forme de base de `when` :\n\n```scheme\n(when test-is-true\n  do-this\n  do-that)\n```\n\n- Si `#t`, toutes les expressions du corps s\'exécutent en séquence.\n- Si `#f`, rien ne se passe ; aucune valeur n\'est renvoyée.\n\n### Exemple\n\n```scheme\n(when (< 0 1)\n  (lumi-message "Condition is true!")\n  (lumi-message "Executing additional actions."))\n```\n\n### Comparer `if` et `when`\n\nLes deux ensemble dans le même exemple :\n\n```scheme\n(if (= 0 1)\n  (lumi-message "This will not run")\n  (when (< 0 1)\n    (lumi-message "The \'when\' condition is true!")\n    (lumi-message "Executing multiple actions within \'when\'.")))\n```\n\n#### Explication\n\n1. **`if` :** `(= 0 1)` est faux, donc branche `else`.\n2. **`when` dans le `else` :** `(< 0 1)` est vrai ; les deux `lumi-message` s\'exécutent.\n\n#### Pourquoi `when` ?\n\n- Pas de `else` vide ou factice.\n- Montre que seule la branche vraie compte.\n\n### Résumé\n\n- **`if` :** quand les deux branches comptent.\n- **`when` :** branche vraie seule, plusieurs actions possibles.\n- Les combiner structure clairement des conditions complexes.'}, 'nl': {'title': 'when', 'body': 'En Scheme, `if` est polyvalent, mais sans `else` explicite il devient vite confus — surtout quand seule la branche vraie doit s\'exécuter. Dans ce cas, `when` est plus clair et concis.\n\nLa forme de base de `when` :\n\n```scheme\n(when test-is-true\n  do-this\n  do-that)\n```\n\n- Si `#t`, toutes les expressions du corps s\'exécutent en séquence.\n- Si `#f`, rien ne se passe ; aucune valeur n\'est renvoyée.\n\n### Exemple\n\n```scheme\n(when (< 0 1)\n  (lumi-message "Condition is true!")\n  (lumi-message "Executing additional actions."))\n```\n\n### Comparer `if` et `when`\n\nLes deux ensemble dans le même exemple :\n\n```scheme\n(if (= 0 1)\n  (lumi-message "This will not run")\n  (when (< 0 1)\n    (lumi-message "The \'when\' condition is true!")\n    (lumi-message "Executing multiple actions within \'when\'.")))\n```\n\n#### Explication\n\n1. **`if` :** `(= 0 1)` est faux, donc branche `else`.\n2. **`when` dans le `else` :** `(< 0 1)` est vrai ; les deux `lumi-message` s\'exécutent.\n\n#### Pourquoi `when` ?\n\n- Pas de `else` vide ou factice.\n- Montre que seule la branche vraie compte.\n\n### Résumé\n\n- **`if` :** quand les deux branches comptent.\n- **`when` :** branche vraie seule, plusieurs actions possibles.\n- Les combiner structure clairement des conditions complexes.'}, 'pl': {'title': 'when', 'body': 'En Scheme, `if` est polyvalent, mais sans `else` explicite il devient vite confus — surtout quand seule la branche vraie doit s\'exécuter. Dans ce cas, `when` est plus clair et concis.\n\nLa forme de base de `when` :\n\n```scheme\n(when test-is-true\n  do-this\n  do-that)\n```\n\n- Si `#t`, toutes les expressions du corps s\'exécutent en séquence.\n- Si `#f`, rien ne se passe ; aucune valeur n\'est renvoyée.\n\n### Exemple\n\n```scheme\n(when (< 0 1)\n  (lumi-message "Condition is true!")\n  (lumi-message "Executing additional actions."))\n```\n\n### Comparer `if` et `when`\n\nLes deux ensemble dans le même exemple :\n\n```scheme\n(if (= 0 1)\n  (lumi-message "This will not run")\n  (when (< 0 1)\n    (lumi-message "The \'when\' condition is true!")\n    (lumi-message "Executing multiple actions within \'when\'.")))\n```\n\n#### Explication\n\n1. **`if` :** `(= 0 1)` est faux, donc branche `else`.\n2. **`when` dans le `else` :** `(< 0 1)` est vrai ; les deux `lumi-message` s\'exécutent.\n\n#### Pourquoi `when` ?\n\n- Pas de `else` vide ou factice.\n- Montre que seule la branche vraie compte.\n\n### Résumé\n\n- **`if` :** quand les deux branches comptent.\n- **`when` :** branche vraie seule, plusieurs actions possibles.\n- Les combiner structure clairement des conditions complexes.'}, 'pt-br': {'title': 'when', 'body': 'En Scheme, `if` est polyvalent, mais sans `else` explicite il devient vite confus — surtout quand seule la branche vraie doit s\'exécuter. Dans ce cas, `when` est plus clair et concis.\n\nLa forme de base de `when` :\n\n```scheme\n(when test-is-true\n  do-this\n  do-that)\n```\n\n- Si `#t`, toutes les expressions du corps s\'exécutent en séquence.\n- Si `#f`, rien ne se passe ; aucune valeur n\'est renvoyée.\n\n### Exemple\n\n```scheme\n(when (< 0 1)\n  (lumi-message "Condition is true!")\n  (lumi-message "Executing additional actions."))\n```\n\n### Comparer `if` et `when`\n\nLes deux ensemble dans le même exemple :\n\n```scheme\n(if (= 0 1)\n  (lumi-message "This will not run")\n  (when (< 0 1)\n    (lumi-message "The \'when\' condition is true!")\n    (lumi-message "Executing multiple actions within \'when\'.")))\n```\n\n#### Explication\n\n1. **`if` :** `(= 0 1)` est faux, donc branche `else`.\n2. **`when` dans le `else` :** `(< 0 1)` est vrai ; les deux `lumi-message` s\'exécutent.\n\n#### Pourquoi `when` ?\n\n- Pas de `else` vide ou factice.\n- Montre que seule la branche vraie compte.\n\n### Résumé\n\n- **`if` :** quand les deux branches comptent.\n- **`when` :** branche vraie seule, plusieurs actions possibles.\n- Les combiner structure clairement des conditions complexes.'}, 'ru': {'title': 'when', 'body': 'En Scheme, `if` est polyvalent, mais sans `else` explicite il devient vite confus — surtout quand seule la branche vraie doit s\'exécuter. Dans ce cas, `when` est plus clair et concis.\n\nLa forme de base de `when` :\n\n```scheme\n(when test-is-true\n  do-this\n  do-that)\n```\n\n- Si `#t`, toutes les expressions du corps s\'exécutent en séquence.\n- Si `#f`, rien ne se passe ; aucune valeur n\'est renvoyée.\n\n### Exemple\n\n```scheme\n(when (< 0 1)\n  (lumi-message "Condition is true!")\n  (lumi-message "Executing additional actions."))\n```\n\n### Comparer `if` et `when`\n\nLes deux ensemble dans le même exemple :\n\n```scheme\n(if (= 0 1)\n  (lumi-message "This will not run")\n  (when (< 0 1)\n    (lumi-message "The \'when\' condition is true!")\n    (lumi-message "Executing multiple actions within \'when\'.")))\n```\n\n#### Explication\n\n1. **`if` :** `(= 0 1)` est faux, donc branche `else`.\n2. **`when` dans le `else` :** `(< 0 1)` est vrai ; les deux `lumi-message` s\'exécutent.\n\n#### Pourquoi `when` ?\n\n- Pas de `else` vide ou factice.\n- Montre que seule la branche vraie compte.\n\n### Résumé\n\n- **`if` :** quand les deux branches comptent.\n- **`when` :** branche vraie seule, plusieurs actions possibles.\n- Les combiner structure clairement des conditions complexes.'}, 'sv': {'title': 'when', 'body': 'En Scheme, `if` est polyvalent, mais sans `else` explicite il devient vite confus — surtout quand seule la branche vraie doit s\'exécuter. Dans ce cas, `when` est plus clair et concis.\n\nLa forme de base de `when` :\n\n```scheme\n(when test-is-true\n  do-this\n  do-that)\n```\n\n- Si `#t`, toutes les expressions du corps s\'exécutent en séquence.\n- Si `#f`, rien ne se passe ; aucune valeur n\'est renvoyée.\n\n### Exemple\n\n```scheme\n(when (< 0 1)\n  (lumi-message "Condition is true!")\n  (lumi-message "Executing additional actions."))\n```\n\n### Comparer `if` et `when`\n\nLes deux ensemble dans le même exemple :\n\n```scheme\n(if (= 0 1)\n  (lumi-message "This will not run")\n  (when (< 0 1)\n    (lumi-message "The \'when\' condition is true!")\n    (lumi-message "Executing multiple actions within \'when\'.")))\n```\n\n#### Explication\n\n1. **`if` :** `(= 0 1)` est faux, donc branche `else`.\n2. **`when` dans le `else` :** `(< 0 1)` est vrai ; les deux `lumi-message` s\'exécutent.\n\n#### Pourquoi `when` ?\n\n- Pas de `else` vide ou factice.\n- Montre que seule la branche vraie compte.\n\n### Résumé\n\n- **`if` :** quand les deux branches comptent.\n- **`when` :** branche vraie seule, plusieurs actions possibles.\n- Les combiner structure clairement des conditions complexes.'}, 'th': {'title': 'when', 'body': 'En Scheme, `if` est polyvalent, mais sans `else` explicite il devient vite confus — surtout quand seule la branche vraie doit s\'exécuter. Dans ce cas, `when` est plus clair et concis.\n\nLa forme de base de `when` :\n\n```scheme\n(when test-is-true\n  do-this\n  do-that)\n```\n\n- Si `#t`, toutes les expressions du corps s\'exécutent en séquence.\n- Si `#f`, rien ne se passe ; aucune valeur n\'est renvoyée.\n\n### Exemple\n\n```scheme\n(when (< 0 1)\n  (lumi-message "Condition is true!")\n  (lumi-message "Executing additional actions."))\n```\n\n### Comparer `if` et `when`\n\nLes deux ensemble dans le même exemple :\n\n```scheme\n(if (= 0 1)\n  (lumi-message "This will not run")\n  (when (< 0 1)\n    (lumi-message "The \'when\' condition is true!")\n    (lumi-message "Executing multiple actions within \'when\'.")))\n```\n\n#### Explication\n\n1. **`if` :** `(= 0 1)` est faux, donc branche `else`.\n2. **`when` dans le `else` :** `(< 0 1)` est vrai ; les deux `lumi-message` s\'exécutent.\n\n#### Pourquoi `when` ?\n\n- Pas de `else` vide ou factice.\n- Montre que seule la branche vraie compte.\n\n### Résumé\n\n- **`if` :** quand les deux branches comptent.\n- **`when` :** branche vraie seule, plusieurs actions possibles.\n- Les combiner structure clairement des conditions complexes.'}, 'zh-cn': {'title': 'when', 'body': 'En Scheme, `if` est polyvalent, mais sans `else` explicite il devient vite confus — surtout quand seule la branche vraie doit s\'exécuter. Dans ce cas, `when` est plus clair et concis.\n\nLa forme de base de `when` :\n\n```scheme\n(when test-is-true\n  do-this\n  do-that)\n```\n\n- Si `#t`, toutes les expressions du corps s\'exécutent en séquence.\n- Si `#f`, rien ne se passe ; aucune valeur n\'est renvoyée.\n\n### Exemple\n\n```scheme\n(when (< 0 1)\n  (lumi-message "Condition is true!")\n  (lumi-message "Executing additional actions."))\n```\n\n### Comparer `if` et `when`\n\nLes deux ensemble dans le même exemple :\n\n```scheme\n(if (= 0 1)\n  (lumi-message "This will not run")\n  (when (< 0 1)\n    (lumi-message "The \'when\' condition is true!")\n    (lumi-message "Executing multiple actions within \'when\'.")))\n```\n\n#### Explication\n\n1. **`if` :** `(= 0 1)` est faux, donc branche `else`.\n2. **`when` dans le `else` :** `(< 0 1)` est vrai ; les deux `lumi-message` s\'exécutent.\n\n#### Pourquoi `when` ?\n\n- Pas de `else` vide ou factice.\n- Montre que seule la branche vraie compte.\n\n### Résumé\n\n- **`if` :** quand les deux branches comptent.\n- **`when` :** branche vraie seule, plusieurs actions possibles.\n- Les combiner structure clairement des conditions complexes.'}, 'zh-tw': {'title': 'when', 'body': 'En Scheme, `if` est polyvalent, mais sans `else` explicite il devient vite confus — surtout quand seule la branche vraie doit s\'exécuter. Dans ce cas, `when` est plus clair et concis.\n\nLa forme de base de `when` :\n\n```scheme\n(when test-is-true\n  do-this\n  do-that)\n```\n\n- Si `#t`, toutes les expressions du corps s\'exécutent en séquence.\n- Si `#f`, rien ne se passe ; aucune valeur n\'est renvoyée.\n\n### Exemple\n\n```scheme\n(when (< 0 1)\n  (lumi-message "Condition is true!")\n  (lumi-message "Executing additional actions."))\n```\n\n### Comparer `if` et `when`\n\nLes deux ensemble dans le même exemple :\n\n```scheme\n(if (= 0 1)\n  (lumi-message "This will not run")\n  (when (< 0 1)\n    (lumi-message "The \'when\' condition is true!")\n    (lumi-message "Executing multiple actions within \'when\'.")))\n```\n\n#### Explication\n\n1. **`if` :** `(= 0 1)` est faux, donc branche `else`.\n2. **`when` dans le `else` :** `(< 0 1)` est vrai ; les deux `lumi-message` s\'exécutent.\n\n#### Pourquoi `when` ?\n\n- Pas de `else` vide ou factice.\n- Montre que seule la branche vraie compte.\n\n### Résumé\n\n- **`if` :** quand les deux branches comptent.\n- **`when` :** branche vraie seule, plusieurs actions possibles.\n- Les combiner structure clairement des conditions complexes.'}}
    meta = bodies[lang]
    return meta["title"], meta["body"]

def _page_content_hub_scripting_fundamentals_Iteration__index_md(lang: str) -> tuple[str, str]:
    bodies = {'de': {'title': 'Iteration', 'body': 'Iteration ist ein Grundpfeiler der Programmierung: Sie ermöglicht Skripten, Aktionen zu wiederholen und Datensammlungen effizient zu verarbeiten. In Scheme bietet Iteration Werkzeuge, um repetitive Aufgaben zu automatisieren, Datenstrukturen zu bearbeiten und ausgefeilte Ausführungsmuster zu erzeugen.\n\n### Die Rolle der Iteration in Scheme\n\n- **Wiederholung automatisieren:** Aktionen mehrfach ausführen, ohne Code zu duplizieren.\n- **Effizienz steigern:** Große Operationen systematisch abwickeln.\n- **Code straffen:** Redundanz vermeiden und Lesbarkeit verbessern.\n\n### Verfügbare Iterationskonstrukte\n\n- **map:** Funktion auf jedes Element anwenden, neue Liste zurückgeben.\n- **for-each:** Wie `map`, aber für Nebeneffekte ohne Rückgabe.\n- **do:** Allgemeine Schleife.\n- **recursion:** Funktionen rufen sich selbst auf.\n\n### So funktioniert Iteration\n\n1. **Wiederholung definieren**\n2. **Sequenziell ausführen**\n3. **Ergebnis zurückgeben (optional)**\n\nDiese Konstrukte helfen, anpassungsfähige und effiziente Skripte zu schreiben.'}, 'es': {'title': 'Iteración', 'body': 'La iteración es un pilar de la programación: permite repetir acciones y procesar colecciones de datos con eficiencia. En Scheme, la iteración ofrece herramientas para automatizar tareas repetitivas, manipular estructuras de datos y crear patrones de ejecución sofisticados.\n\n### El papel de la iteración en Scheme\n\n- **Automatizar la repetición:** Ejecutar acciones varias veces sin duplicar código.\n- **Mejorar la eficiencia:** Procesar operaciones a gran escala.\n- **Agilizar el código:** Eliminar redundancia y mejorar la legibilidad.\n\n### Tipos de iteración disponibles\n\n- **map:** Aplica una función a cada elemento, devuelve nueva lista.\n- **for-each:** Como `map`, pero para efectos secundarios sin retorno.\n- **do:** Bucle general.\n- **recursion:** Las funciones se llaman a sí mismas.\n\n### Cómo funciona la iteración\n\n1. **Definir repetición**\n2. **Ejecutar en secuencia**\n3. **Devolver resultado (opcional)**\n\nEstos constructos ayudan a escribir scripts adaptables y eficientes.'}, 'fr': {'title': 'Itération', 'body': "L'itération est une pierre angulaire de la programmation : elle permet aux scripts de répéter des actions et de traiter efficacement des collections de données. En Scheme, l'itération fournit les outils pour automatiser les tâches répétitives, manipuler des structures de données et créer des schémas d'exécution sophistiqués.\n\n### Le rôle de l'itération en Scheme\n\n- **Automatiser la répétition :** Exécuter des actions plusieurs fois sans dupliquer le code.\n- **Gagner en efficacité :** Traiter des opérations à grande échelle.\n- **Alléger le code :** Éliminer la redondance et améliorer la lisibilité.\n\n### Types d'itération disponibles\n\n- **map :** Applique une fonction à chaque élément, renvoie une nouvelle liste.\n- **for-each :** Comme `map`, mais pour des effets de bord sans valeur de retour.\n- **do :** Boucle générale.\n- **recursion :** Les fonctions s'appellent elles-mêmes.\n\n### Comment fonctionne l'itération\n\n1. **Définir une répétition**\n2. **Exécuter en séquence**\n3. **Renvoyer un résultat (facultatif)**\n\nCes constructions aident à écrire des scripts adaptables et efficaces."}, 'it': {'title': 'Iterazione', 'body': "L'itération est une pierre angulaire de la programmation : elle permet aux scripts de répéter des actions et de traiter efficacement des collections de données. En Scheme, l'itération fournit les outils pour automatiser les tâches répétitives, manipuler des structures de données et créer des schémas d'exécution sophistiqués.\n\n### Le rôle de l'itération en Scheme\n\n- **Automatiser la répétition :** Exécuter des actions plusieurs fois sans dupliquer le code.\n- **Gagner en efficacité :** Traiter des opérations à grande échelle.\n- **Alléger le code :** Éliminer la redondance et améliorer la lisibilité.\n\n### Types d'itération disponibles\n\n- **map :** Applique une fonction à chaque élément, renvoie une nouvelle liste.\n- **for-each :** Comme `map`, mais pour des effets de bord sans valeur de retour.\n- **do :** Boucle générale.\n- **recursion :** Les fonctions s'appellent elles-mêmes.\n\n### Comment fonctionne l'itération\n\n1. **Définir une répétition**\n2. **Exécuter en séquence**\n3. **Renvoyer un résultat (facultatif)**\n\nCes constructions aident à écrire des scripts adaptables et efficaces."}, 'ja': {'title': '反復', 'body': "L'itération est une pierre angulaire de la programmation : elle permet aux scripts de répéter des actions et de traiter efficacement des collections de données. En Scheme, l'itération fournit les outils pour automatiser les tâches répétitives, manipuler des structures de données et créer des schémas d'exécution sophistiqués.\n\n### Le rôle de l'itération en Scheme\n\n- **Automatiser la répétition :** Exécuter des actions plusieurs fois sans dupliquer le code.\n- **Gagner en efficacité :** Traiter des opérations à grande échelle.\n- **Alléger le code :** Éliminer la redondance et améliorer la lisibilité.\n\n### Types d'itération disponibles\n\n- **map :** Applique une fonction à chaque élément, renvoie une nouvelle liste.\n- **for-each :** Comme `map`, mais pour des effets de bord sans valeur de retour.\n- **do :** Boucle générale.\n- **recursion :** Les fonctions s'appellent elles-mêmes.\n\n### Comment fonctionne l'itération\n\n1. **Définir une répétition**\n2. **Exécuter en séquence**\n3. **Renvoyer un résultat (facultatif)**\n\nCes constructions aident à écrire des scripts adaptables et efficaces."}, 'ko': {'title': '반복', 'body': "L'itération est une pierre angulaire de la programmation : elle permet aux scripts de répéter des actions et de traiter efficacement des collections de données. En Scheme, l'itération fournit les outils pour automatiser les tâches répétitives, manipuler des structures de données et créer des schémas d'exécution sophistiqués.\n\n### Le rôle de l'itération en Scheme\n\n- **Automatiser la répétition :** Exécuter des actions plusieurs fois sans dupliquer le code.\n- **Gagner en efficacité :** Traiter des opérations à grande échelle.\n- **Alléger le code :** Éliminer la redondance et améliorer la lisibilité.\n\n### Types d'itération disponibles\n\n- **map :** Applique une fonction à chaque élément, renvoie une nouvelle liste.\n- **for-each :** Comme `map`, mais pour des effets de bord sans valeur de retour.\n- **do :** Boucle générale.\n- **recursion :** Les fonctions s'appellent elles-mêmes.\n\n### Comment fonctionne l'itération\n\n1. **Définir une répétition**\n2. **Exécuter en séquence**\n3. **Renvoyer un résultat (facultatif)**\n\nCes constructions aident à écrire des scripts adaptables et efficaces."}, 'nl': {'title': 'Iteratie', 'body': "L'itération est une pierre angulaire de la programmation : elle permet aux scripts de répéter des actions et de traiter efficacement des collections de données. En Scheme, l'itération fournit les outils pour automatiser les tâches répétitives, manipuler des structures de données et créer des schémas d'exécution sophistiqués.\n\n### Le rôle de l'itération en Scheme\n\n- **Automatiser la répétition :** Exécuter des actions plusieurs fois sans dupliquer le code.\n- **Gagner en efficacité :** Traiter des opérations à grande échelle.\n- **Alléger le code :** Éliminer la redondance et améliorer la lisibilité.\n\n### Types d'itération disponibles\n\n- **map :** Applique une fonction à chaque élément, renvoie une nouvelle liste.\n- **for-each :** Comme `map`, mais pour des effets de bord sans valeur de retour.\n- **do :** Boucle générale.\n- **recursion :** Les fonctions s'appellent elles-mêmes.\n\n### Comment fonctionne l'itération\n\n1. **Définir une répétition**\n2. **Exécuter en séquence**\n3. **Renvoyer un résultat (facultatif)**\n\nCes constructions aident à écrire des scripts adaptables et efficaces."}, 'pl': {'title': 'Iteracja', 'body': "L'itération est une pierre angulaire de la programmation : elle permet aux scripts de répéter des actions et de traiter efficacement des collections de données. En Scheme, l'itération fournit les outils pour automatiser les tâches répétitives, manipuler des structures de données et créer des schémas d'exécution sophistiqués.\n\n### Le rôle de l'itération en Scheme\n\n- **Automatiser la répétition :** Exécuter des actions plusieurs fois sans dupliquer le code.\n- **Gagner en efficacité :** Traiter des opérations à grande échelle.\n- **Alléger le code :** Éliminer la redondance et améliorer la lisibilité.\n\n### Types d'itération disponibles\n\n- **map :** Applique une fonction à chaque élément, renvoie une nouvelle liste.\n- **for-each :** Comme `map`, mais pour des effets de bord sans valeur de retour.\n- **do :** Boucle générale.\n- **recursion :** Les fonctions s'appellent elles-mêmes.\n\n### Comment fonctionne l'itération\n\n1. **Définir une répétition**\n2. **Exécuter en séquence**\n3. **Renvoyer un résultat (facultatif)**\n\nCes constructions aident à écrire des scripts adaptables et efficaces."}, 'pt-br': {'title': 'Iteração', 'body': "L'itération est une pierre angulaire de la programmation : elle permet aux scripts de répéter des actions et de traiter efficacement des collections de données. En Scheme, l'itération fournit les outils pour automatiser les tâches répétitives, manipuler des structures de données et créer des schémas d'exécution sophistiqués.\n\n### Le rôle de l'itération en Scheme\n\n- **Automatiser la répétition :** Exécuter des actions plusieurs fois sans dupliquer le code.\n- **Gagner en efficacité :** Traiter des opérations à grande échelle.\n- **Alléger le code :** Éliminer la redondance et améliorer la lisibilité.\n\n### Types d'itération disponibles\n\n- **map :** Applique une fonction à chaque élément, renvoie une nouvelle liste.\n- **for-each :** Comme `map`, mais pour des effets de bord sans valeur de retour.\n- **do :** Boucle générale.\n- **recursion :** Les fonctions s'appellent elles-mêmes.\n\n### Comment fonctionne l'itération\n\n1. **Définir une répétition**\n2. **Exécuter en séquence**\n3. **Renvoyer un résultat (facultatif)**\n\nCes constructions aident à écrire des scripts adaptables et efficaces."}, 'ru': {'title': 'Итерация', 'body': "L'itération est une pierre angulaire de la programmation : elle permet aux scripts de répéter des actions et de traiter efficacement des collections de données. En Scheme, l'itération fournit les outils pour automatiser les tâches répétitives, manipuler des structures de données et créer des schémas d'exécution sophistiqués.\n\n### Le rôle de l'itération en Scheme\n\n- **Automatiser la répétition :** Exécuter des actions plusieurs fois sans dupliquer le code.\n- **Gagner en efficacité :** Traiter des opérations à grande échelle.\n- **Alléger le code :** Éliminer la redondance et améliorer la lisibilité.\n\n### Types d'itération disponibles\n\n- **map :** Applique une fonction à chaque élément, renvoie une nouvelle liste.\n- **for-each :** Comme `map`, mais pour des effets de bord sans valeur de retour.\n- **do :** Boucle générale.\n- **recursion :** Les fonctions s'appellent elles-mêmes.\n\n### Comment fonctionne l'itération\n\n1. **Définir une répétition**\n2. **Exécuter en séquence**\n3. **Renvoyer un résultat (facultatif)**\n\nCes constructions aident à écrire des scripts adaptables et efficaces."}, 'sv': {'title': 'Iteration', 'body': "L'itération est une pierre angulaire de la programmation : elle permet aux scripts de répéter des actions et de traiter efficacement des collections de données. En Scheme, l'itération fournit les outils pour automatiser les tâches répétitives, manipuler des structures de données et créer des schémas d'exécution sophistiqués.\n\n### Le rôle de l'itération en Scheme\n\n- **Automatiser la répétition :** Exécuter des actions plusieurs fois sans dupliquer le code.\n- **Gagner en efficacité :** Traiter des opérations à grande échelle.\n- **Alléger le code :** Éliminer la redondance et améliorer la lisibilité.\n\n### Types d'itération disponibles\n\n- **map :** Applique une fonction à chaque élément, renvoie une nouvelle liste.\n- **for-each :** Comme `map`, mais pour des effets de bord sans valeur de retour.\n- **do :** Boucle générale.\n- **recursion :** Les fonctions s'appellent elles-mêmes.\n\n### Comment fonctionne l'itération\n\n1. **Définir une répétition**\n2. **Exécuter en séquence**\n3. **Renvoyer un résultat (facultatif)**\n\nCes constructions aident à écrire des scripts adaptables et efficaces."}, 'th': {'title': 'การวนซ้ำ', 'body': "L'itération est une pierre angulaire de la programmation : elle permet aux scripts de répéter des actions et de traiter efficacement des collections de données. En Scheme, l'itération fournit les outils pour automatiser les tâches répétitives, manipuler des structures de données et créer des schémas d'exécution sophistiqués.\n\n### Le rôle de l'itération en Scheme\n\n- **Automatiser la répétition :** Exécuter des actions plusieurs fois sans dupliquer le code.\n- **Gagner en efficacité :** Traiter des opérations à grande échelle.\n- **Alléger le code :** Éliminer la redondance et améliorer la lisibilité.\n\n### Types d'itération disponibles\n\n- **map :** Applique une fonction à chaque élément, renvoie une nouvelle liste.\n- **for-each :** Comme `map`, mais pour des effets de bord sans valeur de retour.\n- **do :** Boucle générale.\n- **recursion :** Les fonctions s'appellent elles-mêmes.\n\n### Comment fonctionne l'itération\n\n1. **Définir une répétition**\n2. **Exécuter en séquence**\n3. **Renvoyer un résultat (facultatif)**\n\nCes constructions aident à écrire des scripts adaptables et efficaces."}, 'zh-cn': {'title': '迭代', 'body': "L'itération est une pierre angulaire de la programmation : elle permet aux scripts de répéter des actions et de traiter efficacement des collections de données. En Scheme, l'itération fournit les outils pour automatiser les tâches répétitives, manipuler des structures de données et créer des schémas d'exécution sophistiqués.\n\n### Le rôle de l'itération en Scheme\n\n- **Automatiser la répétition :** Exécuter des actions plusieurs fois sans dupliquer le code.\n- **Gagner en efficacité :** Traiter des opérations à grande échelle.\n- **Alléger le code :** Éliminer la redondance et améliorer la lisibilité.\n\n### Types d'itération disponibles\n\n- **map :** Applique une fonction à chaque élément, renvoie une nouvelle liste.\n- **for-each :** Comme `map`, mais pour des effets de bord sans valeur de retour.\n- **do :** Boucle générale.\n- **recursion :** Les fonctions s'appellent elles-mêmes.\n\n### Comment fonctionne l'itération\n\n1. **Définir une répétition**\n2. **Exécuter en séquence**\n3. **Renvoyer un résultat (facultatif)**\n\nCes constructions aident à écrire des scripts adaptables et efficaces."}, 'zh-tw': {'title': '迭代', 'body': "L'itération est une pierre angulaire de la programmation : elle permet aux scripts de répéter des actions et de traiter efficacement des collections de données. En Scheme, l'itération fournit les outils pour automatiser les tâches répétitives, manipuler des structures de données et créer des schémas d'exécution sophistiqués.\n\n### Le rôle de l'itération en Scheme\n\n- **Automatiser la répétition :** Exécuter des actions plusieurs fois sans dupliquer le code.\n- **Gagner en efficacité :** Traiter des opérations à grande échelle.\n- **Alléger le code :** Éliminer la redondance et améliorer la lisibilité.\n\n### Types d'itération disponibles\n\n- **map :** Applique une fonction à chaque élément, renvoie une nouvelle liste.\n- **for-each :** Comme `map`, mais pour des effets de bord sans valeur de retour.\n- **do :** Boucle générale.\n- **recursion :** Les fonctions s'appellent elles-mêmes.\n\n### Comment fonctionne l'itération\n\n1. **Définir une répétition**\n2. **Exécuter en séquence**\n3. **Renvoyer un résultat (facultatif)**\n\nCes constructions aident à écrire des scripts adaptables et efficaces."}}
    meta = bodies[lang]
    return meta["title"], meta["body"]

def _page_content_hub_scripting_fundamentals_Iteration_do_md(lang: str) -> tuple[str, str]:
    bodies = {'de': {'title': 'do', 'body': 'Die Funktion `do` in Scheme ist eine Schleife mit Initialisierung, Aktualisierung und Abbruchbedingung. Nützlich, wenn eine Sequenz eine bestimmte Anzahl von Malen oder bis zu einer Bedingung laufen soll.\n\nDie allgemeine Form von `do`:\n\n```scheme\n(do ((var1 init1 update1)\n     (var2 init2 update2)\n     (var3 init3 update3))\n    (termination-condition result)\n  body)\n```\n\n- **Variable:** Schleifenvariable(n).\n- **Initial-value:** Startwert.\n- **Update-expression:** Aktualisierung pro Iteration.\n- **Termination-condition:** Abbruchbedingung.\n- **Result-expression:** Rückgabewert beim Abbruch.\n- **Body:** Code pro Iteration.\n\n---\n\n### Beispiel: Summe von 1 bis 5\n\n```scheme\n(do ((i 1 (+ i 1))      ; Initialize i to 1, increment by 1\n     (sum 0 (+ sum i))) ; Initialize sum to 0, add i to sum\n    ((> i 5) sum)       ; Terminate when i > 5, return sum\n  (lumi-message (number->string sum))) ; Print sum at each step\n```\n\n- `i` startet bei 1 und wird inkrementiert.\n- `sum` akkumuliert die Summe.\n- Abbruch bei `i > 5`, Rückgabe von `sum`.\n\n**Ausgabe**: `15`\n\n---\n\n### So funktioniert es\n\n1. **Initialisierung:** Startwerte zuweisen.\n2. **Abbruchprüfung:** Zu Beginn jeder Iteration.\n3. **Iteration:** Body ausführen, Variablen aktualisieren.\n\n---\n\n### Zusammenfassung\n\n- `do` bietet flexible Schleifen mit mehreren Variablen.\n- Nützlich bei Zustandsänderungen über Iterationen.\n- Die Abbruchbedingung bestimmt Ende und Ergebnis.\n\n`do` verbindet **gebundene Variablen** (wie `let`) mit **iterativer Steuerung**.'}, 'es': {'title': 'do', 'body': 'La función `do` en Scheme es un bucle con inicialización, actualización y condición de terminación. Útil para ejecutar una secuencia un número de veces o hasta cumplir una condición.\n\nLa forma general de `do`:\n\n```scheme\n(do ((var1 init1 update1)\n     (var2 init2 update2)\n     (var3 init3 update3))\n    (termination-condition result)\n  body)\n```\n\n- **Variable:** variable(s) del bucle.\n- **Initial-value:** valor inicial.\n- **Update-expression:** actualización por iteración.\n- **Termination-condition:** condición de parada.\n- **Result-expression:** valor al terminar.\n- **Body:** código por iteración.\n\n---\n\n### Ejemplo: suma del 1 al 5\n\n```scheme\n(do ((i 1 (+ i 1))      ; Initialize i to 1, increment by 1\n     (sum 0 (+ sum i))) ; Initialize sum to 0, add i to sum\n    ((> i 5) sum)       ; Terminate when i > 5, return sum\n  (lumi-message (number->string sum))) ; Print sum at each step\n```\n\n- `i` empieza en 1 e incrementa.\n- `sum` acumula la suma.\n- Parada cuando `i > 5`, retorno de `sum`.\n\n**Salida**: `15`\n\n---\n\n### Cómo funciona\n\n1. **Inicialización:** valores iniciales.\n2. **Comprobación de parada:** al inicio de cada vuelta.\n3. **Iteración:** ejecutar cuerpo, actualizar variables.\n\n---\n\n### Resumen\n\n- `do` ofrece bucles flexibles con varias variables.\n- Útil cuando el estado cambia en cada vuelta.\n- La condición de parada fija el fin y el resultado.\n\n`do` combina **enlaces** (como `let`) y **control iterativo**.'}, 'fr': {'title': 'do', 'body': "La fonction `do` en Scheme est une boucle avec initialisation, mise à jour et condition d'arrêt. Utile pour exécuter une séquence un nombre défini de fois ou jusqu'à une condition.\n\nLa forme générale de `do` :\n\n```scheme\n(do ((var1 init1 update1)\n     (var2 init2 update2)\n     (var3 init3 update3))\n    (termination-condition result)\n  body)\n```\n\n- **Variable :** variable(s) de boucle.\n- **Initial-value :** valeur initiale.\n- **Update-expression :** mise à jour par itération.\n- **Termination-condition :** condition d'arrêt.\n- **Result-expression :** valeur renvoyée à l'arrêt.\n- **Body :** code exécuté à chaque tour.\n\n---\n\n### Exemple : somme de 1 à 5\n\n```scheme\n(do ((i 1 (+ i 1))      ; Initialize i to 1, increment by 1\n     (sum 0 (+ sum i))) ; Initialize sum to 0, add i to sum\n    ((> i 5) sum)       ; Terminate when i > 5, return sum\n  (lumi-message (number->string sum))) ; Print sum at each step\n```\n\n- `i` commence à 1 et s'incrémente.\n- `sum` accumule la somme.\n- Arrêt quand `i > 5`, retour de `sum`.\n\n**Sortie** : `15`\n\n---\n\n### Comment ça marche\n\n1. **Initialisation :** valeurs de départ.\n2. **Test d'arrêt :** au début de chaque tour.\n3. **Itération :** exécuter le corps, mettre à jour les variables.\n\n---\n\n### Résumé\n\n- `do` offre des boucles flexibles à plusieurs variables.\n- Utile quand l'état évolue à chaque tour.\n- La condition d'arrêt fixe la fin et le résultat.\n\n`do` combine **liaisons** (comme `let`) et **contrôle itératif**."}, 'it': {'title': 'do', 'body': "La fonction `do` en Scheme est une boucle avec initialisation, mise à jour et condition d'arrêt. Utile pour exécuter une séquence un nombre défini de fois ou jusqu'à une condition.\n\nLa forme générale de `do` :\n\n```scheme\n(do ((var1 init1 update1)\n     (var2 init2 update2)\n     (var3 init3 update3))\n    (termination-condition result)\n  body)\n```\n\n- **Variable :** variable(s) de boucle.\n- **Initial-value :** valeur initiale.\n- **Update-expression :** mise à jour par itération.\n- **Termination-condition :** condition d'arrêt.\n- **Result-expression :** valeur renvoyée à l'arrêt.\n- **Body :** code exécuté à chaque tour.\n\n---\n\n### Exemple : somme de 1 à 5\n\n```scheme\n(do ((i 1 (+ i 1))      ; Initialize i to 1, increment by 1\n     (sum 0 (+ sum i))) ; Initialize sum to 0, add i to sum\n    ((> i 5) sum)       ; Terminate when i > 5, return sum\n  (lumi-message (number->string sum))) ; Print sum at each step\n```\n\n- `i` commence à 1 et s'incrémente.\n- `sum` accumule la somme.\n- Arrêt quand `i > 5`, retour de `sum`.\n\n**Sortie** : `15`\n\n---\n\n### Comment ça marche\n\n1. **Initialisation :** valeurs de départ.\n2. **Test d'arrêt :** au début de chaque tour.\n3. **Itération :** exécuter le corps, mettre à jour les variables.\n\n---\n\n### Résumé\n\n- `do` offre des boucles flexibles à plusieurs variables.\n- Utile quand l'état évolue à chaque tour.\n- La condition d'arrêt fixe la fin et le résultat.\n\n`do` combine **liaisons** (comme `let`) et **contrôle itératif**."}, 'ja': {'title': 'do', 'body': "La fonction `do` en Scheme est une boucle avec initialisation, mise à jour et condition d'arrêt. Utile pour exécuter une séquence un nombre défini de fois ou jusqu'à une condition.\n\nLa forme générale de `do` :\n\n```scheme\n(do ((var1 init1 update1)\n     (var2 init2 update2)\n     (var3 init3 update3))\n    (termination-condition result)\n  body)\n```\n\n- **Variable :** variable(s) de boucle.\n- **Initial-value :** valeur initiale.\n- **Update-expression :** mise à jour par itération.\n- **Termination-condition :** condition d'arrêt.\n- **Result-expression :** valeur renvoyée à l'arrêt.\n- **Body :** code exécuté à chaque tour.\n\n---\n\n### Exemple : somme de 1 à 5\n\n```scheme\n(do ((i 1 (+ i 1))      ; Initialize i to 1, increment by 1\n     (sum 0 (+ sum i))) ; Initialize sum to 0, add i to sum\n    ((> i 5) sum)       ; Terminate when i > 5, return sum\n  (lumi-message (number->string sum))) ; Print sum at each step\n```\n\n- `i` commence à 1 et s'incrémente.\n- `sum` accumule la somme.\n- Arrêt quand `i > 5`, retour de `sum`.\n\n**Sortie** : `15`\n\n---\n\n### Comment ça marche\n\n1. **Initialisation :** valeurs de départ.\n2. **Test d'arrêt :** au début de chaque tour.\n3. **Itération :** exécuter le corps, mettre à jour les variables.\n\n---\n\n### Résumé\n\n- `do` offre des boucles flexibles à plusieurs variables.\n- Utile quand l'état évolue à chaque tour.\n- La condition d'arrêt fixe la fin et le résultat.\n\n`do` combine **liaisons** (comme `let`) et **contrôle itératif**."}, 'ko': {'title': 'do', 'body': "La fonction `do` en Scheme est une boucle avec initialisation, mise à jour et condition d'arrêt. Utile pour exécuter une séquence un nombre défini de fois ou jusqu'à une condition.\n\nLa forme générale de `do` :\n\n```scheme\n(do ((var1 init1 update1)\n     (var2 init2 update2)\n     (var3 init3 update3))\n    (termination-condition result)\n  body)\n```\n\n- **Variable :** variable(s) de boucle.\n- **Initial-value :** valeur initiale.\n- **Update-expression :** mise à jour par itération.\n- **Termination-condition :** condition d'arrêt.\n- **Result-expression :** valeur renvoyée à l'arrêt.\n- **Body :** code exécuté à chaque tour.\n\n---\n\n### Exemple : somme de 1 à 5\n\n```scheme\n(do ((i 1 (+ i 1))      ; Initialize i to 1, increment by 1\n     (sum 0 (+ sum i))) ; Initialize sum to 0, add i to sum\n    ((> i 5) sum)       ; Terminate when i > 5, return sum\n  (lumi-message (number->string sum))) ; Print sum at each step\n```\n\n- `i` commence à 1 et s'incrémente.\n- `sum` accumule la somme.\n- Arrêt quand `i > 5`, retour de `sum`.\n\n**Sortie** : `15`\n\n---\n\n### Comment ça marche\n\n1. **Initialisation :** valeurs de départ.\n2. **Test d'arrêt :** au début de chaque tour.\n3. **Itération :** exécuter le corps, mettre à jour les variables.\n\n---\n\n### Résumé\n\n- `do` offre des boucles flexibles à plusieurs variables.\n- Utile quand l'état évolue à chaque tour.\n- La condition d'arrêt fixe la fin et le résultat.\n\n`do` combine **liaisons** (comme `let`) et **contrôle itératif**."}, 'nl': {'title': 'do', 'body': "La fonction `do` en Scheme est une boucle avec initialisation, mise à jour et condition d'arrêt. Utile pour exécuter une séquence un nombre défini de fois ou jusqu'à une condition.\n\nLa forme générale de `do` :\n\n```scheme\n(do ((var1 init1 update1)\n     (var2 init2 update2)\n     (var3 init3 update3))\n    (termination-condition result)\n  body)\n```\n\n- **Variable :** variable(s) de boucle.\n- **Initial-value :** valeur initiale.\n- **Update-expression :** mise à jour par itération.\n- **Termination-condition :** condition d'arrêt.\n- **Result-expression :** valeur renvoyée à l'arrêt.\n- **Body :** code exécuté à chaque tour.\n\n---\n\n### Exemple : somme de 1 à 5\n\n```scheme\n(do ((i 1 (+ i 1))      ; Initialize i to 1, increment by 1\n     (sum 0 (+ sum i))) ; Initialize sum to 0, add i to sum\n    ((> i 5) sum)       ; Terminate when i > 5, return sum\n  (lumi-message (number->string sum))) ; Print sum at each step\n```\n\n- `i` commence à 1 et s'incrémente.\n- `sum` accumule la somme.\n- Arrêt quand `i > 5`, retour de `sum`.\n\n**Sortie** : `15`\n\n---\n\n### Comment ça marche\n\n1. **Initialisation :** valeurs de départ.\n2. **Test d'arrêt :** au début de chaque tour.\n3. **Itération :** exécuter le corps, mettre à jour les variables.\n\n---\n\n### Résumé\n\n- `do` offre des boucles flexibles à plusieurs variables.\n- Utile quand l'état évolue à chaque tour.\n- La condition d'arrêt fixe la fin et le résultat.\n\n`do` combine **liaisons** (comme `let`) et **contrôle itératif**."}, 'pl': {'title': 'do', 'body': "La fonction `do` en Scheme est une boucle avec initialisation, mise à jour et condition d'arrêt. Utile pour exécuter une séquence un nombre défini de fois ou jusqu'à une condition.\n\nLa forme générale de `do` :\n\n```scheme\n(do ((var1 init1 update1)\n     (var2 init2 update2)\n     (var3 init3 update3))\n    (termination-condition result)\n  body)\n```\n\n- **Variable :** variable(s) de boucle.\n- **Initial-value :** valeur initiale.\n- **Update-expression :** mise à jour par itération.\n- **Termination-condition :** condition d'arrêt.\n- **Result-expression :** valeur renvoyée à l'arrêt.\n- **Body :** code exécuté à chaque tour.\n\n---\n\n### Exemple : somme de 1 à 5\n\n```scheme\n(do ((i 1 (+ i 1))      ; Initialize i to 1, increment by 1\n     (sum 0 (+ sum i))) ; Initialize sum to 0, add i to sum\n    ((> i 5) sum)       ; Terminate when i > 5, return sum\n  (lumi-message (number->string sum))) ; Print sum at each step\n```\n\n- `i` commence à 1 et s'incrémente.\n- `sum` accumule la somme.\n- Arrêt quand `i > 5`, retour de `sum`.\n\n**Sortie** : `15`\n\n---\n\n### Comment ça marche\n\n1. **Initialisation :** valeurs de départ.\n2. **Test d'arrêt :** au début de chaque tour.\n3. **Itération :** exécuter le corps, mettre à jour les variables.\n\n---\n\n### Résumé\n\n- `do` offre des boucles flexibles à plusieurs variables.\n- Utile quand l'état évolue à chaque tour.\n- La condition d'arrêt fixe la fin et le résultat.\n\n`do` combine **liaisons** (comme `let`) et **contrôle itératif**."}, 'pt-br': {'title': 'do', 'body': "La fonction `do` en Scheme est une boucle avec initialisation, mise à jour et condition d'arrêt. Utile pour exécuter une séquence un nombre défini de fois ou jusqu'à une condition.\n\nLa forme générale de `do` :\n\n```scheme\n(do ((var1 init1 update1)\n     (var2 init2 update2)\n     (var3 init3 update3))\n    (termination-condition result)\n  body)\n```\n\n- **Variable :** variable(s) de boucle.\n- **Initial-value :** valeur initiale.\n- **Update-expression :** mise à jour par itération.\n- **Termination-condition :** condition d'arrêt.\n- **Result-expression :** valeur renvoyée à l'arrêt.\n- **Body :** code exécuté à chaque tour.\n\n---\n\n### Exemple : somme de 1 à 5\n\n```scheme\n(do ((i 1 (+ i 1))      ; Initialize i to 1, increment by 1\n     (sum 0 (+ sum i))) ; Initialize sum to 0, add i to sum\n    ((> i 5) sum)       ; Terminate when i > 5, return sum\n  (lumi-message (number->string sum))) ; Print sum at each step\n```\n\n- `i` commence à 1 et s'incrémente.\n- `sum` accumule la somme.\n- Arrêt quand `i > 5`, retour de `sum`.\n\n**Sortie** : `15`\n\n---\n\n### Comment ça marche\n\n1. **Initialisation :** valeurs de départ.\n2. **Test d'arrêt :** au début de chaque tour.\n3. **Itération :** exécuter le corps, mettre à jour les variables.\n\n---\n\n### Résumé\n\n- `do` offre des boucles flexibles à plusieurs variables.\n- Utile quand l'état évolue à chaque tour.\n- La condition d'arrêt fixe la fin et le résultat.\n\n`do` combine **liaisons** (comme `let`) et **contrôle itératif**."}, 'ru': {'title': 'do', 'body': "La fonction `do` en Scheme est une boucle avec initialisation, mise à jour et condition d'arrêt. Utile pour exécuter une séquence un nombre défini de fois ou jusqu'à une condition.\n\nLa forme générale de `do` :\n\n```scheme\n(do ((var1 init1 update1)\n     (var2 init2 update2)\n     (var3 init3 update3))\n    (termination-condition result)\n  body)\n```\n\n- **Variable :** variable(s) de boucle.\n- **Initial-value :** valeur initiale.\n- **Update-expression :** mise à jour par itération.\n- **Termination-condition :** condition d'arrêt.\n- **Result-expression :** valeur renvoyée à l'arrêt.\n- **Body :** code exécuté à chaque tour.\n\n---\n\n### Exemple : somme de 1 à 5\n\n```scheme\n(do ((i 1 (+ i 1))      ; Initialize i to 1, increment by 1\n     (sum 0 (+ sum i))) ; Initialize sum to 0, add i to sum\n    ((> i 5) sum)       ; Terminate when i > 5, return sum\n  (lumi-message (number->string sum))) ; Print sum at each step\n```\n\n- `i` commence à 1 et s'incrémente.\n- `sum` accumule la somme.\n- Arrêt quand `i > 5`, retour de `sum`.\n\n**Sortie** : `15`\n\n---\n\n### Comment ça marche\n\n1. **Initialisation :** valeurs de départ.\n2. **Test d'arrêt :** au début de chaque tour.\n3. **Itération :** exécuter le corps, mettre à jour les variables.\n\n---\n\n### Résumé\n\n- `do` offre des boucles flexibles à plusieurs variables.\n- Utile quand l'état évolue à chaque tour.\n- La condition d'arrêt fixe la fin et le résultat.\n\n`do` combine **liaisons** (comme `let`) et **contrôle itératif**."}, 'sv': {'title': 'do', 'body': "La fonction `do` en Scheme est une boucle avec initialisation, mise à jour et condition d'arrêt. Utile pour exécuter une séquence un nombre défini de fois ou jusqu'à une condition.\n\nLa forme générale de `do` :\n\n```scheme\n(do ((var1 init1 update1)\n     (var2 init2 update2)\n     (var3 init3 update3))\n    (termination-condition result)\n  body)\n```\n\n- **Variable :** variable(s) de boucle.\n- **Initial-value :** valeur initiale.\n- **Update-expression :** mise à jour par itération.\n- **Termination-condition :** condition d'arrêt.\n- **Result-expression :** valeur renvoyée à l'arrêt.\n- **Body :** code exécuté à chaque tour.\n\n---\n\n### Exemple : somme de 1 à 5\n\n```scheme\n(do ((i 1 (+ i 1))      ; Initialize i to 1, increment by 1\n     (sum 0 (+ sum i))) ; Initialize sum to 0, add i to sum\n    ((> i 5) sum)       ; Terminate when i > 5, return sum\n  (lumi-message (number->string sum))) ; Print sum at each step\n```\n\n- `i` commence à 1 et s'incrémente.\n- `sum` accumule la somme.\n- Arrêt quand `i > 5`, retour de `sum`.\n\n**Sortie** : `15`\n\n---\n\n### Comment ça marche\n\n1. **Initialisation :** valeurs de départ.\n2. **Test d'arrêt :** au début de chaque tour.\n3. **Itération :** exécuter le corps, mettre à jour les variables.\n\n---\n\n### Résumé\n\n- `do` offre des boucles flexibles à plusieurs variables.\n- Utile quand l'état évolue à chaque tour.\n- La condition d'arrêt fixe la fin et le résultat.\n\n`do` combine **liaisons** (comme `let`) et **contrôle itératif**."}, 'th': {'title': 'do', 'body': "La fonction `do` en Scheme est une boucle avec initialisation, mise à jour et condition d'arrêt. Utile pour exécuter une séquence un nombre défini de fois ou jusqu'à une condition.\n\nLa forme générale de `do` :\n\n```scheme\n(do ((var1 init1 update1)\n     (var2 init2 update2)\n     (var3 init3 update3))\n    (termination-condition result)\n  body)\n```\n\n- **Variable :** variable(s) de boucle.\n- **Initial-value :** valeur initiale.\n- **Update-expression :** mise à jour par itération.\n- **Termination-condition :** condition d'arrêt.\n- **Result-expression :** valeur renvoyée à l'arrêt.\n- **Body :** code exécuté à chaque tour.\n\n---\n\n### Exemple : somme de 1 à 5\n\n```scheme\n(do ((i 1 (+ i 1))      ; Initialize i to 1, increment by 1\n     (sum 0 (+ sum i))) ; Initialize sum to 0, add i to sum\n    ((> i 5) sum)       ; Terminate when i > 5, return sum\n  (lumi-message (number->string sum))) ; Print sum at each step\n```\n\n- `i` commence à 1 et s'incrémente.\n- `sum` accumule la somme.\n- Arrêt quand `i > 5`, retour de `sum`.\n\n**Sortie** : `15`\n\n---\n\n### Comment ça marche\n\n1. **Initialisation :** valeurs de départ.\n2. **Test d'arrêt :** au début de chaque tour.\n3. **Itération :** exécuter le corps, mettre à jour les variables.\n\n---\n\n### Résumé\n\n- `do` offre des boucles flexibles à plusieurs variables.\n- Utile quand l'état évolue à chaque tour.\n- La condition d'arrêt fixe la fin et le résultat.\n\n`do` combine **liaisons** (comme `let`) et **contrôle itératif**."}, 'zh-cn': {'title': 'do', 'body': "La fonction `do` en Scheme est une boucle avec initialisation, mise à jour et condition d'arrêt. Utile pour exécuter une séquence un nombre défini de fois ou jusqu'à une condition.\n\nLa forme générale de `do` :\n\n```scheme\n(do ((var1 init1 update1)\n     (var2 init2 update2)\n     (var3 init3 update3))\n    (termination-condition result)\n  body)\n```\n\n- **Variable :** variable(s) de boucle.\n- **Initial-value :** valeur initiale.\n- **Update-expression :** mise à jour par itération.\n- **Termination-condition :** condition d'arrêt.\n- **Result-expression :** valeur renvoyée à l'arrêt.\n- **Body :** code exécuté à chaque tour.\n\n---\n\n### Exemple : somme de 1 à 5\n\n```scheme\n(do ((i 1 (+ i 1))      ; Initialize i to 1, increment by 1\n     (sum 0 (+ sum i))) ; Initialize sum to 0, add i to sum\n    ((> i 5) sum)       ; Terminate when i > 5, return sum\n  (lumi-message (number->string sum))) ; Print sum at each step\n```\n\n- `i` commence à 1 et s'incrémente.\n- `sum` accumule la somme.\n- Arrêt quand `i > 5`, retour de `sum`.\n\n**Sortie** : `15`\n\n---\n\n### Comment ça marche\n\n1. **Initialisation :** valeurs de départ.\n2. **Test d'arrêt :** au début de chaque tour.\n3. **Itération :** exécuter le corps, mettre à jour les variables.\n\n---\n\n### Résumé\n\n- `do` offre des boucles flexibles à plusieurs variables.\n- Utile quand l'état évolue à chaque tour.\n- La condition d'arrêt fixe la fin et le résultat.\n\n`do` combine **liaisons** (comme `let`) et **contrôle itératif**."}, 'zh-tw': {'title': 'do', 'body': "La fonction `do` en Scheme est une boucle avec initialisation, mise à jour et condition d'arrêt. Utile pour exécuter une séquence un nombre défini de fois ou jusqu'à une condition.\n\nLa forme générale de `do` :\n\n```scheme\n(do ((var1 init1 update1)\n     (var2 init2 update2)\n     (var3 init3 update3))\n    (termination-condition result)\n  body)\n```\n\n- **Variable :** variable(s) de boucle.\n- **Initial-value :** valeur initiale.\n- **Update-expression :** mise à jour par itération.\n- **Termination-condition :** condition d'arrêt.\n- **Result-expression :** valeur renvoyée à l'arrêt.\n- **Body :** code exécuté à chaque tour.\n\n---\n\n### Exemple : somme de 1 à 5\n\n```scheme\n(do ((i 1 (+ i 1))      ; Initialize i to 1, increment by 1\n     (sum 0 (+ sum i))) ; Initialize sum to 0, add i to sum\n    ((> i 5) sum)       ; Terminate when i > 5, return sum\n  (lumi-message (number->string sum))) ; Print sum at each step\n```\n\n- `i` commence à 1 et s'incrémente.\n- `sum` accumule la somme.\n- Arrêt quand `i > 5`, retour de `sum`.\n\n**Sortie** : `15`\n\n---\n\n### Comment ça marche\n\n1. **Initialisation :** valeurs de départ.\n2. **Test d'arrêt :** au début de chaque tour.\n3. **Itération :** exécuter le corps, mettre à jour les variables.\n\n---\n\n### Résumé\n\n- `do` offre des boucles flexibles à plusieurs variables.\n- Utile quand l'état évolue à chaque tour.\n- La condition d'arrêt fixe la fin et le résultat.\n\n`do` combine **liaisons** (comme `let`) et **contrôle itératif**."}}
    meta = bodies[lang]
    return meta["title"], meta["body"]

def _page_content_hub_scripting_fundamentals_Iteration_for_each_md(lang: str) -> tuple[str, str]:
    bodies = {'de': {'title': 'for-each', 'body': 'Die Funktion `for-each` in Scheme wendet eine Prozedur auf jedes Element einer Liste (oder mehrerer Listen) an. Im Gegensatz zu `map`, das eine neue Liste zurückgibt, dient `for-each` **Nebeneffekten** wie Ausgabe oder Variablenänderung.\n\nDie einfachste Form von `for-each`:\n\n```scheme\n(for-each procedure list)\n```\n\n- **Prozedur:** Funktion pro Element.\n- **Liste:** Zu verarbeitende Liste.\n\n---\n\n### Beispiel: Liste ausgeben\n\n```scheme\n(define (print-item x)\n  (lumi-message (number->string x)))\n\n(for-each print-item (list 1 2 3 4))\n```\n\n- `print-item` wird auf `(1 2 3 4)` angewendet.\n- Jede Zahl wird nacheinander ausgegeben.\n\n**Ausgabe**: `1 2 3 4`\n\n---\n\n### So funktioniert es\n\n1. **Über jedes Element iterieren:** Die Prozedur wird der Reihe nach ausgeführt.\n2. **Nebeneffekte:** Drucken, Protokollieren oder externe Variablen ändern — ohne neue Liste.\n\n---\n\n#### Mehrere Listen\n\nMit mehreren Listen verarbeitet `for-each` entsprechende Elemente paarweise.\n\n```scheme\n(define (sum-and-print x y)\n  (lumi-message (number->string (+ x y))))\n\n(for-each sum-and-print (list 1 2 3) (list 4 5 6))\n```\n\n**Ausgabe**: `5 7 9`\n\n---\n\n### Zusammenfassung\n\n- `for-each` eignet sich für Nebeneffekte pro Listenelement.\n- Im Gegensatz zu `map` **keine neue Liste**.\n- Mehrere Listen werden gleichzeitig verarbeitet.\n\nNutzen Sie `for-each`, wenn Aktionen wichtiger sind als Datentransformation.'}, 'es': {'title': 'for-each', 'body': 'La función `for-each` en Scheme aplica un procedimiento a cada elemento de una lista (o varias listas). A diferencia de `map`, que devuelve una nueva lista, `for-each` se usa por sus **efectos secundarios**: imprimir, registrar o modificar variables.\n\nLa forma más simple de `for-each`:\n\n```scheme\n(for-each procedure list)\n```\n\n- **Procedimiento:** Función por elemento.\n- **Lista:** Lista a procesar.\n\n---\n\n### Ejemplo: imprimir una lista\n\n```scheme\n(define (print-item x)\n  (lumi-message (number->string x)))\n\n(for-each print-item (list 1 2 3 4))\n```\n\n- `print-item` se aplica a `(1 2 3 4)`.\n- Cada número se imprime en secuencia.\n\n**Salida**: `1 2 3 4`\n\n---\n\n### Cómo funciona\n\n1. **Iterar cada elemento:** El procedimiento se ejecuta en orden.\n2. **Efectos secundarios:** Imprimir o modificar estado — sin nueva lista.\n\n---\n\n#### Varias listas\n\nCon varias listas, `for-each` procesa elementos correspondientes.\n\n```scheme\n(define (sum-and-print x y)\n  (lumi-message (number->string (+ x y))))\n\n(for-each sum-and-print (list 1 2 3) (list 4 5 6))\n```\n\n**Salida**: `5 7 9`\n\n---\n\n### Resumen\n\n- `for-each` sirve para efectos secundarios por elemento.\n- A diferencia de `map`, **sin nueva lista**.\n- Varias listas a la vez.\n\nUse `for-each` cuando la acción importa más que transformar datos.'}, 'fr': {'title': 'for-each', 'body': "La fonction `for-each` en Scheme applique une procédure à chaque élément d'une liste (ou de plusieurs listes). Contrairement à `map`, qui renvoie une nouvelle liste, `for-each` sert aux **effets de bord** : affichage, journalisation ou modification de variables.\n\nLa forme la plus simple de `for-each` :\n\n```scheme\n(for-each procedure list)\n```\n\n- **Procédure :** Fonction par élément.\n- **Liste :** Liste à parcourir.\n\n---\n\n### Exemple : afficher une liste\n\n```scheme\n(define (print-item x)\n  (lumi-message (number->string x)))\n\n(for-each print-item (list 1 2 3 4))\n```\n\n- `print-item` est appliqué à `(1 2 3 4)`.\n- Chaque nombre est affiché successivement.\n\n**Sortie** : `1 2 3 4`\n\n---\n\n### Comment ça marche\n\n1. **Parcourir chaque élément :** La procédure s'exécute dans l'ordre.\n2. **Effets de bord :** Affichage, journalisation ou modification — sans nouvelle liste.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `for-each` traite les éléments correspondants.\n\n```scheme\n(define (sum-and-print x y)\n  (lumi-message (number->string (+ x y))))\n\n(for-each sum-and-print (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `5 7 9`\n\n---\n\n### Résumé\n\n- `for-each` convient aux effets de bord sur chaque élément.\n- Contrairement à `map`, **pas de nouvelle liste**.\n- Plusieurs listes simultanément.\n\nUtilisez `for-each` lorsque l'action prime sur la transformation."}, 'it': {'title': 'for-each', 'body': "La fonction `for-each` en Scheme applique une procédure à chaque élément d'une liste (ou de plusieurs listes). Contrairement à `map`, qui renvoie une nouvelle liste, `for-each` sert aux **effets de bord** : affichage, journalisation ou modification de variables.\n\nLa forme la plus simple de `for-each` :\n\n```scheme\n(for-each procedure list)\n```\n\n- **Procédure :** Fonction par élément.\n- **Liste :** Liste à parcourir.\n\n---\n\n### Exemple : afficher une liste\n\n```scheme\n(define (print-item x)\n  (lumi-message (number->string x)))\n\n(for-each print-item (list 1 2 3 4))\n```\n\n- `print-item` est appliqué à `(1 2 3 4)`.\n- Chaque nombre est affiché successivement.\n\n**Sortie** : `1 2 3 4`\n\n---\n\n### Comment ça marche\n\n1. **Parcourir chaque élément :** La procédure s'exécute dans l'ordre.\n2. **Effets de bord :** Affichage, journalisation ou modification — sans nouvelle liste.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `for-each` traite les éléments correspondants.\n\n```scheme\n(define (sum-and-print x y)\n  (lumi-message (number->string (+ x y))))\n\n(for-each sum-and-print (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `5 7 9`\n\n---\n\n### Résumé\n\n- `for-each` convient aux effets de bord sur chaque élément.\n- Contrairement à `map`, **pas de nouvelle liste**.\n- Plusieurs listes simultanément.\n\nUtilisez `for-each` lorsque l'action prime sur la transformation."}, 'ja': {'title': 'for-each', 'body': "La fonction `for-each` en Scheme applique une procédure à chaque élément d'une liste (ou de plusieurs listes). Contrairement à `map`, qui renvoie une nouvelle liste, `for-each` sert aux **effets de bord** : affichage, journalisation ou modification de variables.\n\nLa forme la plus simple de `for-each` :\n\n```scheme\n(for-each procedure list)\n```\n\n- **Procédure :** Fonction par élément.\n- **Liste :** Liste à parcourir.\n\n---\n\n### Exemple : afficher une liste\n\n```scheme\n(define (print-item x)\n  (lumi-message (number->string x)))\n\n(for-each print-item (list 1 2 3 4))\n```\n\n- `print-item` est appliqué à `(1 2 3 4)`.\n- Chaque nombre est affiché successivement.\n\n**Sortie** : `1 2 3 4`\n\n---\n\n### Comment ça marche\n\n1. **Parcourir chaque élément :** La procédure s'exécute dans l'ordre.\n2. **Effets de bord :** Affichage, journalisation ou modification — sans nouvelle liste.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `for-each` traite les éléments correspondants.\n\n```scheme\n(define (sum-and-print x y)\n  (lumi-message (number->string (+ x y))))\n\n(for-each sum-and-print (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `5 7 9`\n\n---\n\n### Résumé\n\n- `for-each` convient aux effets de bord sur chaque élément.\n- Contrairement à `map`, **pas de nouvelle liste**.\n- Plusieurs listes simultanément.\n\nUtilisez `for-each` lorsque l'action prime sur la transformation."}, 'ko': {'title': 'for-each', 'body': "La fonction `for-each` en Scheme applique une procédure à chaque élément d'une liste (ou de plusieurs listes). Contrairement à `map`, qui renvoie une nouvelle liste, `for-each` sert aux **effets de bord** : affichage, journalisation ou modification de variables.\n\nLa forme la plus simple de `for-each` :\n\n```scheme\n(for-each procedure list)\n```\n\n- **Procédure :** Fonction par élément.\n- **Liste :** Liste à parcourir.\n\n---\n\n### Exemple : afficher une liste\n\n```scheme\n(define (print-item x)\n  (lumi-message (number->string x)))\n\n(for-each print-item (list 1 2 3 4))\n```\n\n- `print-item` est appliqué à `(1 2 3 4)`.\n- Chaque nombre est affiché successivement.\n\n**Sortie** : `1 2 3 4`\n\n---\n\n### Comment ça marche\n\n1. **Parcourir chaque élément :** La procédure s'exécute dans l'ordre.\n2. **Effets de bord :** Affichage, journalisation ou modification — sans nouvelle liste.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `for-each` traite les éléments correspondants.\n\n```scheme\n(define (sum-and-print x y)\n  (lumi-message (number->string (+ x y))))\n\n(for-each sum-and-print (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `5 7 9`\n\n---\n\n### Résumé\n\n- `for-each` convient aux effets de bord sur chaque élément.\n- Contrairement à `map`, **pas de nouvelle liste**.\n- Plusieurs listes simultanément.\n\nUtilisez `for-each` lorsque l'action prime sur la transformation."}, 'nl': {'title': 'for-each', 'body': "La fonction `for-each` en Scheme applique une procédure à chaque élément d'une liste (ou de plusieurs listes). Contrairement à `map`, qui renvoie une nouvelle liste, `for-each` sert aux **effets de bord** : affichage, journalisation ou modification de variables.\n\nLa forme la plus simple de `for-each` :\n\n```scheme\n(for-each procedure list)\n```\n\n- **Procédure :** Fonction par élément.\n- **Liste :** Liste à parcourir.\n\n---\n\n### Exemple : afficher une liste\n\n```scheme\n(define (print-item x)\n  (lumi-message (number->string x)))\n\n(for-each print-item (list 1 2 3 4))\n```\n\n- `print-item` est appliqué à `(1 2 3 4)`.\n- Chaque nombre est affiché successivement.\n\n**Sortie** : `1 2 3 4`\n\n---\n\n### Comment ça marche\n\n1. **Parcourir chaque élément :** La procédure s'exécute dans l'ordre.\n2. **Effets de bord :** Affichage, journalisation ou modification — sans nouvelle liste.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `for-each` traite les éléments correspondants.\n\n```scheme\n(define (sum-and-print x y)\n  (lumi-message (number->string (+ x y))))\n\n(for-each sum-and-print (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `5 7 9`\n\n---\n\n### Résumé\n\n- `for-each` convient aux effets de bord sur chaque élément.\n- Contrairement à `map`, **pas de nouvelle liste**.\n- Plusieurs listes simultanément.\n\nUtilisez `for-each` lorsque l'action prime sur la transformation."}, 'pl': {'title': 'for-each', 'body': "La fonction `for-each` en Scheme applique une procédure à chaque élément d'une liste (ou de plusieurs listes). Contrairement à `map`, qui renvoie une nouvelle liste, `for-each` sert aux **effets de bord** : affichage, journalisation ou modification de variables.\n\nLa forme la plus simple de `for-each` :\n\n```scheme\n(for-each procedure list)\n```\n\n- **Procédure :** Fonction par élément.\n- **Liste :** Liste à parcourir.\n\n---\n\n### Exemple : afficher une liste\n\n```scheme\n(define (print-item x)\n  (lumi-message (number->string x)))\n\n(for-each print-item (list 1 2 3 4))\n```\n\n- `print-item` est appliqué à `(1 2 3 4)`.\n- Chaque nombre est affiché successivement.\n\n**Sortie** : `1 2 3 4`\n\n---\n\n### Comment ça marche\n\n1. **Parcourir chaque élément :** La procédure s'exécute dans l'ordre.\n2. **Effets de bord :** Affichage, journalisation ou modification — sans nouvelle liste.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `for-each` traite les éléments correspondants.\n\n```scheme\n(define (sum-and-print x y)\n  (lumi-message (number->string (+ x y))))\n\n(for-each sum-and-print (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `5 7 9`\n\n---\n\n### Résumé\n\n- `for-each` convient aux effets de bord sur chaque élément.\n- Contrairement à `map`, **pas de nouvelle liste**.\n- Plusieurs listes simultanément.\n\nUtilisez `for-each` lorsque l'action prime sur la transformation."}, 'pt-br': {'title': 'for-each', 'body': "La fonction `for-each` en Scheme applique une procédure à chaque élément d'une liste (ou de plusieurs listes). Contrairement à `map`, qui renvoie une nouvelle liste, `for-each` sert aux **effets de bord** : affichage, journalisation ou modification de variables.\n\nLa forme la plus simple de `for-each` :\n\n```scheme\n(for-each procedure list)\n```\n\n- **Procédure :** Fonction par élément.\n- **Liste :** Liste à parcourir.\n\n---\n\n### Exemple : afficher une liste\n\n```scheme\n(define (print-item x)\n  (lumi-message (number->string x)))\n\n(for-each print-item (list 1 2 3 4))\n```\n\n- `print-item` est appliqué à `(1 2 3 4)`.\n- Chaque nombre est affiché successivement.\n\n**Sortie** : `1 2 3 4`\n\n---\n\n### Comment ça marche\n\n1. **Parcourir chaque élément :** La procédure s'exécute dans l'ordre.\n2. **Effets de bord :** Affichage, journalisation ou modification — sans nouvelle liste.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `for-each` traite les éléments correspondants.\n\n```scheme\n(define (sum-and-print x y)\n  (lumi-message (number->string (+ x y))))\n\n(for-each sum-and-print (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `5 7 9`\n\n---\n\n### Résumé\n\n- `for-each` convient aux effets de bord sur chaque élément.\n- Contrairement à `map`, **pas de nouvelle liste**.\n- Plusieurs listes simultanément.\n\nUtilisez `for-each` lorsque l'action prime sur la transformation."}, 'ru': {'title': 'for-each', 'body': "La fonction `for-each` en Scheme applique une procédure à chaque élément d'une liste (ou de plusieurs listes). Contrairement à `map`, qui renvoie une nouvelle liste, `for-each` sert aux **effets de bord** : affichage, journalisation ou modification de variables.\n\nLa forme la plus simple de `for-each` :\n\n```scheme\n(for-each procedure list)\n```\n\n- **Procédure :** Fonction par élément.\n- **Liste :** Liste à parcourir.\n\n---\n\n### Exemple : afficher une liste\n\n```scheme\n(define (print-item x)\n  (lumi-message (number->string x)))\n\n(for-each print-item (list 1 2 3 4))\n```\n\n- `print-item` est appliqué à `(1 2 3 4)`.\n- Chaque nombre est affiché successivement.\n\n**Sortie** : `1 2 3 4`\n\n---\n\n### Comment ça marche\n\n1. **Parcourir chaque élément :** La procédure s'exécute dans l'ordre.\n2. **Effets de bord :** Affichage, journalisation ou modification — sans nouvelle liste.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `for-each` traite les éléments correspondants.\n\n```scheme\n(define (sum-and-print x y)\n  (lumi-message (number->string (+ x y))))\n\n(for-each sum-and-print (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `5 7 9`\n\n---\n\n### Résumé\n\n- `for-each` convient aux effets de bord sur chaque élément.\n- Contrairement à `map`, **pas de nouvelle liste**.\n- Plusieurs listes simultanément.\n\nUtilisez `for-each` lorsque l'action prime sur la transformation."}, 'sv': {'title': 'for-each', 'body': "La fonction `for-each` en Scheme applique une procédure à chaque élément d'une liste (ou de plusieurs listes). Contrairement à `map`, qui renvoie une nouvelle liste, `for-each` sert aux **effets de bord** : affichage, journalisation ou modification de variables.\n\nLa forme la plus simple de `for-each` :\n\n```scheme\n(for-each procedure list)\n```\n\n- **Procédure :** Fonction par élément.\n- **Liste :** Liste à parcourir.\n\n---\n\n### Exemple : afficher une liste\n\n```scheme\n(define (print-item x)\n  (lumi-message (number->string x)))\n\n(for-each print-item (list 1 2 3 4))\n```\n\n- `print-item` est appliqué à `(1 2 3 4)`.\n- Chaque nombre est affiché successivement.\n\n**Sortie** : `1 2 3 4`\n\n---\n\n### Comment ça marche\n\n1. **Parcourir chaque élément :** La procédure s'exécute dans l'ordre.\n2. **Effets de bord :** Affichage, journalisation ou modification — sans nouvelle liste.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `for-each` traite les éléments correspondants.\n\n```scheme\n(define (sum-and-print x y)\n  (lumi-message (number->string (+ x y))))\n\n(for-each sum-and-print (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `5 7 9`\n\n---\n\n### Résumé\n\n- `for-each` convient aux effets de bord sur chaque élément.\n- Contrairement à `map`, **pas de nouvelle liste**.\n- Plusieurs listes simultanément.\n\nUtilisez `for-each` lorsque l'action prime sur la transformation."}, 'th': {'title': 'for-each', 'body': "La fonction `for-each` en Scheme applique une procédure à chaque élément d'une liste (ou de plusieurs listes). Contrairement à `map`, qui renvoie une nouvelle liste, `for-each` sert aux **effets de bord** : affichage, journalisation ou modification de variables.\n\nLa forme la plus simple de `for-each` :\n\n```scheme\n(for-each procedure list)\n```\n\n- **Procédure :** Fonction par élément.\n- **Liste :** Liste à parcourir.\n\n---\n\n### Exemple : afficher une liste\n\n```scheme\n(define (print-item x)\n  (lumi-message (number->string x)))\n\n(for-each print-item (list 1 2 3 4))\n```\n\n- `print-item` est appliqué à `(1 2 3 4)`.\n- Chaque nombre est affiché successivement.\n\n**Sortie** : `1 2 3 4`\n\n---\n\n### Comment ça marche\n\n1. **Parcourir chaque élément :** La procédure s'exécute dans l'ordre.\n2. **Effets de bord :** Affichage, journalisation ou modification — sans nouvelle liste.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `for-each` traite les éléments correspondants.\n\n```scheme\n(define (sum-and-print x y)\n  (lumi-message (number->string (+ x y))))\n\n(for-each sum-and-print (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `5 7 9`\n\n---\n\n### Résumé\n\n- `for-each` convient aux effets de bord sur chaque élément.\n- Contrairement à `map`, **pas de nouvelle liste**.\n- Plusieurs listes simultanément.\n\nUtilisez `for-each` lorsque l'action prime sur la transformation."}, 'zh-cn': {'title': 'for-each', 'body': "La fonction `for-each` en Scheme applique une procédure à chaque élément d'une liste (ou de plusieurs listes). Contrairement à `map`, qui renvoie une nouvelle liste, `for-each` sert aux **effets de bord** : affichage, journalisation ou modification de variables.\n\nLa forme la plus simple de `for-each` :\n\n```scheme\n(for-each procedure list)\n```\n\n- **Procédure :** Fonction par élément.\n- **Liste :** Liste à parcourir.\n\n---\n\n### Exemple : afficher une liste\n\n```scheme\n(define (print-item x)\n  (lumi-message (number->string x)))\n\n(for-each print-item (list 1 2 3 4))\n```\n\n- `print-item` est appliqué à `(1 2 3 4)`.\n- Chaque nombre est affiché successivement.\n\n**Sortie** : `1 2 3 4`\n\n---\n\n### Comment ça marche\n\n1. **Parcourir chaque élément :** La procédure s'exécute dans l'ordre.\n2. **Effets de bord :** Affichage, journalisation ou modification — sans nouvelle liste.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `for-each` traite les éléments correspondants.\n\n```scheme\n(define (sum-and-print x y)\n  (lumi-message (number->string (+ x y))))\n\n(for-each sum-and-print (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `5 7 9`\n\n---\n\n### Résumé\n\n- `for-each` convient aux effets de bord sur chaque élément.\n- Contrairement à `map`, **pas de nouvelle liste**.\n- Plusieurs listes simultanément.\n\nUtilisez `for-each` lorsque l'action prime sur la transformation."}, 'zh-tw': {'title': 'for-each', 'body': "La fonction `for-each` en Scheme applique une procédure à chaque élément d'une liste (ou de plusieurs listes). Contrairement à `map`, qui renvoie une nouvelle liste, `for-each` sert aux **effets de bord** : affichage, journalisation ou modification de variables.\n\nLa forme la plus simple de `for-each` :\n\n```scheme\n(for-each procedure list)\n```\n\n- **Procédure :** Fonction par élément.\n- **Liste :** Liste à parcourir.\n\n---\n\n### Exemple : afficher une liste\n\n```scheme\n(define (print-item x)\n  (lumi-message (number->string x)))\n\n(for-each print-item (list 1 2 3 4))\n```\n\n- `print-item` est appliqué à `(1 2 3 4)`.\n- Chaque nombre est affiché successivement.\n\n**Sortie** : `1 2 3 4`\n\n---\n\n### Comment ça marche\n\n1. **Parcourir chaque élément :** La procédure s'exécute dans l'ordre.\n2. **Effets de bord :** Affichage, journalisation ou modification — sans nouvelle liste.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `for-each` traite les éléments correspondants.\n\n```scheme\n(define (sum-and-print x y)\n  (lumi-message (number->string (+ x y))))\n\n(for-each sum-and-print (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `5 7 9`\n\n---\n\n### Résumé\n\n- `for-each` convient aux effets de bord sur chaque élément.\n- Contrairement à `map`, **pas de nouvelle liste**.\n- Plusieurs listes simultanément.\n\nUtilisez `for-each` lorsque l'action prime sur la transformation."}}
    meta = bodies[lang]
    return meta["title"], meta["body"]

def _page_content_hub_scripting_fundamentals_Iteration_map_md(lang: str) -> tuple[str, str]:
    bodies = {'de': {'title': 'map', 'body': 'Die Funktion `map` in Scheme wendet eine Prozedur auf jedes Element einer Liste (oder mehrerer Listen) an und **gibt eine neue Liste** mit den Ergebnissen zurück. Ideal für Datentransformationen.\n\nDie einfachste Form von `map` sieht so aus:\n\n```scheme\n(map procedure list)\n```\n\n- **Prozedur:** Funktion für jedes Listenelement.\n- **Liste:** Zu transformierende Liste.\n\n---\n\n### Beispiel: Elemente verdoppeln\n\n```scheme\n(define (double x)\n  (* x 2))\n\n(map double (list 1 2 3 4))\n```\n\n- `double` wird auf `(1 2 3 4)` angewendet.\n- Ergebnis: neue Liste mit verdoppelten Werten.\n\n**Ausgabe**: `(2 4 6 8)`\n\n---\n\n### So funktioniert es\n\n1. **Neue Liste:** `map` sammelt Ergebnisse in einer neuen Liste.\n2. **Transformation:** Für Datentransformation, nicht Nebeneffekte.\n\n---\n\n#### Mehrere Listen\n\nMit mehreren Listen verarbeitet `map` Elemente paarweise.\n\n```scheme\n(define (sum x y)\n  (+ x y))\n\n(map sum (list 1 2 3) (list 4 5 6))\n```\n\n**Ausgabe**: `(5 7 9)`\n\n---\n\n### Zusammenfassung\n\n- `map` transformiert Listen elementweise.\n- Im Gegensatz zu `for-each` **erzeugt `map` eine neue Liste**.\n- Mehrere Listen werden paarweise verarbeitet.\n\nMit `map` erstellen Sie transformierte Datensätze, während Originallisten unverändert bleiben.'}, 'es': {'title': 'map', 'body': 'La función `map` en Scheme aplica un procedimiento a cada elemento de una lista (o varias listas) y **devuelve una nueva lista** con los resultados. Ideal para transformar datos.\n\nLa forma más simple de `map` es:\n\n```scheme\n(map procedure list)\n```\n\n- **Procedimiento:** Función por elemento.\n- **Lista:** Lista a transformar.\n\n---\n\n### Ejemplo: duplicar cada elemento\n\n```scheme\n(define (double x)\n  (* x 2))\n\n(map double (list 1 2 3 4))\n```\n\n- `double` se aplica a `(1 2 3 4)`.\n- Resultado: nueva lista duplicada.\n\n**Salida**: `(2 4 6 8)`\n\n---\n\n### Cómo funciona\n\n1. **Nueva lista:** `map` recopila resultados.\n2. **Transformación:** Más que efectos secundarios.\n\n---\n\n#### Varias listas\n\nCon varias listas, `map` procesa elementos correspondientes.\n\n```scheme\n(define (sum x y)\n  (+ x y))\n\n(map sum (list 1 2 3) (list 4 5 6))\n```\n\n**Salida**: `(5 7 9)`\n\n---\n\n### Resumen\n\n- `map` transforma listas elemento a elemento.\n- A diferencia de `for-each`, `map` **produce una nueva lista**.\n- Varias listas se procesan por pares.\n\nCon `map`, cree versiones transformadas manteniendo las listas originales.'}, 'fr': {'title': 'map', 'body': "La fonction `map` en Scheme applique une procédure à chaque élément d'une liste (ou de plusieurs listes) et **renvoie une nouvelle liste** contenant les résultats. Idéale pour transformer des données.\n\nLa forme la plus simple de `map` ressemble à ceci :\n\n```scheme\n(map procedure list)\n```\n\n- **Procédure :** Fonction appliquée à chaque élément.\n- **Liste :** Liste à transformer.\n\n---\n\n### Exemple : doubler chaque élément\n\n```scheme\n(define (double x)\n  (* x 2))\n\n(map double (list 1 2 3 4))\n```\n\n- `double` est appliqué à `(1 2 3 4)`.\n- Résultat : nouvelle liste avec valeurs doublées.\n\n**Sortie** : `(2 4 6 8)`\n\n---\n\n### Comment ça marche\n\n1. **Nouvelle liste :** `map` collecte les résultats.\n2. **Transformation :** Plutôt que des effets de bord.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `map` traite les éléments correspondants.\n\n```scheme\n(define (sum x y)\n  (+ x y))\n\n(map sum (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `(5 7 9)`\n\n---\n\n### Résumé\n\n- `map` transforme des listes élément par élément.\n- Contrairement à `for-each`, `map` **produit une nouvelle liste**.\n- Plusieurs listes sont traitées par paires.\n\nAvec `map`, créez des versions transformées tout en conservant les listes d'origine."}, 'it': {'title': 'map', 'body': "La funzione `map` in Scheme applica una procedura a ogni elemento di una lista (o più liste) e **restituisce una nuova lista** con i risultati. È ideale per trasformare i dati.\n\nLa forme la plus simple de `map` ressemble à ceci :\n\n```scheme\n(map procedure list)\n```\n\n- **Procédure :** Fonction appliquée à chaque élément.\n- **Liste :** Liste à transformer.\n\n---\n\n### Exemple : doubler chaque élément\n\n```scheme\n(define (double x)\n  (* x 2))\n\n(map double (list 1 2 3 4))\n```\n\n- `double` est appliqué à `(1 2 3 4)`.\n- Résultat : nouvelle liste avec valeurs doublées.\n\n**Sortie** : `(2 4 6 8)`\n\n---\n\n### Comment ça marche\n\n1. **Nouvelle liste :** `map` collecte les résultats.\n2. **Transformation :** Plutôt que des effets de bord.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `map` traite les éléments correspondants.\n\n```scheme\n(define (sum x y)\n  (+ x y))\n\n(map sum (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `(5 7 9)`\n\n---\n\n### Résumé\n\n- `map` transforme des listes élément par élément.\n- Contrairement à `for-each`, `map` **produit une nouvelle liste**.\n- Plusieurs listes sont traitées par paires.\n\nAvec `map`, créez des versions transformées tout en conservant les listes d'origine."}, 'ja': {'title': 'map', 'body': "Scheme の `map` 関数は、リスト（または複数のリスト）の各要素に手続きを適用し、結果を含む**新しいリスト**を返します。データ変換に最適です。\n\nLa forme la plus simple de `map` ressemble à ceci :\n\n```scheme\n(map procedure list)\n```\n\n- **Procédure :** Fonction appliquée à chaque élément.\n- **Liste :** Liste à transformer.\n\n---\n\n### Exemple : doubler chaque élément\n\n```scheme\n(define (double x)\n  (* x 2))\n\n(map double (list 1 2 3 4))\n```\n\n- `double` est appliqué à `(1 2 3 4)`.\n- Résultat : nouvelle liste avec valeurs doublées.\n\n**Sortie** : `(2 4 6 8)`\n\n---\n\n### Comment ça marche\n\n1. **Nouvelle liste :** `map` collecte les résultats.\n2. **Transformation :** Plutôt que des effets de bord.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `map` traite les éléments correspondants.\n\n```scheme\n(define (sum x y)\n  (+ x y))\n\n(map sum (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `(5 7 9)`\n\n---\n\n### Résumé\n\n- `map` transforme des listes élément par élément.\n- Contrairement à `for-each`, `map` **produit une nouvelle liste**.\n- Plusieurs listes sont traitées par paires.\n\nAvec `map`, créez des versions transformées tout en conservant les listes d'origine."}, 'ko': {'title': 'map', 'body': "Scheme의 `map` 함수는 리스트(또는 여러 리스트)의 각 요소에 프로시저를 적용하고 결과가 담긴 **새 리스트**를 반환합니다. 데이터 변환에 이상적입니다.\n\nLa forme la plus simple de `map` ressemble à ceci :\n\n```scheme\n(map procedure list)\n```\n\n- **Procédure :** Fonction appliquée à chaque élément.\n- **Liste :** Liste à transformer.\n\n---\n\n### Exemple : doubler chaque élément\n\n```scheme\n(define (double x)\n  (* x 2))\n\n(map double (list 1 2 3 4))\n```\n\n- `double` est appliqué à `(1 2 3 4)`.\n- Résultat : nouvelle liste avec valeurs doublées.\n\n**Sortie** : `(2 4 6 8)`\n\n---\n\n### Comment ça marche\n\n1. **Nouvelle liste :** `map` collecte les résultats.\n2. **Transformation :** Plutôt que des effets de bord.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `map` traite les éléments correspondants.\n\n```scheme\n(define (sum x y)\n  (+ x y))\n\n(map sum (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `(5 7 9)`\n\n---\n\n### Résumé\n\n- `map` transforme des listes élément par élément.\n- Contrairement à `for-each`, `map` **produit une nouvelle liste**.\n- Plusieurs listes sont traitées par paires.\n\nAvec `map`, créez des versions transformées tout en conservant les listes d'origine."}, 'nl': {'title': 'map', 'body': "De functie `map` in Scheme past een procedure toe op elk element van een lijst (of meerdere lijsten) en **geeft een nieuwe lijst** met de resultaten terug. Ideaal voor datatransformaties.\n\nLa forme la plus simple de `map` ressemble à ceci :\n\n```scheme\n(map procedure list)\n```\n\n- **Procédure :** Fonction appliquée à chaque élément.\n- **Liste :** Liste à transformer.\n\n---\n\n### Exemple : doubler chaque élément\n\n```scheme\n(define (double x)\n  (* x 2))\n\n(map double (list 1 2 3 4))\n```\n\n- `double` est appliqué à `(1 2 3 4)`.\n- Résultat : nouvelle liste avec valeurs doublées.\n\n**Sortie** : `(2 4 6 8)`\n\n---\n\n### Comment ça marche\n\n1. **Nouvelle liste :** `map` collecte les résultats.\n2. **Transformation :** Plutôt que des effets de bord.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `map` traite les éléments correspondants.\n\n```scheme\n(define (sum x y)\n  (+ x y))\n\n(map sum (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `(5 7 9)`\n\n---\n\n### Résumé\n\n- `map` transforme des listes élément par élément.\n- Contrairement à `for-each`, `map` **produit une nouvelle liste**.\n- Plusieurs listes sont traitées par paires.\n\nAvec `map`, créez des versions transformées tout en conservant les listes d'origine."}, 'pl': {'title': 'map', 'body': "Funkcja `map` w Scheme stosuje procedurę do każdego elementu listy (lub wielu list) i **zwraca nową listę** z wynikami. Idealna do transformacji danych.\n\nLa forme la plus simple de `map` ressemble à ceci :\n\n```scheme\n(map procedure list)\n```\n\n- **Procédure :** Fonction appliquée à chaque élément.\n- **Liste :** Liste à transformer.\n\n---\n\n### Exemple : doubler chaque élément\n\n```scheme\n(define (double x)\n  (* x 2))\n\n(map double (list 1 2 3 4))\n```\n\n- `double` est appliqué à `(1 2 3 4)`.\n- Résultat : nouvelle liste avec valeurs doublées.\n\n**Sortie** : `(2 4 6 8)`\n\n---\n\n### Comment ça marche\n\n1. **Nouvelle liste :** `map` collecte les résultats.\n2. **Transformation :** Plutôt que des effets de bord.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `map` traite les éléments correspondants.\n\n```scheme\n(define (sum x y)\n  (+ x y))\n\n(map sum (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `(5 7 9)`\n\n---\n\n### Résumé\n\n- `map` transforme des listes élément par élément.\n- Contrairement à `for-each`, `map` **produit une nouvelle liste**.\n- Plusieurs listes sont traitées par paires.\n\nAvec `map`, créez des versions transformées tout en conservant les listes d'origine."}, 'pt-br': {'title': 'map', 'body': "A função `map` em Scheme aplica um procedimento a cada elemento de uma lista (ou várias listas) e **retorna uma nova lista** com os resultados. É ideal para transformar dados.\n\nLa forme la plus simple de `map` ressemble à ceci :\n\n```scheme\n(map procedure list)\n```\n\n- **Procédure :** Fonction appliquée à chaque élément.\n- **Liste :** Liste à transformer.\n\n---\n\n### Exemple : doubler chaque élément\n\n```scheme\n(define (double x)\n  (* x 2))\n\n(map double (list 1 2 3 4))\n```\n\n- `double` est appliqué à `(1 2 3 4)`.\n- Résultat : nouvelle liste avec valeurs doublées.\n\n**Sortie** : `(2 4 6 8)`\n\n---\n\n### Comment ça marche\n\n1. **Nouvelle liste :** `map` collecte les résultats.\n2. **Transformation :** Plutôt que des effets de bord.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `map` traite les éléments correspondants.\n\n```scheme\n(define (sum x y)\n  (+ x y))\n\n(map sum (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `(5 7 9)`\n\n---\n\n### Résumé\n\n- `map` transforme des listes élément par élément.\n- Contrairement à `for-each`, `map` **produit une nouvelle liste**.\n- Plusieurs listes sont traitées par paires.\n\nAvec `map`, créez des versions transformées tout en conservant les listes d'origine."}, 'ru': {'title': 'map', 'body': "Функция `map` в Scheme применяет процедуру к каждому элементу списка (или нескольких списков) и **возвращает новый список** с результатами. Идеальна для преобразования данных.\n\nLa forme la plus simple de `map` ressemble à ceci :\n\n```scheme\n(map procedure list)\n```\n\n- **Procédure :** Fonction appliquée à chaque élément.\n- **Liste :** Liste à transformer.\n\n---\n\n### Exemple : doubler chaque élément\n\n```scheme\n(define (double x)\n  (* x 2))\n\n(map double (list 1 2 3 4))\n```\n\n- `double` est appliqué à `(1 2 3 4)`.\n- Résultat : nouvelle liste avec valeurs doublées.\n\n**Sortie** : `(2 4 6 8)`\n\n---\n\n### Comment ça marche\n\n1. **Nouvelle liste :** `map` collecte les résultats.\n2. **Transformation :** Plutôt que des effets de bord.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `map` traite les éléments correspondants.\n\n```scheme\n(define (sum x y)\n  (+ x y))\n\n(map sum (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `(5 7 9)`\n\n---\n\n### Résumé\n\n- `map` transforme des listes élément par élément.\n- Contrairement à `for-each`, `map` **produit une nouvelle liste**.\n- Plusieurs listes sont traitées par paires.\n\nAvec `map`, créez des versions transformées tout en conservant les listes d'origine."}, 'sv': {'title': 'map', 'body': "Funktionen `map` i Scheme tillämpar en procedur på varje element i en lista (eller flera listor) och **returnerar en ny lista** med resultaten. Idealisk för datatransformation.\n\nLa forme la plus simple de `map` ressemble à ceci :\n\n```scheme\n(map procedure list)\n```\n\n- **Procédure :** Fonction appliquée à chaque élément.\n- **Liste :** Liste à transformer.\n\n---\n\n### Exemple : doubler chaque élément\n\n```scheme\n(define (double x)\n  (* x 2))\n\n(map double (list 1 2 3 4))\n```\n\n- `double` est appliqué à `(1 2 3 4)`.\n- Résultat : nouvelle liste avec valeurs doublées.\n\n**Sortie** : `(2 4 6 8)`\n\n---\n\n### Comment ça marche\n\n1. **Nouvelle liste :** `map` collecte les résultats.\n2. **Transformation :** Plutôt que des effets de bord.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `map` traite les éléments correspondants.\n\n```scheme\n(define (sum x y)\n  (+ x y))\n\n(map sum (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `(5 7 9)`\n\n---\n\n### Résumé\n\n- `map` transforme des listes élément par élément.\n- Contrairement à `for-each`, `map` **produit une nouvelle liste**.\n- Plusieurs listes sont traitées par paires.\n\nAvec `map`, créez des versions transformées tout en conservant les listes d'origine."}, 'th': {'title': 'map', 'body': "ฟังก์ชัน `map` ใน Scheme ใช้ procedure กับแต่ละองค์ประกอบของ list (หรือหลาย list) และ**คืน list ใหม่**ที่มีผลลัพธ์ เหมาะสำหรับการแปลงข้อมูล\n\nLa forme la plus simple de `map` ressemble à ceci :\n\n```scheme\n(map procedure list)\n```\n\n- **Procédure :** Fonction appliquée à chaque élément.\n- **Liste :** Liste à transformer.\n\n---\n\n### Exemple : doubler chaque élément\n\n```scheme\n(define (double x)\n  (* x 2))\n\n(map double (list 1 2 3 4))\n```\n\n- `double` est appliqué à `(1 2 3 4)`.\n- Résultat : nouvelle liste avec valeurs doublées.\n\n**Sortie** : `(2 4 6 8)`\n\n---\n\n### Comment ça marche\n\n1. **Nouvelle liste :** `map` collecte les résultats.\n2. **Transformation :** Plutôt que des effets de bord.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `map` traite les éléments correspondants.\n\n```scheme\n(define (sum x y)\n  (+ x y))\n\n(map sum (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `(5 7 9)`\n\n---\n\n### Résumé\n\n- `map` transforme des listes élément par élément.\n- Contrairement à `for-each`, `map` **produit une nouvelle liste**.\n- Plusieurs listes sont traitées par paires.\n\nAvec `map`, créez des versions transformées tout en conservant les listes d'origine."}, 'zh-cn': {'title': 'map', 'body': "Scheme 中的 `map` 函数对列表（或多个列表）的每个元素应用一个过程，并**返回包含结果的新列表**。非常适合数据转换。\n\nLa forme la plus simple de `map` ressemble à ceci :\n\n```scheme\n(map procedure list)\n```\n\n- **Procédure :** Fonction appliquée à chaque élément.\n- **Liste :** Liste à transformer.\n\n---\n\n### Exemple : doubler chaque élément\n\n```scheme\n(define (double x)\n  (* x 2))\n\n(map double (list 1 2 3 4))\n```\n\n- `double` est appliqué à `(1 2 3 4)`.\n- Résultat : nouvelle liste avec valeurs doublées.\n\n**Sortie** : `(2 4 6 8)`\n\n---\n\n### Comment ça marche\n\n1. **Nouvelle liste :** `map` collecte les résultats.\n2. **Transformation :** Plutôt que des effets de bord.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `map` traite les éléments correspondants.\n\n```scheme\n(define (sum x y)\n  (+ x y))\n\n(map sum (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `(5 7 9)`\n\n---\n\n### Résumé\n\n- `map` transforme des listes élément par élément.\n- Contrairement à `for-each`, `map` **produit une nouvelle liste**.\n- Plusieurs listes sont traitées par paires.\n\nAvec `map`, créez des versions transformées tout en conservant les listes d'origine."}, 'zh-tw': {'title': 'map', 'body': "Scheme 中的 `map` 函數對列表（或多個列表）的每個元素套用一個程序，並**回傳包含結果的新列表**。非常適合資料轉換。\n\nLa forme la plus simple de `map` ressemble à ceci :\n\n```scheme\n(map procedure list)\n```\n\n- **Procédure :** Fonction appliquée à chaque élément.\n- **Liste :** Liste à transformer.\n\n---\n\n### Exemple : doubler chaque élément\n\n```scheme\n(define (double x)\n  (* x 2))\n\n(map double (list 1 2 3 4))\n```\n\n- `double` est appliqué à `(1 2 3 4)`.\n- Résultat : nouvelle liste avec valeurs doublées.\n\n**Sortie** : `(2 4 6 8)`\n\n---\n\n### Comment ça marche\n\n1. **Nouvelle liste :** `map` collecte les résultats.\n2. **Transformation :** Plutôt que des effets de bord.\n\n---\n\n#### Plusieurs listes\n\nAvec plusieurs listes, `map` traite les éléments correspondants.\n\n```scheme\n(define (sum x y)\n  (+ x y))\n\n(map sum (list 1 2 3) (list 4 5 6))\n```\n\n**Sortie** : `(5 7 9)`\n\n---\n\n### Résumé\n\n- `map` transforme des listes élément par élément.\n- Contrairement à `for-each`, `map` **produit une nouvelle liste**.\n- Plusieurs listes sont traitées par paires.\n\nAvec `map`, créez des versions transformées tout en conservant les listes d'origine."}}
    meta = bodies[lang]
    return meta["title"], meta["body"]

def _page_content_hub_scripting_fundamentals_Iteration_recursion_md(lang: str) -> tuple[str, str]:
    bodies = {'de': {'title': 'Einfache Rekursion', 'body': 'Rekursion in Scheme bedeutet, dass eine Funktion sich selbst aufruft, um kleinere Teilprobleme zu lösen. **Einfache Rekursion** hat einen Basisfall zum Stoppen und einen rekursiven Fall zur Problemverkleinerung.\n\nAllgemeine Struktur:\n\n```scheme\n(define (function-name args)\n  (if (base-condition)\n    base-result\n    (recursive-call)))\n```\n\n- **Base Condition:** stoppt die Rekursion.\n- **Base Result:** Wert im Basisfall.\n- **Recursive Call:** Aufruf mit angepassten Argumenten.\n\n---\n\n### Beispiel: Summe 1 bis n\n\n```scheme\n(define (sum-to-n n)\n  (if (= n 0)                  ; Base case: stop when n is 0\n    0                          ; Base result: sum is 0\n    (+ n (sum-to-n (- n 1))))) ; Recursive call: sum current n with result of smaller problem\n```\n\n#### Zerlegen und wieder zusammensetzen\n\nRekursion zerlegt das Problem; jeder Aufruf bearbeitet ein Stück. Am Basisfall setzt sich das Ergebnis wieder zusammen.\n\n#### Schritt für Schritt: sum-to-n 3\n\n1. *sum-to-n 3* → *(+ 3 (sum-to-n 2))*\n2. *sum-to-n 2* → *(+ 2 (sum-to-n 1))*\n3. *sum-to-n 1* → *(+ 1 (sum-to-n 0))*\n4. *sum-to-n 0* → *0*\n\n#### Ergebnis zusammensetzen\n\n1. *sum-to-n 0* → *0*\n2. *sum-to-n 1* → *1*\n3. *sum-to-n 2* → *3*\n4. *sum-to-n 3* → *6*\n\n---\n\n### Beispiel: Listenelemente ausgeben\n\n```scheme\n(define (print-elements lst)\n  (if (null? lst)\n    (lumi-message "done")\n    (begin\n      (lumi-message (number->string (car lst))) ; Print the first element\n      (print-elements (cdr lst)))))             ; Process the rest of the list\n```\n\n- **Basisfall:** leere Liste → `"done"`.\n- **Rekursiv:** `car` ausgeben, Rest mit `cdr` verarbeiten.\n\n#### Verwendung\n\n```scheme\n(print-elements (list 1 2 3))\n```\n\nAusgabe: *"1"*, *"2"*, *"3"* — Ergebnis: *"done"*\n\n### Zusammenfassung\n\n- Basisfall stoppt; rekursiver Fall verkleinert das Problem.\n- Jeder Aufruf nähert sich dem Basisfall.\n- Immer einen Basisfall definieren — sonst endlose Rekursion.'}, 'es': {'title': 'Recursión simple', 'body': 'En Scheme, la recursión significa que una función se llama a sí misma para resolver subproblemas. Una **recursión simple** tiene caso base para detenerse y caso recursivo que reduce el problema.\n\nEstructura general:\n\n```scheme\n(define (function-name args)\n  (if (base-condition)\n    base-result\n    (recursive-call)))\n```\n\n- **Base Condition:** detiene la recursión.\n- **Base Result:** valor en caso base.\n- **Recursive Call:** llamada con argumentos reducidos.\n\n---\n\n### Ejemplo: suma de 1 a n\n\n```scheme\n(define (sum-to-n n)\n  (if (= n 0)                  ; Base case: stop when n is 0\n    0                          ; Base result: sum is 0\n    (+ n (sum-to-n (- n 1))))) ; Recursive call: sum current n with result of smaller problem\n```\n\n#### Descomponer y recombinar\n\nLa recursión descompone el problema; cada llamada trata una parte. En el caso base, el resultado se recomponen.\n\n#### Paso a paso: sum-to-n 3\n\n1. *sum-to-n 3* → *(+ 3 (sum-to-n 2))*\n2. *sum-to-n 2* → *(+ 2 (sum-to-n 1))*\n3. *sum-to-n 1* → *(+ 1 (sum-to-n 0))*\n4. *sum-to-n 0* → *0*\n\n#### Recombinar el resultado\n\n1. *sum-to-n 0* → *0*\n2. *sum-to-n 1* → *1*\n3. *sum-to-n 2* → *3*\n4. *sum-to-n 3* → *6*\n\n---\n\n### Ejemplo: imprimir cada elemento\n\n```scheme\n(define (print-elements lst)\n  (if (null? lst)\n    (lumi-message "done")\n    (begin\n      (lumi-message (number->string (car lst))) ; Print the first element\n      (print-elements (cdr lst)))))             ; Process the rest of the list\n```\n\n- **Caso base:** lista vacía → `"done"`.\n- **Recursivo:** imprimir `car`, procesar resto con `cdr`.\n\n#### Uso\n\n```scheme\n(print-elements (list 1 2 3))\n```\n\nSalida: *"1"*, *"2"*, *"3"* — resultado: *"done"*\n\n### Resumen\n\n- Caso base para parar; recursivo para reducir.\n- Cada llamada avanza hacia el caso base.\n- Siempre un caso base — o recursión infinita.'}, 'fr': {'title': 'Récursion simple', 'body': 'En Scheme, la récursion signifie qu\'une fonction s\'appelle elle-même pour résoudre des sous-problèmes. Une **récursion simple** a un cas de base pour s\'arrêter et un cas récursif qui réduit le problème.\n\nStructure générale :\n\n```scheme\n(define (function-name args)\n  (if (base-condition)\n    base-result\n    (recursive-call)))\n```\n\n- **Base Condition :** arrête la récursion.\n- **Base Result :** valeur au cas de base.\n- **Recursive Call :** appel avec arguments réduits.\n\n---\n\n### Exemple : somme de 1 à n\n\n```scheme\n(define (sum-to-n n)\n  (if (= n 0)                  ; Base case: stop when n is 0\n    0                          ; Base result: sum is 0\n    (+ n (sum-to-n (- n 1))))) ; Recursive call: sum current n with result of smaller problem\n```\n\n#### Décomposer et recomposer\n\nLa récursion décompose le problème ; chaque appel traite une partie. Au cas de base, le résultat se recompose.\n\n#### Pas à pas : sum-to-n 3\n\n1. *sum-to-n 3* → *(+ 3 (sum-to-n 2))*\n2. *sum-to-n 2* → *(+ 2 (sum-to-n 1))*\n3. *sum-to-n 1* → *(+ 1 (sum-to-n 0))*\n4. *sum-to-n 0* → *0*\n\n#### Recomposer le résultat\n\n1. *sum-to-n 0* → *0*\n2. *sum-to-n 1* → *1*\n3. *sum-to-n 2* → *3*\n4. *sum-to-n 3* → *6*\n\n---\n\n### Exemple : afficher chaque élément\n\n```scheme\n(define (print-elements lst)\n  (if (null? lst)\n    (lumi-message "done")\n    (begin\n      (lumi-message (number->string (car lst))) ; Print the first element\n      (print-elements (cdr lst)))))             ; Process the rest of the list\n```\n\n- **Cas de base :** liste vide → `"done"`.\n- **Récursif :** afficher `car`, traiter le reste avec `cdr`.\n\n#### Utilisation\n\n```scheme\n(print-elements (list 1 2 3))\n```\n\nSortie : *"1"*, *"2"*, *"3"* — résultat : *"done"*\n\n### Résumé\n\n- Cas de base pour arrêter ; cas récursif pour réduire.\n- Chaque appel progresse vers le cas de base.\n- Toujours un cas de base — sinon récursion infinie.'}, 'it': {'title': 'Ricorsione semplice', 'body': 'En Scheme, la récursion signifie qu\'une fonction s\'appelle elle-même pour résoudre des sous-problèmes. Une **récursion simple** a un cas de base pour s\'arrêter et un cas récursif qui réduit le problème.\n\nStructure générale :\n\n```scheme\n(define (function-name args)\n  (if (base-condition)\n    base-result\n    (recursive-call)))\n```\n\n- **Base Condition :** arrête la récursion.\n- **Base Result :** valeur au cas de base.\n- **Recursive Call :** appel avec arguments réduits.\n\n---\n\n### Exemple : somme de 1 à n\n\n```scheme\n(define (sum-to-n n)\n  (if (= n 0)                  ; Base case: stop when n is 0\n    0                          ; Base result: sum is 0\n    (+ n (sum-to-n (- n 1))))) ; Recursive call: sum current n with result of smaller problem\n```\n\n#### Décomposer et recomposer\n\nLa récursion décompose le problème ; chaque appel traite une partie. Au cas de base, le résultat se recompose.\n\n#### Pas à pas : sum-to-n 3\n\n1. *sum-to-n 3* → *(+ 3 (sum-to-n 2))*\n2. *sum-to-n 2* → *(+ 2 (sum-to-n 1))*\n3. *sum-to-n 1* → *(+ 1 (sum-to-n 0))*\n4. *sum-to-n 0* → *0*\n\n#### Recomposer le résultat\n\n1. *sum-to-n 0* → *0*\n2. *sum-to-n 1* → *1*\n3. *sum-to-n 2* → *3*\n4. *sum-to-n 3* → *6*\n\n---\n\n### Exemple : afficher chaque élément\n\n```scheme\n(define (print-elements lst)\n  (if (null? lst)\n    (lumi-message "done")\n    (begin\n      (lumi-message (number->string (car lst))) ; Print the first element\n      (print-elements (cdr lst)))))             ; Process the rest of the list\n```\n\n- **Cas de base :** liste vide → `"done"`.\n- **Récursif :** afficher `car`, traiter le reste avec `cdr`.\n\n#### Utilisation\n\n```scheme\n(print-elements (list 1 2 3))\n```\n\nSortie : *"1"*, *"2"*, *"3"* — résultat : *"done"*\n\n### Résumé\n\n- Cas de base pour arrêter ; cas récursif pour réduire.\n- Chaque appel progresse vers le cas de base.\n- Toujours un cas de base — sinon récursion infinie.'}, 'ja': {'title': '単純な再帰', 'body': 'En Scheme, la récursion signifie qu\'une fonction s\'appelle elle-même pour résoudre des sous-problèmes. Une **récursion simple** a un cas de base pour s\'arrêter et un cas récursif qui réduit le problème.\n\nStructure générale :\n\n```scheme\n(define (function-name args)\n  (if (base-condition)\n    base-result\n    (recursive-call)))\n```\n\n- **Base Condition :** arrête la récursion.\n- **Base Result :** valeur au cas de base.\n- **Recursive Call :** appel avec arguments réduits.\n\n---\n\n### Exemple : somme de 1 à n\n\n```scheme\n(define (sum-to-n n)\n  (if (= n 0)                  ; Base case: stop when n is 0\n    0                          ; Base result: sum is 0\n    (+ n (sum-to-n (- n 1))))) ; Recursive call: sum current n with result of smaller problem\n```\n\n#### Décomposer et recomposer\n\nLa récursion décompose le problème ; chaque appel traite une partie. Au cas de base, le résultat se recompose.\n\n#### Pas à pas : sum-to-n 3\n\n1. *sum-to-n 3* → *(+ 3 (sum-to-n 2))*\n2. *sum-to-n 2* → *(+ 2 (sum-to-n 1))*\n3. *sum-to-n 1* → *(+ 1 (sum-to-n 0))*\n4. *sum-to-n 0* → *0*\n\n#### Recomposer le résultat\n\n1. *sum-to-n 0* → *0*\n2. *sum-to-n 1* → *1*\n3. *sum-to-n 2* → *3*\n4. *sum-to-n 3* → *6*\n\n---\n\n### Exemple : afficher chaque élément\n\n```scheme\n(define (print-elements lst)\n  (if (null? lst)\n    (lumi-message "done")\n    (begin\n      (lumi-message (number->string (car lst))) ; Print the first element\n      (print-elements (cdr lst)))))             ; Process the rest of the list\n```\n\n- **Cas de base :** liste vide → `"done"`.\n- **Récursif :** afficher `car`, traiter le reste avec `cdr`.\n\n#### Utilisation\n\n```scheme\n(print-elements (list 1 2 3))\n```\n\nSortie : *"1"*, *"2"*, *"3"* — résultat : *"done"*\n\n### Résumé\n\n- Cas de base pour arrêter ; cas récursif pour réduire.\n- Chaque appel progresse vers le cas de base.\n- Toujours un cas de base — sinon récursion infinie.'}, 'ko': {'title': '단순 재귀', 'body': 'En Scheme, la récursion signifie qu\'une fonction s\'appelle elle-même pour résoudre des sous-problèmes. Une **récursion simple** a un cas de base pour s\'arrêter et un cas récursif qui réduit le problème.\n\nStructure générale :\n\n```scheme\n(define (function-name args)\n  (if (base-condition)\n    base-result\n    (recursive-call)))\n```\n\n- **Base Condition :** arrête la récursion.\n- **Base Result :** valeur au cas de base.\n- **Recursive Call :** appel avec arguments réduits.\n\n---\n\n### Exemple : somme de 1 à n\n\n```scheme\n(define (sum-to-n n)\n  (if (= n 0)                  ; Base case: stop when n is 0\n    0                          ; Base result: sum is 0\n    (+ n (sum-to-n (- n 1))))) ; Recursive call: sum current n with result of smaller problem\n```\n\n#### Décomposer et recomposer\n\nLa récursion décompose le problème ; chaque appel traite une partie. Au cas de base, le résultat se recompose.\n\n#### Pas à pas : sum-to-n 3\n\n1. *sum-to-n 3* → *(+ 3 (sum-to-n 2))*\n2. *sum-to-n 2* → *(+ 2 (sum-to-n 1))*\n3. *sum-to-n 1* → *(+ 1 (sum-to-n 0))*\n4. *sum-to-n 0* → *0*\n\n#### Recomposer le résultat\n\n1. *sum-to-n 0* → *0*\n2. *sum-to-n 1* → *1*\n3. *sum-to-n 2* → *3*\n4. *sum-to-n 3* → *6*\n\n---\n\n### Exemple : afficher chaque élément\n\n```scheme\n(define (print-elements lst)\n  (if (null? lst)\n    (lumi-message "done")\n    (begin\n      (lumi-message (number->string (car lst))) ; Print the first element\n      (print-elements (cdr lst)))))             ; Process the rest of the list\n```\n\n- **Cas de base :** liste vide → `"done"`.\n- **Récursif :** afficher `car`, traiter le reste avec `cdr`.\n\n#### Utilisation\n\n```scheme\n(print-elements (list 1 2 3))\n```\n\nSortie : *"1"*, *"2"*, *"3"* — résultat : *"done"*\n\n### Résumé\n\n- Cas de base pour arrêter ; cas récursif pour réduire.\n- Chaque appel progresse vers le cas de base.\n- Toujours un cas de base — sinon récursion infinie.'}, 'nl': {'title': 'Eenvoudige recursie', 'body': 'En Scheme, la récursion signifie qu\'une fonction s\'appelle elle-même pour résoudre des sous-problèmes. Une **récursion simple** a un cas de base pour s\'arrêter et un cas récursif qui réduit le problème.\n\nStructure générale :\n\n```scheme\n(define (function-name args)\n  (if (base-condition)\n    base-result\n    (recursive-call)))\n```\n\n- **Base Condition :** arrête la récursion.\n- **Base Result :** valeur au cas de base.\n- **Recursive Call :** appel avec arguments réduits.\n\n---\n\n### Exemple : somme de 1 à n\n\n```scheme\n(define (sum-to-n n)\n  (if (= n 0)                  ; Base case: stop when n is 0\n    0                          ; Base result: sum is 0\n    (+ n (sum-to-n (- n 1))))) ; Recursive call: sum current n with result of smaller problem\n```\n\n#### Décomposer et recomposer\n\nLa récursion décompose le problème ; chaque appel traite une partie. Au cas de base, le résultat se recompose.\n\n#### Pas à pas : sum-to-n 3\n\n1. *sum-to-n 3* → *(+ 3 (sum-to-n 2))*\n2. *sum-to-n 2* → *(+ 2 (sum-to-n 1))*\n3. *sum-to-n 1* → *(+ 1 (sum-to-n 0))*\n4. *sum-to-n 0* → *0*\n\n#### Recomposer le résultat\n\n1. *sum-to-n 0* → *0*\n2. *sum-to-n 1* → *1*\n3. *sum-to-n 2* → *3*\n4. *sum-to-n 3* → *6*\n\n---\n\n### Exemple : afficher chaque élément\n\n```scheme\n(define (print-elements lst)\n  (if (null? lst)\n    (lumi-message "done")\n    (begin\n      (lumi-message (number->string (car lst))) ; Print the first element\n      (print-elements (cdr lst)))))             ; Process the rest of the list\n```\n\n- **Cas de base :** liste vide → `"done"`.\n- **Récursif :** afficher `car`, traiter le reste avec `cdr`.\n\n#### Utilisation\n\n```scheme\n(print-elements (list 1 2 3))\n```\n\nSortie : *"1"*, *"2"*, *"3"* — résultat : *"done"*\n\n### Résumé\n\n- Cas de base pour arrêter ; cas récursif pour réduire.\n- Chaque appel progresse vers le cas de base.\n- Toujours un cas de base — sinon récursion infinie.'}, 'pl': {'title': 'Prosta rekursja', 'body': 'En Scheme, la récursion signifie qu\'une fonction s\'appelle elle-même pour résoudre des sous-problèmes. Une **récursion simple** a un cas de base pour s\'arrêter et un cas récursif qui réduit le problème.\n\nStructure générale :\n\n```scheme\n(define (function-name args)\n  (if (base-condition)\n    base-result\n    (recursive-call)))\n```\n\n- **Base Condition :** arrête la récursion.\n- **Base Result :** valeur au cas de base.\n- **Recursive Call :** appel avec arguments réduits.\n\n---\n\n### Exemple : somme de 1 à n\n\n```scheme\n(define (sum-to-n n)\n  (if (= n 0)                  ; Base case: stop when n is 0\n    0                          ; Base result: sum is 0\n    (+ n (sum-to-n (- n 1))))) ; Recursive call: sum current n with result of smaller problem\n```\n\n#### Décomposer et recomposer\n\nLa récursion décompose le problème ; chaque appel traite une partie. Au cas de base, le résultat se recompose.\n\n#### Pas à pas : sum-to-n 3\n\n1. *sum-to-n 3* → *(+ 3 (sum-to-n 2))*\n2. *sum-to-n 2* → *(+ 2 (sum-to-n 1))*\n3. *sum-to-n 1* → *(+ 1 (sum-to-n 0))*\n4. *sum-to-n 0* → *0*\n\n#### Recomposer le résultat\n\n1. *sum-to-n 0* → *0*\n2. *sum-to-n 1* → *1*\n3. *sum-to-n 2* → *3*\n4. *sum-to-n 3* → *6*\n\n---\n\n### Exemple : afficher chaque élément\n\n```scheme\n(define (print-elements lst)\n  (if (null? lst)\n    (lumi-message "done")\n    (begin\n      (lumi-message (number->string (car lst))) ; Print the first element\n      (print-elements (cdr lst)))))             ; Process the rest of the list\n```\n\n- **Cas de base :** liste vide → `"done"`.\n- **Récursif :** afficher `car`, traiter le reste avec `cdr`.\n\n#### Utilisation\n\n```scheme\n(print-elements (list 1 2 3))\n```\n\nSortie : *"1"*, *"2"*, *"3"* — résultat : *"done"*\n\n### Résumé\n\n- Cas de base pour arrêter ; cas récursif pour réduire.\n- Chaque appel progresse vers le cas de base.\n- Toujours un cas de base — sinon récursion infinie.'}, 'pt-br': {'title': 'Recursão simples', 'body': 'En Scheme, la récursion signifie qu\'une fonction s\'appelle elle-même pour résoudre des sous-problèmes. Une **récursion simple** a un cas de base pour s\'arrêter et un cas récursif qui réduit le problème.\n\nStructure générale :\n\n```scheme\n(define (function-name args)\n  (if (base-condition)\n    base-result\n    (recursive-call)))\n```\n\n- **Base Condition :** arrête la récursion.\n- **Base Result :** valeur au cas de base.\n- **Recursive Call :** appel avec arguments réduits.\n\n---\n\n### Exemple : somme de 1 à n\n\n```scheme\n(define (sum-to-n n)\n  (if (= n 0)                  ; Base case: stop when n is 0\n    0                          ; Base result: sum is 0\n    (+ n (sum-to-n (- n 1))))) ; Recursive call: sum current n with result of smaller problem\n```\n\n#### Décomposer et recomposer\n\nLa récursion décompose le problème ; chaque appel traite une partie. Au cas de base, le résultat se recompose.\n\n#### Pas à pas : sum-to-n 3\n\n1. *sum-to-n 3* → *(+ 3 (sum-to-n 2))*\n2. *sum-to-n 2* → *(+ 2 (sum-to-n 1))*\n3. *sum-to-n 1* → *(+ 1 (sum-to-n 0))*\n4. *sum-to-n 0* → *0*\n\n#### Recomposer le résultat\n\n1. *sum-to-n 0* → *0*\n2. *sum-to-n 1* → *1*\n3. *sum-to-n 2* → *3*\n4. *sum-to-n 3* → *6*\n\n---\n\n### Exemple : afficher chaque élément\n\n```scheme\n(define (print-elements lst)\n  (if (null? lst)\n    (lumi-message "done")\n    (begin\n      (lumi-message (number->string (car lst))) ; Print the first element\n      (print-elements (cdr lst)))))             ; Process the rest of the list\n```\n\n- **Cas de base :** liste vide → `"done"`.\n- **Récursif :** afficher `car`, traiter le reste avec `cdr`.\n\n#### Utilisation\n\n```scheme\n(print-elements (list 1 2 3))\n```\n\nSortie : *"1"*, *"2"*, *"3"* — résultat : *"done"*\n\n### Résumé\n\n- Cas de base pour arrêter ; cas récursif pour réduire.\n- Chaque appel progresse vers le cas de base.\n- Toujours un cas de base — sinon récursion infinie.'}, 'ru': {'title': 'Простая рекурсия', 'body': 'En Scheme, la récursion signifie qu\'une fonction s\'appelle elle-même pour résoudre des sous-problèmes. Une **récursion simple** a un cas de base pour s\'arrêter et un cas récursif qui réduit le problème.\n\nStructure générale :\n\n```scheme\n(define (function-name args)\n  (if (base-condition)\n    base-result\n    (recursive-call)))\n```\n\n- **Base Condition :** arrête la récursion.\n- **Base Result :** valeur au cas de base.\n- **Recursive Call :** appel avec arguments réduits.\n\n---\n\n### Exemple : somme de 1 à n\n\n```scheme\n(define (sum-to-n n)\n  (if (= n 0)                  ; Base case: stop when n is 0\n    0                          ; Base result: sum is 0\n    (+ n (sum-to-n (- n 1))))) ; Recursive call: sum current n with result of smaller problem\n```\n\n#### Décomposer et recomposer\n\nLa récursion décompose le problème ; chaque appel traite une partie. Au cas de base, le résultat se recompose.\n\n#### Pas à pas : sum-to-n 3\n\n1. *sum-to-n 3* → *(+ 3 (sum-to-n 2))*\n2. *sum-to-n 2* → *(+ 2 (sum-to-n 1))*\n3. *sum-to-n 1* → *(+ 1 (sum-to-n 0))*\n4. *sum-to-n 0* → *0*\n\n#### Recomposer le résultat\n\n1. *sum-to-n 0* → *0*\n2. *sum-to-n 1* → *1*\n3. *sum-to-n 2* → *3*\n4. *sum-to-n 3* → *6*\n\n---\n\n### Exemple : afficher chaque élément\n\n```scheme\n(define (print-elements lst)\n  (if (null? lst)\n    (lumi-message "done")\n    (begin\n      (lumi-message (number->string (car lst))) ; Print the first element\n      (print-elements (cdr lst)))))             ; Process the rest of the list\n```\n\n- **Cas de base :** liste vide → `"done"`.\n- **Récursif :** afficher `car`, traiter le reste avec `cdr`.\n\n#### Utilisation\n\n```scheme\n(print-elements (list 1 2 3))\n```\n\nSortie : *"1"*, *"2"*, *"3"* — résultat : *"done"*\n\n### Résumé\n\n- Cas de base pour arrêter ; cas récursif pour réduire.\n- Chaque appel progresse vers le cas de base.\n- Toujours un cas de base — sinon récursion infinie.'}, 'sv': {'title': 'Enkel rekursion', 'body': 'En Scheme, la récursion signifie qu\'une fonction s\'appelle elle-même pour résoudre des sous-problèmes. Une **récursion simple** a un cas de base pour s\'arrêter et un cas récursif qui réduit le problème.\n\nStructure générale :\n\n```scheme\n(define (function-name args)\n  (if (base-condition)\n    base-result\n    (recursive-call)))\n```\n\n- **Base Condition :** arrête la récursion.\n- **Base Result :** valeur au cas de base.\n- **Recursive Call :** appel avec arguments réduits.\n\n---\n\n### Exemple : somme de 1 à n\n\n```scheme\n(define (sum-to-n n)\n  (if (= n 0)                  ; Base case: stop when n is 0\n    0                          ; Base result: sum is 0\n    (+ n (sum-to-n (- n 1))))) ; Recursive call: sum current n with result of smaller problem\n```\n\n#### Décomposer et recomposer\n\nLa récursion décompose le problème ; chaque appel traite une partie. Au cas de base, le résultat se recompose.\n\n#### Pas à pas : sum-to-n 3\n\n1. *sum-to-n 3* → *(+ 3 (sum-to-n 2))*\n2. *sum-to-n 2* → *(+ 2 (sum-to-n 1))*\n3. *sum-to-n 1* → *(+ 1 (sum-to-n 0))*\n4. *sum-to-n 0* → *0*\n\n#### Recomposer le résultat\n\n1. *sum-to-n 0* → *0*\n2. *sum-to-n 1* → *1*\n3. *sum-to-n 2* → *3*\n4. *sum-to-n 3* → *6*\n\n---\n\n### Exemple : afficher chaque élément\n\n```scheme\n(define (print-elements lst)\n  (if (null? lst)\n    (lumi-message "done")\n    (begin\n      (lumi-message (number->string (car lst))) ; Print the first element\n      (print-elements (cdr lst)))))             ; Process the rest of the list\n```\n\n- **Cas de base :** liste vide → `"done"`.\n- **Récursif :** afficher `car`, traiter le reste avec `cdr`.\n\n#### Utilisation\n\n```scheme\n(print-elements (list 1 2 3))\n```\n\nSortie : *"1"*, *"2"*, *"3"* — résultat : *"done"*\n\n### Résumé\n\n- Cas de base pour arrêter ; cas récursif pour réduire.\n- Chaque appel progresse vers le cas de base.\n- Toujours un cas de base — sinon récursion infinie.'}, 'th': {'title': 'การเรียกซ้ำแบบง่าย', 'body': 'En Scheme, la récursion signifie qu\'une fonction s\'appelle elle-même pour résoudre des sous-problèmes. Une **récursion simple** a un cas de base pour s\'arrêter et un cas récursif qui réduit le problème.\n\nStructure générale :\n\n```scheme\n(define (function-name args)\n  (if (base-condition)\n    base-result\n    (recursive-call)))\n```\n\n- **Base Condition :** arrête la récursion.\n- **Base Result :** valeur au cas de base.\n- **Recursive Call :** appel avec arguments réduits.\n\n---\n\n### Exemple : somme de 1 à n\n\n```scheme\n(define (sum-to-n n)\n  (if (= n 0)                  ; Base case: stop when n is 0\n    0                          ; Base result: sum is 0\n    (+ n (sum-to-n (- n 1))))) ; Recursive call: sum current n with result of smaller problem\n```\n\n#### Décomposer et recomposer\n\nLa récursion décompose le problème ; chaque appel traite une partie. Au cas de base, le résultat se recompose.\n\n#### Pas à pas : sum-to-n 3\n\n1. *sum-to-n 3* → *(+ 3 (sum-to-n 2))*\n2. *sum-to-n 2* → *(+ 2 (sum-to-n 1))*\n3. *sum-to-n 1* → *(+ 1 (sum-to-n 0))*\n4. *sum-to-n 0* → *0*\n\n#### Recomposer le résultat\n\n1. *sum-to-n 0* → *0*\n2. *sum-to-n 1* → *1*\n3. *sum-to-n 2* → *3*\n4. *sum-to-n 3* → *6*\n\n---\n\n### Exemple : afficher chaque élément\n\n```scheme\n(define (print-elements lst)\n  (if (null? lst)\n    (lumi-message "done")\n    (begin\n      (lumi-message (number->string (car lst))) ; Print the first element\n      (print-elements (cdr lst)))))             ; Process the rest of the list\n```\n\n- **Cas de base :** liste vide → `"done"`.\n- **Récursif :** afficher `car`, traiter le reste avec `cdr`.\n\n#### Utilisation\n\n```scheme\n(print-elements (list 1 2 3))\n```\n\nSortie : *"1"*, *"2"*, *"3"* — résultat : *"done"*\n\n### Résumé\n\n- Cas de base pour arrêter ; cas récursif pour réduire.\n- Chaque appel progresse vers le cas de base.\n- Toujours un cas de base — sinon récursion infinie.'}, 'zh-cn': {'title': '简单递归', 'body': 'En Scheme, la récursion signifie qu\'une fonction s\'appelle elle-même pour résoudre des sous-problèmes. Une **récursion simple** a un cas de base pour s\'arrêter et un cas récursif qui réduit le problème.\n\nStructure générale :\n\n```scheme\n(define (function-name args)\n  (if (base-condition)\n    base-result\n    (recursive-call)))\n```\n\n- **Base Condition :** arrête la récursion.\n- **Base Result :** valeur au cas de base.\n- **Recursive Call :** appel avec arguments réduits.\n\n---\n\n### Exemple : somme de 1 à n\n\n```scheme\n(define (sum-to-n n)\n  (if (= n 0)                  ; Base case: stop when n is 0\n    0                          ; Base result: sum is 0\n    (+ n (sum-to-n (- n 1))))) ; Recursive call: sum current n with result of smaller problem\n```\n\n#### Décomposer et recomposer\n\nLa récursion décompose le problème ; chaque appel traite une partie. Au cas de base, le résultat se recompose.\n\n#### Pas à pas : sum-to-n 3\n\n1. *sum-to-n 3* → *(+ 3 (sum-to-n 2))*\n2. *sum-to-n 2* → *(+ 2 (sum-to-n 1))*\n3. *sum-to-n 1* → *(+ 1 (sum-to-n 0))*\n4. *sum-to-n 0* → *0*\n\n#### Recomposer le résultat\n\n1. *sum-to-n 0* → *0*\n2. *sum-to-n 1* → *1*\n3. *sum-to-n 2* → *3*\n4. *sum-to-n 3* → *6*\n\n---\n\n### Exemple : afficher chaque élément\n\n```scheme\n(define (print-elements lst)\n  (if (null? lst)\n    (lumi-message "done")\n    (begin\n      (lumi-message (number->string (car lst))) ; Print the first element\n      (print-elements (cdr lst)))))             ; Process the rest of the list\n```\n\n- **Cas de base :** liste vide → `"done"`.\n- **Récursif :** afficher `car`, traiter le reste avec `cdr`.\n\n#### Utilisation\n\n```scheme\n(print-elements (list 1 2 3))\n```\n\nSortie : *"1"*, *"2"*, *"3"* — résultat : *"done"*\n\n### Résumé\n\n- Cas de base pour arrêter ; cas récursif pour réduire.\n- Chaque appel progresse vers le cas de base.\n- Toujours un cas de base — sinon récursion infinie.'}, 'zh-tw': {'title': '簡單遞迴', 'body': 'En Scheme, la récursion signifie qu\'une fonction s\'appelle elle-même pour résoudre des sous-problèmes. Une **récursion simple** a un cas de base pour s\'arrêter et un cas récursif qui réduit le problème.\n\nStructure générale :\n\n```scheme\n(define (function-name args)\n  (if (base-condition)\n    base-result\n    (recursive-call)))\n```\n\n- **Base Condition :** arrête la récursion.\n- **Base Result :** valeur au cas de base.\n- **Recursive Call :** appel avec arguments réduits.\n\n---\n\n### Exemple : somme de 1 à n\n\n```scheme\n(define (sum-to-n n)\n  (if (= n 0)                  ; Base case: stop when n is 0\n    0                          ; Base result: sum is 0\n    (+ n (sum-to-n (- n 1))))) ; Recursive call: sum current n with result of smaller problem\n```\n\n#### Décomposer et recomposer\n\nLa récursion décompose le problème ; chaque appel traite une partie. Au cas de base, le résultat se recompose.\n\n#### Pas à pas : sum-to-n 3\n\n1. *sum-to-n 3* → *(+ 3 (sum-to-n 2))*\n2. *sum-to-n 2* → *(+ 2 (sum-to-n 1))*\n3. *sum-to-n 1* → *(+ 1 (sum-to-n 0))*\n4. *sum-to-n 0* → *0*\n\n#### Recomposer le résultat\n\n1. *sum-to-n 0* → *0*\n2. *sum-to-n 1* → *1*\n3. *sum-to-n 2* → *3*\n4. *sum-to-n 3* → *6*\n\n---\n\n### Exemple : afficher chaque élément\n\n```scheme\n(define (print-elements lst)\n  (if (null? lst)\n    (lumi-message "done")\n    (begin\n      (lumi-message (number->string (car lst))) ; Print the first element\n      (print-elements (cdr lst)))))             ; Process the rest of the list\n```\n\n- **Cas de base :** liste vide → `"done"`.\n- **Récursif :** afficher `car`, traiter le reste avec `cdr`.\n\n#### Utilisation\n\n```scheme\n(print-elements (list 1 2 3))\n```\n\nSortie : *"1"*, *"2"*, *"3"* — résultat : *"done"*\n\n### Résumé\n\n- Cas de base pour arrêter ; cas récursif pour réduire.\n- Chaque appel progresse vers le cas de base.\n- Toujours un cas de base — sinon récursion infinie.'}}
    meta = bodies[lang]
    return meta["title"], meta["body"]

PAGE_BUILDERS = {
    "content/hub/scripting/fundamentals/Conditionals/_index.md": cond_index,
    "content/hub/scripting/fundamentals/Conditionals/conditionals-cond.md": _page_content_hub_scripting_fundamentals_Conditionals_conditionals_cond_md,
    "content/hub/scripting/fundamentals/Conditionals/conditionals-if.md": _page_content_hub_scripting_fundamentals_Conditionals_conditionals_if_md,
    "content/hub/scripting/fundamentals/Conditionals/conditionals-when.md": _page_content_hub_scripting_fundamentals_Conditionals_conditionals_when_md,
    "content/hub/scripting/fundamentals/Iteration/_index.md": _page_content_hub_scripting_fundamentals_Iteration__index_md,
    "content/hub/scripting/fundamentals/Iteration/do.md": _page_content_hub_scripting_fundamentals_Iteration_do_md,
    "content/hub/scripting/fundamentals/Iteration/for-each.md": _page_content_hub_scripting_fundamentals_Iteration_for_each_md,
    "content/hub/scripting/fundamentals/Iteration/map.md": _page_content_hub_scripting_fundamentals_Iteration_map_md,
    "content/hub/scripting/fundamentals/Iteration/recursion.md": _page_content_hub_scripting_fundamentals_Iteration_recursion_md,
}
